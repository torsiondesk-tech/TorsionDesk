---
phase: 00-foundation-tenancy-auth-and-data-spine
plan: 04
type: execute
wave: 3
depends_on: [00-03]
files_modified:
  - src/app/(app)/settings/layout.tsx
  - src/app/(app)/settings/company-profile/page.tsx
  - src/app/(app)/settings/company-profile/actions.ts
  - src/app/(app)/settings/users/page.tsx
  - src/app/(app)/settings/users/actions.ts
  - src/lib/storage.ts
autonomous: false
requirements: [AUTH-02, TENANT-02]
user_setup:
  - service: supabase
    why: "Storage bucket for the tenant logo (TENANT-02, D-12)"
    dashboard_config:
      - task: "Create a Storage bucket named tenant-assets (private)"
        location: "Supabase Dashboard → Storage → Buckets"
must_haves:
  truths:
    - "An admin can set and persist the full company profile (name, address, phone, email) and it survives a reload, scoped to their tenant"
    - "An admin can upload a logo; the file is stored in Supabase Storage under the tenant's folder and the stored URL is persisted on the tenants row"
    - "An admin can invite a teammate by email and assign a role (Admin / Dispatcher / Technician) via Clerk's built-in invitation system"
    - "The Settings page shows all future tab sections; only Company Profile and Users are functional, the rest render 'Available in Phase N' stubs with real phase numbers (2, 3, 7, 8)"
    - "The Drizzle schema is pushed to the database so the tenants table and its RLS policy physically exist"
  artifacts:
    - path: "src/app/(app)/settings/layout.tsx"
      provides: "settings tab shell with all tabs (functional + stubbed)"
      contains: "Company Profile"
    - path: "src/app/(app)/settings/company-profile/page.tsx"
      provides: "full profile form + logo upload"
      contains: "logo"
    - path: "src/app/(app)/settings/company-profile/actions.ts"
      provides: "save profile + upload logo server actions"
      contains: "saveProfile"
    - path: "src/app/(app)/settings/users/page.tsx"
      provides: "invite-by-email + role assign UI"
      min_lines: 20
    - path: "src/lib/storage.ts"
      provides: "tenant-scoped logo upload to Supabase Storage"
      exports: ["uploadLogo"]
  key_links:
    - from: "src/app/(app)/settings/company-profile/actions.ts"
      to: "src/lib/profile.ts"
      via: "saveProfile/getProfile"
      pattern: "saveProfile"
    - from: "src/app/(app)/settings/company-profile/actions.ts"
      to: "src/lib/storage.ts"
      via: "uploadLogo"
      pattern: "uploadLogo"
    - from: "src/app/(app)/settings/users/actions.ts"
      to: "Clerk invitations"
      via: "clerkClient organizations invite"
      pattern: "[Ii]nvit"
---

<objective>
Make the two functional Settings tabs real and push the schema to the database. Build the Settings tab shell showing ALL future tabs (Company Profile, Users, Job Categories, Tags, Templates, Email, SMS, Payment Methods, Tax Items, Lookup Lists) with only Company Profile and Users functional and the rest stubbed; build the full company-profile form (name/address/phone/email + Supabase-Storage logo upload, TENANT-02); build the Users tab that invites teammates by email and assigns a role via Clerk's built-in invitation system (AUTH-02); then run the [BLOCKING] `drizzle-kit push` so the `tenants` table and its RLS policy physically exist.

Purpose: TENANT-02 (business profile + logo for later invoices/email headers) and AUTH-02 (team invitations) are the last functional pieces; the schema push is the gate that turns the in-code RLS policy into a real database constraint, without which the cross-tenant integration test (Plan 05) cannot pass.
Output: Functional Company Profile and Users settings tabs, a logo stored in Storage, all other tabs stubbed, and the schema applied to Supabase.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-SKELETON.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `src/app/(app)/settings/layout.tsx` — settings tab nav: Company Profile + Users (functional), and stubbed tabs with real phase labels: Job Categories/Tags/Tax Items/Lookup Lists → Phase 2, Templates → Phase 3, Payment Methods → Phase 7, Email/SMS → Phase 8
- `src/app/(app)/settings/company-profile/page.tsx` — form: company name, address, phone, email, logo upload + preview
- `src/app/(app)/settings/company-profile/actions.ts` — `saveCompanyProfile` (via `saveProfile`) + `uploadCompanyLogo` (via `uploadLogo`) server actions
- `src/app/(app)/settings/users/page.tsx` — invite-by-email + role select (Admin/Dispatcher/Technician) + member list
- `src/app/(app)/settings/users/actions.ts` — `inviteUser(email, role)` via Clerk organization invitations
- `src/lib/storage.ts` — `export async function uploadLogo(orgId, file)` → Supabase Storage `tenant-assets/{orgId}/logo.*`, returns the stored path/URL, persisted via `saveProfile`
- DB: `tenants` table + `tenant_self_isolation` RLS policy pushed to Supabase via `drizzle-kit push`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settings tab shell + functional Company Profile tab (profile + logo upload)</name>
  <files>src/app/(app)/settings/layout.tsx, src/app/(app)/settings/company-profile/page.tsx, src/app/(app)/settings/company-profile/actions.ts, src/lib/storage.ts</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md (D-13 all Settings tabs stubbed from Phase 0: Company Profile, Users, Job Categories, Tags, Templates, Email, SMS, Payment Methods, Tax Items, Lookup Lists — only Company Profile and Users functional, rest "Available in Phase N"; D-11 full profile address/email/logo lives here; D-12 logo upload via Supabase Storage, later phases read stored URL; D-15 settings admin-only)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (Pattern 8 Storage `tenant-assets/{org_id}/logo.*`, option (a) service-role server-side upload then store path; A3 service-role upload; § Security service-role key server-only)
    - src/lib/profile.ts (saveProfile/getProfile signatures), src/db/schema.ts (tenants.logo_url column)
    - C:/Users/marco/.claude/CLAUDE.md (UI quality standard)
  </read_first>
  <action>
    Create `src/lib/storage.ts`: export `async function uploadLogo(orgId: string, file)` that uploads to the Supabase Storage bucket `tenant-assets` at path `{orgId}/logo.<ext>` using a server-side Supabase client created with `SUPABASE_SERVICE_ROLE_KEY` (Pattern 8 option a — service role bypasses Storage RLS for a trusted server upload; the key is server-only, threat T-00-04) and return the resulting object path/URL. Create `src/app/(app)/settings/layout.tsx`: a tab navigation listing ALL settings tabs in order — Company Profile, Users, Job Categories, Tags, Templates, Email, SMS, Payment Methods, Tax Items, Lookup Lists (D-13) — where Company Profile and Users link to real routes and every other tab renders a stub (greyed, non-clickable) with the actual phase number noted: Job Categories → "Available in Phase 2", Tags → "Available in Phase 2", Templates → "Available in Phase 3", Email → "Available in Phase 8", SMS → "Available in Phase 8", Payment Methods → "Available in Phase 7", Tax Items → "Available in Phase 2", Lookup Lists → "Available in Phase 2". Create `src/app/(app)/settings/company-profile/page.tsx`: a form pre-filled via `getProfile(orgId)` with fields company name, address, phone, email, and a logo file input with a current-logo preview. Create `src/app/(app)/settings/company-profile/actions.ts`: a `saveCompanyProfile` server action that zod-validates the fields and calls `saveProfile(orgId, ...)`, and an `uploadCompanyLogo` action that calls `uploadLogo(orgId, file)` then persists the returned URL via `saveProfile` (so `tenants.logo_url` is set). Read `orgId` from `auth()` in every action. Apply the global UI quality standard.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/settings/layout.tsx` lists all ten settings tab names and renders "Available in Phase N" stubs for the eight non-functional tabs, each with a real phase number (2, 3, 7, or 8)
    - `src/app/(app)/settings/company-profile/page.tsx` contains inputs for company name, address, phone, email, and a logo file input
    - `src/lib/storage.ts` exports `uploadLogo` and uploads to the `tenant-assets` bucket under `{orgId}/`
    - `src/app/(app)/settings/company-profile/actions.ts` calls `saveProfile` and persists the uploaded logo URL
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The Settings shell shows all tabs (only Company Profile + Users functional), and the Company Profile tab saves the full profile and uploads a tenant-scoped logo to Storage.</done>
</task>

<task type="auto">
  <name>Task 2: Functional Users tab — invite by email + assign role via Clerk</name>
  <files>src/app/(app)/settings/users/page.tsx, src/app/(app)/settings/users/actions.ts</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md (D-06 invitations via Clerk's built-in system — admin enters email + role, Clerk sends invite + handles accept; NO custom token management or Resend invite emails in Phase 0; D-15 settings admin-only)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (AUTH-02 Clerk Organization invitations with custom org roles org:admin/org:dispatcher/org:technician; Don't Hand-Roll — use Clerk org invitations not custom invite tokens; A2 role keys)
    - C:/Users/marco/.claude/CLAUDE.md (UI quality standard)
  </read_first>
  <action>
    Create `src/app/(app)/settings/users/actions.ts`: export `inviteUser(email, role)` that uses the Clerk backend SDK (`clerkClient`) to create an organization invitation for the admin's active org, mapping the chosen role to the exact Clerk org role key (`org:admin`, `org:dispatcher`, or `org:technician` — A2; never invent new keys). Clerk sends the invite email and owns the accept flow (D-06) — do NOT build custom invitation tokens or send via Resend. zod-validate the email and constrain `role` to the three allowed keys. Create `src/app/(app)/settings/users/page.tsx`: a form with an email input and a role select (Admin / Dispatcher / Technician), plus a list of current organization members and any pending invitations read via the Clerk SDK. Apply the global UI quality standard. This tab is reachable only because middleware (Plan 03) admin-gates `/settings`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/settings/users/actions.ts` exports `inviteUser` and creates a Clerk organization invitation (no custom token table, no Resend)
    - The role select offers exactly Admin, Dispatcher, Technician and maps them to `org:admin`/`org:dispatcher`/`org:technician`
    - `src/app/(app)/settings/users/page.tsx` contains an email input and a role select and lists members/pending invites
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>An admin can invite a teammate by email with a role through Clerk's built-in invitation system, and sees the member/invite list.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Push Drizzle schema to Supabase (tenants + RLS policy)</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (drizzle-kit push; Pattern 3 RLS policy must exist in the DB; Pitfall 3 — use the direct connection :5432 for migrations/push, transaction pooler :6543 for serverless runtime)
    - CLAUDE.md (pnpm only — `pnpm exec drizzle-kit`, never npx)
    - src/db/schema.ts (the schema being pushed), drizzle.config.ts (created in Plan 02)
  </read_first>
  <action>
    Run `pnpm exec drizzle-kit push` (pnpm — never npx) to apply `src/db/schema.ts` to the Supabase project, creating the `tenants` table and its `tenant_self_isolation` RLS policy in the database. This MUST run AFTER all schema-relevant files are final (Plan 02 schema is unchanged by this plan) and BEFORE the Plan 05 cross-tenant integration test, which queries the real table. `drizzle-kit push` is non-interactive by default; if it prompts for a destructive confirmation that cannot be suppressed, stop and flag for manual intervention (this is why the plan is `autonomous: false`). Prefer the direct connection string (:5432) for the push rather than the transaction pooler (:6543) per RESEARCH Pitfall 3. After push, confirm the table exists by listing it via drizzle-kit or a direct `select` so the BLOCKING gate is provably satisfied.
  </action>
  <verify>
    <automated>pnpm exec drizzle-kit push 2>&1 | grep -v '^#' | grep -Eqi 'changes applied|No changes detected|done|pushed' && echo SCHEMA-PUSHED</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec drizzle-kit push` completes without error (changes applied or no-changes)
    - The `tenants` table physically exists in the Supabase database after the push
    - The `tenant_self_isolation` RLS policy exists on the `tenants` table
    - Command was run with pnpm (no npx/npm/yarn)
  </acceptance_criteria>
  <done>The Drizzle schema is applied to Supabase; the tenants table and its RLS policy physically exist, unblocking the Plan 05 cross-tenant test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (admin) → settings actions | Profile/invite mutations must be tenant-scoped and admin-only |
| Server → Supabase Storage | Logo upload uses the service-role key (server-only); must scope to the tenant's folder |
| App → Clerk backend | Invitation creation must map only to the three allowed org role keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | Information Disclosure | Supabase service-role key used for logo upload | mitigate | Key used server-side only in `src/lib/storage.ts`; never in a `NEXT_PUBLIC_*` var or client bundle; upload path constrained to `{orgId}/` |
| T-00-10 | Elevation of Privilege | Inviting a user with an arbitrary/elevated role | mitigate | `inviteUser` constrains `role` to exactly `org:admin`/`org:dispatcher`/`org:technician` via zod; settings is admin-gated by middleware (T-00-06) |
| T-00-11 | Tampering / Information Disclosure | Profile/logo writes escaping tenant scope | mitigate | `saveProfile` runs through `withTenant`; logo path is `{orgId}/`; RLS policy (pushed in Task 3) enforces `id = current_setting('app.current_tenant_id', true)` |
| T-00-05 | Tampering | Schema push against the transaction pooler | mitigate | Push uses the direct connection (:5432); runtime uses pooler (:6543) with `prepare:false` |
| T-00-SC | Tampering | No new package installs in this plan | accept | This plan installs no new packages; nothing to audit |
</threat_model>

<verification>
- `pnpm build` exits 0
- `pnpm exec drizzle-kit push` applies the schema; `tenants` table + RLS policy exist in Supabase
- Manual: admin sets full company profile + uploads logo → persists across reload; admin invites a teammate with a role → Clerk sends the invite
- Service-role key appears only in server code, never in client/`NEXT_PUBLIC_*`
</verification>

<success_criteria>
- Settings shell shows all tabs; only Company Profile + Users functional, rest stubbed (D-13)
- Full company profile (name/address/phone/email) persists; logo uploads to tenant-scoped Storage (TENANT-02)
- Admin invites teammates by email with a role via Clerk's built-in system (AUTH-02)
- [BLOCKING] schema pushed — tenants table + RLS policy live in Supabase
</success_criteria>

<output>
Create `.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-04-SUMMARY.md` when done
</output>
