---
phase: 00-foundation-tenancy-auth-and-data-spine
plan: 05
type: execute
wave: 4
depends_on: [00-04]
files_modified:
  - tests/rls-cross-tenant.test.ts
  - tests/profile.test.ts
  - package.json
autonomous: false
requirements: [TENANT-01, TENANT-02]
must_haves:
  truths:
    - "Against the real Supabase test project, a row inserted as Tenant A is invisible to a Tenant B session (cross-tenant read returns 0 rows)"
    - "A query with no tenant GUC set returns 0 rows (fail-closed), proving RLS denies by default rather than leaking"
    - "The business-profile round-trip integration test passes against the real database under the correct tenant"
    - "The full vitest suite is green and a `pnpm test` script runs it"
  artifacts:
    - path: "tests/rls-cross-tenant.test.ts"
      provides: "GREEN cross-tenant + fail-closed RLS proof against the real test project"
      contains: "withTenant"
    - path: "package.json"
      provides: "test script wiring the full suite"
      contains: "vitest"
  key_links:
    - from: "tests/rls-cross-tenant.test.ts"
      to: "TEST_DATABASE_URL"
      via: "non-service-role test connection"
      pattern: "TEST_DATABASE_URL"
    - from: "tests/rls-cross-tenant.test.ts"
      to: "src/db/with-tenant.ts"
      via: "withTenant"
      pattern: "withTenant"
---

<objective>
Turn the Phase 0 keystone test GREEN: run the cross-tenant RLS integration test (`tests/rls-cross-tenant.test.ts`) against the real Supabase TEST project and prove that Tenant B cannot read a Tenant A row and that an unset tenant GUC returns zero rows (fail-closed). Confirm the TENANT-02 profile round-trip passes against the real DB, and wire a `pnpm test` script that runs the full green suite — the permanent CI guard against RLS regressions.

Purpose: This is success criterion #4 of the entire phase and the single provable property the walking skeleton exists to demonstrate. Until this test runs GREEN against actual Supabase RLS (not a mock), tenant isolation is unproven. This test becomes the permanent regression guard every later phase relies on.
Output: A green full test suite, with the cross-tenant isolation proof passing against real RLS.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-SKELETON.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `tests/rls-cross-tenant.test.ts` — finalized + GREEN against the real test project (cross-tenant denied + unset-GUC fail-closed)
- `tests/profile.test.ts` — finalized + GREEN against the real DB (TENANT-02 round-trip under correct tenant)
- `package.json` — `test` script = `vitest run` (full suite, the CI guard)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the Supabase TEST project + non-service-role connection are ready</name>
  <what-built>
    Nothing yet — this confirms the test environment the cross-tenant proof requires (RESEARCH Open Question #3, D-17). The test MUST hit RLS as the `authenticated` role; a service-role connection would bypass RLS and make the test pass falsely.
  </what-built>
  <how-to-verify>
    1. A dedicated Supabase TEST project exists (separate from production) and the Plan 04 schema push has been applied to it (the `tenants` table + `tenant_self_isolation` RLS policy exist there). If Plan 04 pushed only to prod, run `pnpm exec drizzle-kit push` against the test project's direct connection now.
    2. `.env.test` (gitignored) sets `TEST_DATABASE_URL` to the TEST project's NON-service-role (`authenticated`-role) connection string — NOT the service-role key.
    3. Confirm Clerk is configured as a third-party auth provider on the test project (so the `authenticated` role resolves).
  </how-to-verify>
  <resume-signal>Type "approved" once the test project has the schema pushed and `TEST_DATABASE_URL` points at a non-service-role connection, or describe what is missing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the cross-tenant + profile integration tests GREEN against real Supabase</name>
  <files>tests/rls-cross-tenant.test.ts, tests/profile.test.ts, package.json</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (Pattern 7 cross-tenant test approach: insert as A, read as B → 0 rows, unset GUC → 0 rows; Pattern 3 fail-closed `current_setting(...,true)`; Open Question #3 non-service-role connection)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md (tasks 00-W0-01, 00-W0-05; Phase gate: full suite green + cross-tenant test green before /gsd-verify-work)
    - tests/rls-cross-tenant.test.ts, tests/profile.test.ts (the Wave 0 stubs to finalize)
    - src/db/with-tenant.ts, src/db/schema.ts, src/lib/profile.ts (the implementations under test)
  </read_first>
  <action>
    Point the integration tests at `process.env.TEST_DATABASE_URL` (loaded from `.env.test`). Finalize `tests/rls-cross-tenant.test.ts` so it runs (not skips) against the real test project: (1) insert a `tenants`-scoped row under `withTenant('<orgA-uuid>')`, (2) under `withTenant('<orgB-uuid>')` attempt to read that row and assert 0 rows (cross-tenant denied — TENANT-01, success criterion #4, threat T-00-01), (3) run a raw query with NO tenant GUC set and assert 0 rows (fail-closed). Use clearly-namespaced test org ids and clean up inserted rows in an `afterAll`/`afterEach` so the test is idempotent and re-runnable. Finalize `tests/profile.test.ts` to round-trip a `{ companyName, phone, address, email }` save/read through `saveProfile`/`getProfile` under one tenant against the real DB and assert equality (TENANT-02). Add a `test` script to `package.json` = `vitest run` (full single-run suite — no watch flag) so the suite is the permanent CI regression guard. If the cross-tenant test does NOT deny (i.e., Tenant B reads Tenant A's row, or unset GUC returns rows), STOP — this is a fail-closed RLS defect that blocks the phase; do not weaken the assertion to make it pass.
  </action>
  <verify>
    <automated>pnpm vitest run tests/rls-cross-tenant.test.ts tests/profile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rls-cross-tenant.test.ts` runs against `TEST_DATABASE_URL` (not skipped) and asserts cross-tenant read = 0 rows AND unset-GUC query = 0 rows
    - `tests/profile.test.ts` round-trips a full profile under one tenant against the real DB and passes
    - `package.json` has a `test` script equal to `vitest run`
    - `pnpm vitest run tests/rls-cross-tenant.test.ts tests/profile.test.ts` exits 0 (GREEN)
    - The cross-tenant assertion was NOT weakened to force a pass (still asserts 0 rows)
  </acceptance_criteria>
  <done>The cross-tenant isolation test and the profile round-trip pass against the real Supabase test project; tenant isolation is proven, not mocked.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green — final phase gate</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md (Phase gate: full suite green + cross-tenant test green before /gsd-verify-work; Validation Sign-Off checklist)
    - tests/ (all five test files)
  </read_first>
  <action>
    Run the entire suite with `pnpm vitest run` and confirm all five Phase 0 tests are GREEN (rls-cross-tenant, with-tenant, roles, webhook, profile). Resolve any remaining red by fixing the implementation (do NOT delete or skip tests, and do NOT weaken the fail-closed cross-tenant assertions). Confirm the `pnpm test` alias runs the same full suite. This satisfies the Nyquist phase gate and readies the phase for `/gsd-verify-work`.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0 with all five test files passing
    - `pnpm test` runs the same full suite and exits 0
    - No test is skipped or deleted to achieve green; cross-tenant fail-closed assertions intact
  </acceptance_criteria>
  <done>The full vitest suite is green, including the cross-tenant RLS guard — Phase 0 is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-env → real Supabase TEST project | The test credential MUST be the `authenticated` role; a service-role connection bypasses RLS and invalidates the proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure / Elevation | Cross-tenant read (the property under test) | mitigate | Test proves RLS denies cross-tenant reads and unset-GUC queries against real Supabase; assertion not weakened to force green |
| T-00-12 | Information Disclosure | Test passes falsely because it ran as service-role | mitigate | `TEST_DATABASE_URL` is the non-service-role (`authenticated`) connection (confirmed in Task 1 checkpoint); fail-closed unset-GUC assertion catches a privileged bypass |
| T-00-SC | Tampering | No new package installs in this plan | accept | This plan installs no new packages; nothing to audit |
</threat_model>

<verification>
- `pnpm vitest run` exits 0 — all five Phase 0 tests green
- `tests/rls-cross-tenant.test.ts` ran against the real test project (not skipped) and proved cross-tenant denial + fail-closed
- `pnpm test` runs the full suite (CI regression guard)
- Manual residuals (from VALIDATION § Manual-Only): technician redirect, /settings gate, full signup→onboarding→dashboard, session persistence, password reset — verified during /gsd-verify-work
</verification>

<success_criteria>
- Cross-tenant isolation proven against real Supabase RLS (success criterion #4, TENANT-01) — Tenant B cannot read Tenant A; unset GUC returns 0 rows
- Profile round-trip passes against the real DB (TENANT-02)
- Full vitest suite green; `pnpm test` is the permanent CI guard
- No assertion weakened or test skipped to force green
</success_criteria>

<output>
Create `.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-05-SUMMARY.md` when done
</output>
