---
phase: 00-foundation-tenancy-auth-and-data-spine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - .env.test.example
  - tests/rls-cross-tenant.test.ts
  - tests/with-tenant.test.ts
  - tests/roles.test.ts
  - tests/webhook.test.ts
  - tests/profile.test.ts
autonomous: true
requirements: [TENANT-01, AUTH-06, AUTH-01, TENANT-02]
must_haves:
  truths:
    - "Running `pnpm vitest run` discovers all five test files and reports them as failing (RED) because the modules under test do not exist yet"
    - "vitest is installed as a dev dependency and `vitest.config.ts` resolves the `@/` alias to `src/`"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with @/ alias and node test environment"
      contains: "test"
    - path: "tests/rls-cross-tenant.test.ts"
      provides: "TENANT-01 cross-tenant isolation guard (success criterion #4)"
      contains: "withTenant"
    - path: "tests/with-tenant.test.ts"
      provides: "TENANT-01 withTenant wrapper unit test"
      contains: "set_config"
    - path: "tests/roles.test.ts"
      provides: "AUTH-06 role-to-modules map unit test"
      contains: "visibleModules"
    - path: "tests/webhook.test.ts"
      provides: "AUTH-01/D-02 org provisioning unit test"
      contains: "organization.created"
    - path: "tests/profile.test.ts"
      provides: "TENANT-02 profile round-trip integration test"
      contains: "companyName"
  key_links:
    - from: "tests/rls-cross-tenant.test.ts"
      to: "src/db/with-tenant.ts"
      via: "import withTenant"
      pattern: "from ['\"]@/db/with-tenant['\"]"
    - from: "tests/roles.test.ts"
      to: "src/lib/roles.ts"
      via: "import visibleModules"
      pattern: "from ['\"]@/lib/roles['\"]"
---

<objective>
Establish the Wave 0 test infrastructure for Phase 0: install vitest, create `vitest.config.ts`, and author five failing test files that pin the behavioral contracts every later plan must satisfy (TENANT-01 cross-tenant isolation, the `withTenant` wrapper, the AUTH-06 role-to-modules map, the AUTH-01 org-provisioning webhook, and the TENANT-02 profile round-trip).

Purpose: Nyquist validation requires that the success-criterion tests exist BEFORE implementation. These tests are the sampling signal that proves each subsequent wave's work is correct. The cross-tenant test is the permanent CI guard against RLS regressions (STATE.md key decision, D-17).
Output: A vitest setup and five RED tests that import not-yet-existing modules and fail to compile/run — the contract surface for Waves 1–4.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md
@.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-SKELETON.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the following symbols/files (the contract surface Waves 1–4 implement against):
- `vitest.config.ts` — vitest config, node environment, `@/` → `src/` alias
- `.env.test.example` — documents `TEST_DATABASE_URL` (non-service-role connection for RLS tests)
- `tests/rls-cross-tenant.test.ts` — asserts cross-tenant read returns 0 rows + unset GUC returns 0 rows
- `tests/with-tenant.test.ts` — asserts `withTenant(orgId, fn)` runs `set_config('app.current_tenant_id', orgId, true)` inside a transaction
- `tests/roles.test.ts` — asserts `visibleModules(role)` returns the correct module set per role
- `tests/webhook.test.ts` — asserts the `organization.created` handler inserts a `tenants` row idempotently
- `tests/profile.test.ts` — asserts business-profile save/read round-trips under the correct tenant
- Dev dependency: `vitest`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create vitest.config.ts + test env template</name>
  <files>package.json, vitest.config.ts, .env.test.example</files>
  <read_first>
    - CLAUDE.md (pnpm-only constraint — never npm/npx/yarn; `pnpm add -D vitest`)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (§ Validation Architecture: framework Vitest 4.x, `pnpm vitest run`; § Open Questions #3: test needs a NON-service-role connection that hits RLS as the `authenticated` role)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
  </read_first>
  <action>
    If `package.json` does not yet exist (this plan may run before scaffold), initialize a minimal one with `pnpm init` so vitest can be added; the Wave 1 `create-next-app` scaffold will merge/extend it. Install vitest as a dev dependency with `pnpm add -D vitest` (Vitest 4.x per RESEARCH). Create `vitest.config.ts` configuring: `test.environment` = `node`, `test.globals` = true, `test.include` = `tests/**/*.test.ts`, and a `resolve.alias` mapping `@` to the absolute `src` directory so test imports like `@/db/with-tenant` resolve. Create `.env.test.example` documenting the required `TEST_DATABASE_URL` variable with an inline comment stating it MUST be a non-service-role (`authenticated`-role) connection string to a dedicated Supabase TEST project (per D-17 and RESEARCH Open Question #3) — the service-role key bypasses RLS and would make the cross-tenant test pass falsely. Do NOT use any `--watch` flag anywhere; all commands are single-run.
  </action>
  <verify>
    <automated>pnpm exec vitest --version</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest --version` prints a 4.x version and exits 0
    - `package.json` devDependencies contains a `vitest` entry
    - `vitest.config.ts` contains `tests/**/*.test.ts` in its include glob
    - `vitest.config.ts` maps the `@` alias to the `src` directory
    - `.env.test.example` contains `TEST_DATABASE_URL=` and a comment containing the word `non-service-role`
    - No `package-lock.json` or `yarn.lock` exists in the repo (pnpm only)
  </acceptance_criteria>
  <done>vitest is installed, `vitest.config.ts` resolves `@/` to `src/`, and `.env.test.example` documents the non-service-role TEST connection string.</done>
</task>

<task type="auto">
  <name>Task 2: Author the TENANT-01 RLS tests (cross-tenant + withTenant unit)</name>
  <files>tests/rls-cross-tenant.test.ts, tests/with-tenant.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (Pattern 4 `withTenant` wrapper signature `withTenant<T>(orgId: string, fn: (tx) => Promise<T>): Promise<T>` using `select set_config('app.current_tenant_id', ${orgId}, true)`; Pattern 7 cross-tenant test approach; Pattern 3 RLS policy fail-closed with `current_setting(..., true)`)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md (tasks 00-W0-01, 00-W0-02 — secure behaviors)
    - vitest.config.ts (created in Task 1 — alias + include)
  </read_first>
  <action>
    Create `tests/with-tenant.test.ts`: import `withTenant` from `@/db/with-tenant` and, using a mocked/fake `db` transaction (vitest `vi.fn`/`vi.mock`), assert that calling `withTenant('org_test_a', fn)` (a) opens a transaction, (b) executes a parameterized `set_config('app.current_tenant_id', 'org_test_a', true)` statement BEFORE invoking `fn`, and (c) passes the transaction handle to `fn`. Assert the GUC value is passed as a bound parameter (not string-concatenated) to prevent SQL injection (threat T-00-02). Create `tests/rls-cross-tenant.test.ts` as an integration test pointed at `process.env.TEST_DATABASE_URL`: it imports `withTenant` from `@/db/with-tenant` and `tenants` from `@/db/schema`, then (1) inserts a row under `withTenant('org_aaaaaaaa...')`, (2) under `withTenant('org_bbbbbbbb...')` attempts to read that row and asserts 0 rows returned, and (3) runs a raw query with NO tenant GUC set and asserts 0 rows (fail-closed). Guard the integration test with `skipIf(!process.env.TEST_DATABASE_URL)` so it skips gracefully in local development when no test DB is configured — but MUST NOT be silently skipped in CI. In CI, `TEST_DATABASE_URL` must be set as a secret and the pipeline must assert the test ran (not skipped) by checking the vitest output for zero skipped tests in the cross-tenant file. Plan 05 finalizes this guard behavior; see that plan for the CI assertion step. These tests MUST currently fail because `@/db/with-tenant` and `@/db/schema` do not exist yet.
  </action>
  <verify>
    <automated>pnpm vitest run tests/with-tenant.test.ts 2>&1 | grep -v '^#' | grep -Eq 'Cannot find module|Failed to resolve|FAIL|failed' && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/with-tenant.test.ts` imports from `@/db/with-tenant`
    - `tests/with-tenant.test.ts` asserts `set_config` is called with `app.current_tenant_id` and a bound parameter (asserts the org id is NOT string-concatenated)
    - `tests/rls-cross-tenant.test.ts` imports `tenants` from `@/db/schema` and `withTenant` from `@/db/with-tenant`
    - `tests/rls-cross-tenant.test.ts` contains an assertion that a cross-tenant read returns 0 rows AND an assertion that an unset-GUC query returns 0 rows
    - `pnpm vitest run tests/with-tenant.test.ts` exits non-zero (RED — module under test absent)
  </acceptance_criteria>
  <done>Both TENANT-01 tests exist, encode fail-closed cross-tenant + parameterized-GUC behavior, and fail because their target modules do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Author the AUTH-06 roles, AUTH-01 webhook, and TENANT-02 profile tests</name>
  <files>tests/roles.test.ts, tests/webhook.test.ts, tests/profile.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-CONTEXT.md (D-15 role→visible modules: Admin sees all incl. Settings; Dispatcher sees Dispatch/Jobs/Customers/Estimates/Invoicing/Reports — NOT Settings; Technician sees nothing/redirect; D-02 webhook provisions tenants; D-11/D-12 profile fields company name/phone/address/email/logo)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-RESEARCH.md (Pattern 6 `organization.created` → tenants insert idempotent; A2 role keys `org:admin`/`org:dispatcher`/`org:technician`; Code Examples saveProfile via withTenant)
    - .planning/phases/00-foundation-tenancy-auth-and-data-spine/00-VALIDATION.md (tasks 00-W0-03, 00-W0-04, 00-W0-05)
  </read_first>
  <action>
    Create `tests/roles.test.ts`: import `visibleModules` from `@/lib/roles` and assert: `visibleModules('org:admin')` includes `settings` and all module keys; `visibleModules('org:dispatcher')` includes `dispatch`, `jobs`, `customers`, `estimates`, `invoicing`, `reports` and does NOT include `settings`; `visibleModules('org:technician')` returns an empty set (or equivalent "no shell" sentinel) per D-14. Create `tests/webhook.test.ts`: import the webhook handler from `@/app/api/webhooks/clerk/route` (or a testable `handleClerkWebhook` helper it exports) and assert that a verified `organization.created` event payload (with `data.id = 'org_xxx'`) triggers exactly one `tenants` insert keyed on `id = 'org_xxx'`, and that replaying the SAME event id does not create a duplicate (idempotency, T-00-03) — mock the DB insert and the `verifyWebhook` call. Create `tests/profile.test.ts`: import a `saveProfile`/`getProfile` server action from `@/lib/profile` (or `@/app/(app)/settings/company-profile/actions`) and assert that saving `{ companyName, phone, address, email }` under tenant A and reading it back via `withTenant('org_a')` returns the same values (round-trip), using a mocked or test-DB-backed `withTenant`. All three tests MUST fail now because their target modules do not exist. Do not hardcode the role keys as strings that diverge from RESEARCH A2 — use exactly `org:admin`, `org:dispatcher`, `org:technician`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/roles.test.ts tests/webhook.test.ts tests/profile.test.ts 2>&1 | grep -v '^#' | grep -Eq 'Cannot find module|Failed to resolve|FAIL|failed' && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/roles.test.ts` asserts `visibleModules('org:dispatcher')` excludes `settings`
    - `tests/roles.test.ts` asserts `visibleModules('org:technician')` yields no shell modules
    - `tests/webhook.test.ts` asserts an `organization.created` payload inserts exactly one `tenants` row and a replayed event id does not duplicate it
    - `tests/profile.test.ts` asserts a `{ companyName, phone, address, email }` save/read round-trips under a single tenant
    - `pnpm vitest run tests/roles.test.ts tests/webhook.test.ts tests/profile.test.ts` exits non-zero (RED — modules under test absent)
  </acceptance_criteria>
  <done>The AUTH-06, AUTH-01, and TENANT-02 tests exist, encode their behavioral contracts with the exact `org:*` role keys, and fail because their target modules do not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-env → TEST Supabase project | The cross-tenant test connects with a credential that MUST be the `authenticated` role, not service-role, or RLS is bypassed and the test passes falsely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | cross-tenant RLS test connection | mitigate | `.env.test.example` documents `TEST_DATABASE_URL` MUST be a non-service-role (`authenticated`) connection; test asserts 0 rows for cross-tenant + unset GUC (proves RLS, not a privileged bypass) |
| T-00-02 | Tampering | `withTenant` GUC value | mitigate | `tests/with-tenant.test.ts` asserts the org id is passed as a bound parameter to `set_config(...)`, never string-concatenated |
| T-00-SC | Tampering | `pnpm add -D vitest` install | accept | vitest verified legitimate in RESEARCH Package Legitimacy Audit (SUS verdict is documented false positive: official Vite-team runner, 20M+ weekly downloads); no `[ASSUMED]`/`[SUS]`/`[SLOP]` install requiring a checkpoint |
</threat_model>

<verification>
- `pnpm exec vitest --version` reports 4.x
- `pnpm vitest run` discovers exactly five test files under `tests/`
- All five tests currently fail (RED) due to missing `@/db/*`, `@/lib/*`, and `@/app/api/webhooks/clerk/route` modules — this is the expected Wave 0 state
- No `package-lock.json` / `yarn.lock` present
</verification>

<success_criteria>
- vitest installed via pnpm; `vitest.config.ts` resolves `@/` → `src/`
- Five test files exist encoding TENANT-01 (×2), AUTH-06, AUTH-01, TENANT-02 contracts
- The cross-tenant test encodes both the cross-tenant-denied AND fail-closed (unset GUC) assertions
- All tests fail now (RED) — they go GREEN as Waves 1–4 deliver the modules
- `.env.test.example` documents the non-service-role TEST connection
</success_criteria>

<output>
Create `.planning/phases/00-foundation-tenancy-auth-and-data-spine/00-01-SUMMARY.md` when done
</output>
