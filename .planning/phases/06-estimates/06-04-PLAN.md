---
phase: 06-estimates
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/app/(app)/estimates/page.tsx
  - src/app/(app)/estimates/estimates-sidebar.tsx
  - src/app/(app)/estimates/estimates-table.tsx
  - src/app/(app)/estimates/new/page.tsx
  - src/app/(app)/estimates/[id]/page.tsx
  - src/app/(app)/estimates/[id]/estimate-form.tsx
  - src/app/(app)/estimates/[id]/estimate-tasks.tsx
autonomous: true
requirements:
  - EST-01
  - EST-02
  - EST-03
  - EST-04
  - EST-05
  - EST-07
  - EST-08

must_haves:
  truths:
    - "GET /estimates returns 200 with estimates dashboard (sidebar + table)"
    - "Estimates sidebar shows My Estimates and All Estimates sections each with count badges per status folder"
    - "Estimates table shows columns: Requested On, Customer, Description, Value, Status, Rating"
    - "Rating column renders filled/empty stars (not a number)"
    - "GET /estimates/new returns 200 — blank estimate form"
    - "GET /estimates/[id] returns 200 — two-panel estimate form with Project Specs left, Sales Data right"
    - "Convert to Job button is disabled/hidden when estimate status is estimate_lost"
    - "Convert to Job shows confirmation dialog before calling convertEstimateToJobAction"
    - "Email Estimate button fires sonner toast (stub message per D-13)"
    - "Task list section renders in estimate form (same pattern as job tasks)"
  artifacts:
    - path: "src/app/(app)/estimates/page.tsx"
      provides: "Server component: estimates dashboard (sidebar + table + New Estimate button)"
    - path: "src/app/(app)/estimates/estimates-sidebar.tsx"
      provides: "Client component: status-folder nav with count badges, My/All Estimates sections, tag filter"
    - path: "src/app/(app)/estimates/estimates-table.tsx"
      provides: "Client component: TanStack Table with 6 columns (Requested On, Customer, Description, Value, Status, Rating)"
    - path: "src/app/(app)/estimates/new/page.tsx"
      provides: "Server component: blank estimate creation page"
    - path: "src/app/(app)/estimates/[id]/page.tsx"
      provides: "Server component: estimate detail page with full data fetch"
    - path: "src/app/(app)/estimates/[id]/estimate-form.tsx"
      provides: "Client component: two-panel form (Project Specs / Sales Data), GroupedLineItems, StarPicker, Convert to Job dialog, Download PDF link, Email stub button"
    - path: "src/app/(app)/estimates/[id]/estimate-tasks.tsx"
      provides: "Client component: estimate task list + reminders (mirror of job-tasks.tsx)"
  key_links:
    - from: "src/app/(app)/estimates/page.tsx"
      to: "countEstimatesByStatus"
      via: "import from ../actions then call in server component"
      pattern: "countEstimatesByStatus"
    - from: "src/app/(app)/estimates/estimates-table.tsx"
      to: "src/components/line-items/star-picker.tsx"
      via: "StarDisplay inline component using StarPicker readOnly"
      pattern: "StarPicker.*readOnly"
    - from: "src/app/(app)/estimates/[id]/estimate-form.tsx"
      to: "convertEstimateToJobAction"
      via: "dialog confirm → router.push(/jobs/[newJobId])"
      pattern: "convertEstimateToJobAction"
    - from: "src/app/(app)/estimates/[id]/estimate-form.tsx"
      to: "src/components/line-items/grouped-line-items.tsx"
      via: "import GroupedLineItems"
      pattern: "GroupedLineItems"
---

<objective>
Wave 3: Build the full estimates UI — dashboard (page, sidebar, table) and estimate form (new, detail, two-panel form, tasks).

Purpose: This is the user-facing surface. After this plan, a user can navigate to /estimates, create a new estimate, view/edit an existing estimate, use grouped line items, rate opportunity, and trigger the Convert to Job flow. All locked decisions for the estimates UI are implemented here: D-01 (free status dropdown), D-03 (count badges), D-04 (star picker), D-05/D-06 (grouped line items via GroupedLineItems), D-08 (margin hidden from tech), D-09 (convert disabled for Lost), D-10/D-11 (conversion atomic + navigate), D-12 (convert button on form header), D-13 (email stub toast).

Output: Full /estimates route tree. References D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-13.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimates/06-CONTEXT.md
@.planning/phases/06-estimates/06-RESEARCH.md
@.planning/phases/06-estimates/06-PATTERNS.md
@.planning/phases/06-estimates/06-UI-SPEC.md
@.planning/phases/06-estimates/06-02-SUMMARY.md
@.planning/phases/06-estimates/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estimates dashboard — page.tsx + estimates-sidebar.tsx + estimates-table.tsx</name>
  <files>
    src/app/(app)/estimates/page.tsx
    src/app/(app)/estimates/estimates-sidebar.tsx
    src/app/(app)/estimates/estimates-table.tsx
  </files>
  <read_first>
    src/app/(app)/jobs/page.tsx — full file: server component pattern, auth guard, Suspense wrapper, layout (aside + main), header with New Job button
    src/app/(app)/jobs/jobs-sidebar.tsx — full file: bucket structure, useQueryStates pattern, count badge pattern (the estimates sidebar MUST render count badges per D-03, where jobs sidebar does not)
    src/app/(app)/jobs/jobs-table.tsx — full file: TanStack Table setup, column definitions, row link, badge rendering, empty state, filter application pattern
    src/app/(app)/estimates/actions.ts — countEstimatesByStatus and listEstimatesAction signatures + return types (from Plan 06-02)
    src/lib/estimates/status.ts — estimateStatusBadgeVariant, estimateStatusLabel (from Plan 06-01)
    src/components/line-items/star-picker.tsx — StarPicker props (readOnly mode for display in table)
    .planning/phases/06-estimates/06-PATTERNS.md — estimates-sidebar.tsx count badge pattern, estimates-table.tsx column definitions pattern
    .planning/phases/06-estimates/06-UI-SPEC.md — copywriting contract (empty state text, "New Estimate" CTA), color contract (active sidebar folder uses bg-sidebar-accent text-primary), column order (Requested On, Customer, Description, Value, Status, Rating)
  </read_first>
  <action>
    Create src/app/(app)/estimates/page.tsx as a Server Component. Import auth from @clerk/nextjs/server, redirect from next/navigation. Call auth() → { orgId, userId } → redirect to /sign-in if missing. Call countEstimatesByStatus(orgId, userId) for sidebar counts. Fetch estimate rows via listEstimatesAction(orgId) for the table. Render layout: flex with hidden lg:block aside (w-60) containing EstimatesSidebar, and main area with page header ("Estimates" text-3xl font-semibold) + New Estimate button (Link to /estimates/new, Button primary) + EstimatesTable. Apply animate-in fade-in-0 duration-300 on the outer div per existing pattern. Pass searchParams through for filter state.

    Create src/app/(app)/estimates/estimates-sidebar.tsx as a Client Component ('use client'). Mirror jobs-sidebar.tsx structure but with two sections: "My Estimates" and "All Estimates" (per D-03). Each section lists the 5 status folders: Requested, Provided, Accepted, Won, Lost. Each folder renders a count badge (the count) alongside the label — use `{count > 0 && <span className="text-xs tabular-nums text-muted-foreground">{count}</span>}`. Use useQueryStates from nuqs for status and mine filter state (same pattern as jobs-sidebar). Selected folder gets bg-sidebar-accent text-primary font-medium (per UI-SPEC). Include a tag filter section below the status folders. Props: statusCounts: { my: Record<string, number>, all: Record<string, number> }. Status folder icons: use FileText for generic estimates, CheckCircle for Won, XCircle for Lost, Clock for Requested, Send for Provided, ThumbsUp for Accepted (all from lucide-react).

    Create src/app/(app)/estimates/estimates-table.tsx as a Client Component ('use client'). Mirror jobs-table.tsx with TanStack Table v8. Define EstimateRow type from listEstimatesAction return shape. Columns in SF order: (1) Requested On — link to /estimates/[id], format createdAt date; (2) Customer — customerName; (3) Description — truncated; (4) Value — formatted as $N.NN with em dash for null; (5) Status — Badge with estimateStatusBadgeVariant + estimateStatusLabel; (6) Rating — StarDisplay component rendering <StarPicker value={row.opportunityRating} onChange={() => {}} readOnly /> (read-only in table, editable only in form). Apply filter from sidebar query state (status, mine flag). Empty state: "No estimates yet" heading + body copy from UI-SPEC when no rows exist. Filtered empty state: "No estimates match your filters." text with tag clear suggestion.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - src/app/(app)/estimates/page.tsx is a valid Next.js 15 Server Component with auth guard
    - estimates-sidebar.tsx has two sections (My Estimates + All Estimates) each with 5 status folders and count badges
    - estimates-table.tsx has 6 columns in correct SF order; Rating column uses StarPicker readOnly
    - Empty state copy matches UI-SPEC: "No estimates yet" / "No estimates match your filters."
  </done>
</task>

<task type="auto">
  <name>Task 2: Estimate new/detail pages + two-panel form + task section</name>
  <files>
    src/app/(app)/estimates/new/page.tsx
    src/app/(app)/estimates/[id]/page.tsx
    src/app/(app)/estimates/[id]/estimate-form.tsx
    src/app/(app)/estimates/[id]/estimate-tasks.tsx
  </files>
  <read_first>
    src/app/(app)/jobs/[id]/page.tsx — full file: server component pattern, data loading (job + contacts + locations + categories + tags + techs + templates), props passed to JobForm
    src/app/(app)/jobs/[id]/job-form.tsx — full file: two-panel layout structure, form state, useActionState pattern, React 19 router.push after success, DialogTrigger + render prop pattern, line items section wiring
    src/app/(app)/jobs/[id]/job-tasks.tsx — full file: task list + reminders section pattern to mirror exactly for estimate-tasks.tsx
    src/app/(app)/estimates/actions.ts — read the full exported interface: getEstimateAction, updateEstimateAction, createEstimateAction, updateEstimateStatusAction, convertEstimateToJobAction, sendEstimateAction signatures; AND the task/reminder actions added in Plan 06-02: createEstimateTaskAction, updateEstimateTaskAction, deleteEstimateTaskAction, createEstimateReminderAction, deleteEstimateReminderAction
    src/lib/estimates/templates.ts — applyEstimateTemplate signature (returns { lineItems, tasks, groups })
    src/components/customer-search.tsx — CustomerSearch props
    src/components/tag-select.tsx — TagSelect props
    src/components/line-items/grouped-line-items.tsx — GroupedLineItems props (from Plan 06-03)
    src/components/line-items/star-picker.tsx — StarPicker props (from Plan 06-03)
    src/lib/estimates/status.ts — estimateStatusLabel
    src/lib/utils.ts — toDateInputValue helper (for date input value props)
    .planning/phases/06-estimates/06-PATTERNS.md — estimate-form.tsx imports pattern, two-panel layout pattern, Convert to Job dialog, React 19 form auto-reset guard, Base UI DialogTrigger render prop
    .planning/phases/06-estimates/06-UI-SPEC.md — copywriting for all buttons/dialogs/toasts, color contract, form panel names ("Project Specs" / "Sales Data"), form field placement
    .planning/phases/06-estimates/06-CONTEXT.md — D-09 (convert disabled for Lost), D-10 (auto-advance to Won), D-11 (navigate to /jobs/[id]), D-12 (convert button in form header), D-13 (email stub toast)
  </read_first>
  <action>
    Create src/app/(app)/estimates/new/page.tsx as a Server Component. Auth guard. Fetch reference data: listJobCategories (for category select), getCustomerContacts if customerId param exists, customer locations, tags, technicians, estimateTemplates. Render a new blank EstimateForm (no initial estimate data). Pass reference data as props. "New Estimate" breadcrumb.

    Create src/app/(app)/estimates/[id]/page.tsx as a Server Component. Auth guard. Params: { id: string } via Next.js 15 params Promise pattern. Call getEstimateAction(orgId, id). If null, notFound(). Fetch same reference data as new page. Render EstimateForm with initialEstimate data. Render EstimateTasks with initial tasks/reminders.

    Create src/app/(app)/estimates/[id]/estimate-form.tsx as a Client Component ('use client'). Mirror job-form.tsx two-panel layout:
    - Left panel label: "Project Specs" (not "Details" — SF naming per EST-03)
    - Left panel fields: CustomerSearch (customer), contact (Select from customer's contacts), service location (Select from customer's locations), category (Select from job categories), description (Textarea), PO# (Input), on-site visit date (Input type="date" using toDateInputValue), arrival window start/end (Input type="time"), assigned techs (TechSelect), notes for techs (Textarea), tags (TagSelect), task section (EstimateTasks component)
    - Right panel label: "Sales Data"
    - Right panel fields: status (Select with 5 estimate_status values — per D-01 free dropdown, no FSM), opportunity rating (StarPicker — 1-5 stars, per D-04), referral source (Select), expiry date (Input type="date" using toDateInputValue), follow-up date (Input type="date"), internal notes (Textarea labeled "Internal Notes — not shown on PDF")
    - Line items section (below both panels, full width): GroupedLineItems component with groups and items state, role prop. Show a "Totals" summary panel below line items (computeEstimateTotals client-side, same as job form totals panel).

    Header action bar (top of form, sticky or inline): "Save Estimate" (primary Button), "Download PDF" (outline Link button → /api/estimates/[id]/pdf, target="_blank"), "Email Estimate" (outline Button, onClick fires sendEstimateAction stub then shows sonner toast per UI-SPEC: "Email sending arrives in a later release. The estimate PDF is ready to download."), "Convert to Job" (outline or primary Button per UI-SPEC — disabled when estimate.status === 'estimate_lost' per D-09, hidden for new estimates, shown for existing estimates only).

    Convert to Job flow (D-12): "Convert to Job" button uses Base UI DialogTrigger with render prop: <DialogTrigger render={<Button variant="outline" disabled={estimate.status === 'estimate_lost'}>Convert to Job</Button>}. Dialog content shows confirmation copy from UI-SPEC. "Convert to Job" confirm button calls convertEstimateToJobAction(orgId, estimateId), then on { jobId } result calls router.push('/jobs/' + jobId). Show sonner toast "Converted to Job {#JOB-N}" after push. Use startTransition + isPending for loading state on the button.

    React 19 guard: do NOT read FormData after action. Use router.refresh() + prop-sync pattern (same as Phase 2 fix — on state.success for update, router.refresh(); for create, router.push to new estimate URL).

    Date fields from Drizzle: use toDateInputValue(estimate.expiryDate) for the value prop of date inputs (handles UTC midnight Date objects from RSC flight — per CLAUDE.md date handling rules).

    Create src/app/(app)/estimates/[id]/estimate-tasks.tsx mirroring src/app/(app)/jobs/[id]/job-tasks.tsx exactly, swapping jobId references to estimateId and swapping job task actions to estimate task actions. Import createEstimateTaskAction, updateEstimateTaskAction, deleteEstimateTaskAction, createEstimateReminderAction, deleteEstimateReminderAction from src/app/(app)/estimates/actions.ts — these actions were created in Plan 06-02 and are stable exports, do NOT add them to estimates/actions.ts again.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm build exits 0 — all estimate route files compile
    - src/app/(app)/estimates/new/page.tsx renders EstimateForm with no initial data
    - src/app/(app)/estimates/[id]/page.tsx fetches estimate and renders EstimateForm + EstimateTasks
    - estimate-form.tsx: left panel labeled "Project Specs", right panel labeled "Sales Data"
    - estimate-form.tsx: status field is a free Select (not FSM-locked per D-01)
    - estimate-form.tsx: Convert to Job button is disabled when status is estimate_lost
    - estimate-form.tsx: DialogTrigger uses render prop (not asChild)
    - estimate-form.tsx: email button fires sendEstimateAction stub + sonner toast per D-13
    - estimate-tasks.tsx mirrors job-tasks.tsx structure; imports task/reminder CRUD from estimates/actions.ts
    - pnpm vitest run exits 0 — no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → estimate form | User controls status dropdown (soft/free per D-01); no server-side FSM enforcement for estimates |
| Browser → convertEstimateToJobAction | Client disables button for Lost status; server re-validates (throws if status is estimate_lost — defense in depth) |
| Browser → sendEstimateAction | Stub returns success immediately; no data leaves the system in Phase 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | /estimates/[id] page | mitigate | getEstimateAction uses withTenant(orgId) — RLS enforces tenant isolation; returns null (→ notFound()) if not owned by caller |
| T-06-02 | Information Disclosure | estimate-form.tsx margin column | mitigate | role prop passed from server component (auth-derived); GroupedLineItems hides margin column when role='technician' per D-08 |
| T-06-03 | Tampering | Convert to Job button | mitigate | Client disables for Lost status (UX guard); server-side throws if status=estimate_lost (defense in depth per actions.ts implementation in Plan 06-02) |
| T-06-04 | Information Disclosure | Download PDF link | mitigate | PDF route (Plan 06-05) verifies ownership via auth() + withTenant; link is authenticated URL |
| T-06-07 | Tampering | DialogTrigger + Button | mitigate | Base UI render prop pattern used (not asChild) — prevents nested button hydration error |
</threat_model>

<verification>
After plan 06-04: GET /estimates returns 200. GET /estimates/new returns 200. GET /estimates/[id] returns 200 (for existing estimate). pnpm build exits 0. pnpm vitest run exits 0. Sidebar shows status folders with count badges. Table shows 6 columns including star rating. Estimate form has two panels labeled "Project Specs" and "Sales Data".
</verification>

<success_criteria>
1. pnpm build exits 0
2. pnpm vitest run exits 0 — no regressions
3. GET /estimates returns 200 with sidebar (My/All Estimates + count badges) and table (6 columns)
4. GET /estimates/new returns 200 — blank two-panel form
5. GET /estimates/[id] returns 200 — populated form with correct panel labels
6. estimate-form.tsx: Convert to Job button disabled when status=estimate_lost; shows confirmation dialog
7. estimate-form.tsx: Email Estimate button fires sonner toast with stub message per UI-SPEC
8. estimate-form.tsx: GroupedLineItems wired with groups + line items state
9. estimate-tasks.tsx renders task list + reminders same structure as job-tasks.tsx; imports task/reminder CRUD from estimates/actions.ts (no modifications to that file in this plan)
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/app/(app)/estimates/page.tsx` — Server Component: estimates dashboard
- `src/app/(app)/estimates/estimates-sidebar.tsx` — Client Component: status-folder nav with count badges (My / All × 5 statuses)
- `src/app/(app)/estimates/estimates-table.tsx` — Client Component: TanStack Table, 6 columns, star rating display, empty states
- `src/app/(app)/estimates/new/page.tsx` — Server Component: blank estimate creation
- `src/app/(app)/estimates/[id]/page.tsx` — Server Component: estimate detail fetch + render
- `src/app/(app)/estimates/[id]/estimate-form.tsx` — Client Component: two-panel form, GroupedLineItems, StarPicker, convert dialog, PDF download link, email stub button
- `src/app/(app)/estimates/[id]/estimate-tasks.tsx` — Client Component: task list + reminders; imports createEstimateTaskAction, updateEstimateTaskAction, deleteEstimateTaskAction, createEstimateReminderAction, deleteEstimateReminderAction from estimates/actions.ts (all added in Plan 06-02)

**Read (not modified) in this plan:**
- `src/app/(app)/estimates/actions.ts` — consumed as a dependency; task/reminder CRUD actions were created in Plan 06-02

<output>
Create `.planning/phases/06-estimates/06-04-SUMMARY.md` when done
</output>
