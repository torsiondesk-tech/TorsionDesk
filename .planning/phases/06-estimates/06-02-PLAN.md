---
phase: 06-estimates
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/app/(app)/estimates/actions.ts
  - src/lib/estimates/templates.ts
  - src/lib/estimates/pdf-data.ts
autonomous: true
requirements:
  - EST-01
  - EST-02
  - EST-03
  - EST-04
  - EST-05
  - EST-06
  - EST-07
  - EST-08
  - EST-09

must_haves:
  truths:
    - "pnpm vitest run tests/estimates/ exits 0 (all 7 GREEN)"
    - "tests/tech/estimate-create.test.tsx passes (PWA contract satisfied)"
    - "createEstimateAction(orgId, input) accepts both PWA shape and office shape"
    - "convertEstimateToJobAction is atomic — single withTenant transaction"
    - "updateEstimateStatusAction writes a customer_events row per status change"
    - "listEstimatesAction returns { rows: CachedEstimate[] } matching dexie.ts shape"
    - "countEstimatesByStatus returns zero (not missing key) for statuses with no rows"
  artifacts:
    - path: "src/app/(app)/estimates/actions.ts"
      provides: "createEstimateAction, updateEstimateAction, deleteEstimateAction, getEstimateAction, listEstimatesAction, updateEstimateStatusAction, convertEstimateToJobAction, sendEstimateAction (stub), countEstimatesByStatus, createEstimateTemplateAction, applyEstimateTemplateAction, listEstimateTemplatesAction"
      exports:
        - "createEstimateAction"
        - "updateEstimateAction"
        - "deleteEstimateAction"
        - "getEstimateAction"
        - "listEstimatesAction"
        - "updateEstimateStatusAction"
        - "convertEstimateToJobAction"
        - "sendEstimateAction"
        - "countEstimatesByStatus"
        - "createEstimateTemplateAction"
        - "applyEstimateTemplateAction"
        - "listEstimateTemplatesAction"
    - path: "src/lib/estimates/templates.ts"
      provides: "listEstimateTemplates, createEstimateTemplate, updateEstimateTemplate, deleteEstimateTemplate, applyEstimateTemplate"
    - path: "src/lib/estimates/pdf-data.ts"
      provides: "getEstimateForPdf(orgId, estimateId): Promise<EstimatePdfData | null>"
  key_links:
    - from: "src/app/(tech)/tech/estimates/actions.ts"
      to: "src/app/(app)/estimates/actions.ts"
      via: "dynamic import delegation (mod.createEstimateAction(orgId, input))"
      pattern: "createEstimateAction\\(orgId"
    - from: "convertEstimateToJobAction"
      to: "withTenant single tx"
      via: "single withTenant call wrapping all 4 DB operations"
      pattern: "withTenant.*async.*tx"
---

<objective>
Wave 1: Implement all canonical server actions for the estimates module plus template helpers and PDF data fetcher.

Purpose: The server actions are the integration hub — they must satisfy the PWA interface contract (already tested by tests/tech/estimate-create.test.tsx), turn the 7 RED tests GREEN, and enforce all security decisions (D-02 customer_events write, D-08 guard, D-09/D-10 conversion logic). This is the only plan that creates business-logic implementations; all other plans consume these actions.

Output: A complete src/app/(app)/estimates/actions.ts, src/lib/estimates/templates.ts, and src/lib/estimates/pdf-data.ts. All 7 tests/estimates/ tests pass GREEN after this plan. References D-01, D-02, D-08, D-09, D-10, D-11, D-13.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimates/06-CONTEXT.md
@.planning/phases/06-estimates/06-RESEARCH.md
@.planning/phases/06-estimates/06-PATTERNS.md
@.planning/phases/06-estimates/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical server actions — estimates/actions.ts (createEstimate, updateEstimate, delete, list, status, convert, sendStub, countByStatus)</name>
  <files>src/app/(app)/estimates/actions.ts</files>
  <read_first>
    src/app/(app)/estimates/actions.ts — current stub (read before overwriting)
    src/app/(app)/jobs/actions.ts — full file: createJob pattern, guardCustomer/guardContact/guardServiceLocation helpers, extractErrorMessage, emptyToNull Zod helper, race-safe retry loop (withTenant + 23505 retry), lineItemSchema, customer_events write
    src/app/(tech)/tech/estimates/actions.ts — CreateEstimateInput interface shape (MUST match exactly); convertEstimateToJobAction signature; listTechEstimatesAction call pattern
    src/app/(tech)/lib/dexie.ts — CachedEstimate type (listEstimatesAction MUST return { rows: CachedEstimate[] } matching this)
    src/app/(tech)/lib/sync.ts — syncEstimateCreate / syncEstimateConversion callers to verify compatibility
    src/lib/estimates/estimate-number.ts — nextEstimateNo import path
    src/lib/estimates/totals.ts — computeEstimateTotals (used in getEstimateAction response)
    src/lib/jobs/job-number.ts — nextJobNo import path (used by convertEstimateToJobAction)
    src/db/schema.ts — all new estimate tables, lineItemGroups, customerEvents, jobs, jobLineItems exports
    src/db/with-tenant.ts — withTenant(orgId, fn) signature and Tx type
    .planning/phases/06-estimates/06-PATTERNS.md — full patterns for imports, guard helpers, createEstimateAction, convertEstimateToJobAction, updateEstimateStatusAction, listEstimatesAction
    .planning/phases/06-estimates/06-RESEARCH.md — section 1 Interface Contract (CreateEstimateInput exact shape), section 4 customer_events write pattern, section 5 (listEstimatesAction CachedEstimate shape must use TO_CHAR for date strings not Date objects)
  </read_first>
  <behavior>
    - createEstimateAction(orgId, input: CreateEstimateInput): handles BOTH PWA shape ({ name, qty, unitPrice }) and office shape ({ description, type, qty, rate, cost, groupId, taxItemId }) in the same lineItems array. PWA shape detection: if item has 'name' key, map to { description: item.name, qty, rate: item.unitPrice, type: 'service', cost: '0', groupId: null }. Office shape: use item.description, item.rate, item.type, item.cost, item.groupId directly. Race-safe retry loop on 23505 unique constraint (estimateNo). Writes 1 customer_events row: 'Estimate #EST-{N} created' (per D-02). Returns { id: string }.
    - updateEstimateAction(orgId, estimateId, input): updates estimate fields (excludes status — that goes through updateEstimateStatusAction). Replaces line items and line_item_groups atomically within withTenant tx. revalidatePath('/estimates') + revalidatePath('/estimates/[estimateId]').
    - deleteEstimateAction(orgId, estimateId): deletes estimate (cascades to line items via FK). revalidatePath('/estimates').
    - getEstimateAction(orgId, estimateId): returns full estimate with line items, groups, tasks, reminders, tags, assignees. Includes computeEstimateTotals result. Date fields: use toISOString().slice(0,10) on date columns (UTC midnight Date objects from Drizzle — per CLAUDE.md date handling rules).
    - listEstimatesAction(orgId): returns { rows: CachedEstimate[] } matching dexie.ts CachedEstimate type exactly. Date fields as ISO strings using TO_CHAR in SQL (not Date objects). value field: COALESCE(SUM(rate * qty), 0) joined from estimate_line_items.
    - updateEstimateStatusAction(orgId, estimateId, newStatus): soft update (no FSM per D-01). Writes 1 customer_events row with title 'Estimate #EST-{N} moved to {label}' where label = estimateStatusLabel(newStatus). revalidatePath both /estimates and /estimates/[id].
    - convertEstimateToJobAction(orgId, estimateId): ENTIRE operation in single withTenant(orgId, async (tx) => { ... }). Steps: (1) load estimate — throw if not found or status is estimate_lost (per D-09); (2) nextJobNo from jobs table; (3) INSERT jobs row with fields from estimate (customerId, contactId, serviceLocationId, description, poNumber, notesForTechs, status='unscheduled'); (4) copy estimate's line_item_groups → new job_line_items with remapped groupIds; (5) UPDATE estimate status to estimate_won; (6) INSERT 2 customer_events rows: "moved to Won" and "converted to Job #JOB-{N}". Returns { jobId: string }. revalidatePath('/estimates') + revalidatePath('/jobs').
    - sendEstimateAction(orgId, estimateId): stub — logs intent, returns { success: true } immediately (per D-13). No email sent. revalidatePath not needed.
    - countEstimatesByStatus(orgId, userId): returns { my: Record<estimateStatus, number>, all: Record<estimateStatus, number> }. "My" filters by assignedAgentId = userId. Both "my" and "all" return all 5 status keys even if count is 0 (not missing keys — per D-03 and dashboard.test.ts contract).
  </behavior>
  <action>
    Replace the stub in src/app/(app)/estimates/actions.ts with the full implementation.

    File must start with 'use server'. Import revalidatePath, z, eq, and, sql, max, count, desc, isNull, leftJoin from their respective packages. Import auth from @clerk/nextjs/server. Import withTenant, Tx from @/db/with-tenant. Import all needed tables from @/db/schema: estimates, estimateLineItems, estimateAssignees, estimateTags, estimateTasks, estimateReminders, lineItemGroups, customerEvents, customers, contacts, serviceLocations, taxItems, jobs, jobLineItems. Import nextEstimateNo from @/lib/estimates/estimate-number. Import nextJobNo from @/lib/jobs/job-number. Import computeEstimateTotals from @/lib/estimates/totals. Import estimateStatusLabel from @/lib/estimates/status.

    Define CreateEstimateInput interface matching the PWA shape exactly (customerId, serviceLocationId, contactName, contactPhone, description, lineItems array with name/qty/unitPrice, followUpDate, expiryDate, notes, internalNotes). This is the required minimum — the interface may have additional optional fields for office callers.

    Define Zod schemas for validation: emptyToNull helper (preprocess empty string/null/undefined to null). lineItemSchema: z.object with id optional, description optional, name optional (PWA shape), qty optional, rate optional, unitPrice optional (PWA shape), type optional, cost optional, groupId optional nullable, taxItemId optional nullable, sortOrder optional, refId optional. lineItemGroupSchema: z.object with id optional, name z.string().min(1), sortOrder z.number().default(0).

    Guard helpers: guardEstimateOwner(tx, orgId, estimateId) — SELECT id WHERE tenantId=orgId AND id=estimateId, throw 'Estimate not found or access denied' if empty. Re-export guardCustomer/guardContact/guardServiceLocation pattern from jobs/actions.ts or duplicate inline.

    For createEstimateAction: accept (orgId: string, input: CreateEstimateInput) — NOT FormData. Validate orgId, guardCustomer. Race-safe retry loop (catch 23505, retry up to 3 times with 100ms*attempts). All DB writes in single withTenant tx: nextEstimateNo, INSERT estimates, INSERT estimateLineItems (map both PWA and office shapes), INSERT customerEvents 'created'.

    For updateEstimateAction: accept (orgId, estimateId, data) where data matches a separate Zod schema with all estimate fields except status. Within withTenant: guardEstimateOwner, DELETE existing lineItemGroups for this estimate, INSERT new groups (collect returned IDs for FK mapping), DELETE existing estimateLineItems, INSERT new items with resolved groupIds, UPDATE estimates row. revalidatePath both paths.

    For convertEstimateToJobAction: all 6 steps in single withTenant call (critical — per pitfall 6). After withTenant completes, revalidatePath('/estimates') and revalidatePath('/jobs'). Return { jobId }.

    For listEstimatesAction(orgId): use SQL TO_CHAR for date columns to return strings (not Date objects). Join estimates to customers for customerName. LEFT JOIN to estimate_line_items for value SUM. Group by estimates.id, customers.name. Return { rows } matching CachedEstimate shape from dexie.ts.

    For countEstimatesByStatus(orgId, userId): run two GROUP BY queries (my + all) inside withTenant. Initialize all 5 status keys to 0 before overlaying the DB count results — never return missing keys.

    For sendEstimateAction: 'use server', auth(), return { success: true } immediately. Log "sendEstimateAction called — stub, Phase 8 will implement Resend send" using console.log.

    Export all functions: createEstimateAction, updateEstimateAction, deleteEstimateAction, getEstimateAction, listEstimatesAction, updateEstimateStatusAction, convertEstimateToJobAction, sendEstimateAction, countEstimatesByStatus. Also export CreateEstimateInput type.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm vitest run tests/estimates/ 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm vitest run tests/estimates/ exits 0 — all 7 tests GREEN
    - pnpm vitest run tests/tech/estimate-create.test.tsx exits 0 (PWA contract satisfied)
    - pnpm vitest run tests/tech/estimate-convert.test.ts exits 0 (PWA convert contract satisfied)
    - src/app/(app)/estimates/actions.ts exports all 9 required functions plus CreateEstimateInput type
  </done>
</task>

<task type="auto">
  <name>Task 2: Template helpers + PDF data fetcher (src/lib/estimates/templates.ts + pdf-data.ts)</name>
  <files>
    src/lib/estimates/templates.ts
    src/lib/estimates/pdf-data.ts
  </files>
  <read_first>
    src/lib/job-templates.ts — full file: listJobTemplates, createJobTemplate, applyJobTemplate patterns, all DB operations, return types
    src/db/schema.ts — estimateTemplates, estimateTemplateLineItems, estimateTemplateTasks exports (created in Plan 06-01)
    src/db/with-tenant.ts — withTenant signature
    src/app/(app)/settings/company-profile/actions.ts — canonical withTenant + auth() pattern
    .planning/phases/06-estimates/06-PATTERNS.md — templates.ts analog pattern, listEstimateTemplates/createEstimateTemplate/applyEstimateTemplate signatures
    .planning/phases/06-estimates/06-RESEARCH.md — section 5 Templates Architecture (separate tables, groupName inline on template line items, applyEstimateTemplate returns { lineItems, tasks, groups })
  </read_first>
  <action>
    Create src/lib/estimates/templates.ts mirroring src/lib/job-templates.ts.

    Functions to implement:
    - listEstimateTemplates(orgId: string): Promise<EstimateTemplate[]> — SELECT from estimateTemplates WHERE tenantId = orgId, join estimateTemplateLineItems and estimateTemplateTasks via FK. Return array with nested lineItems and tasks.
    - createEstimateTemplate(orgId: string, input: { name: string; description?: string; lineItems: TemplateLineItemInput[]; tasks: TemplateTaskInput[] }): Promise<{ id: string }> — INSERT into estimateTemplates, then INSERT estimateTemplateLineItems (with groupName field for group structure) and estimateTemplateTasks. All in single withTenant tx.
    - updateEstimateTemplate(orgId, templateId, input): withTenant — guardTemplateOwner, DELETE old line items and tasks, INSERT new ones, UPDATE template name/description.
    - deleteEstimateTemplate(orgId, templateId): withTenant — DELETE from estimateTemplates (cascades to line items and tasks via FK).
    - applyEstimateTemplate(orgId, templateId): returns { lineItems: AppliedLineItem[], tasks: AppliedTask[], groups: AppliedGroup[] }. Reads template line items sorted by sortOrder. Groups are derived from unique groupName values in template line items. Returns client-ready data that the estimate form merges into its local state. This is a READ action — no DB writes. No revalidatePath needed.

    Define EstimateTemplate, TemplateLineItemInput, TemplateTaskInput, AppliedLineItem, AppliedTask, AppliedGroup types. Export all types and functions.

    Create src/lib/estimates/pdf-data.ts:
    - Define EstimatePdfData interface: { estimateNo: number, companyName: string, companyAddress: string, companyPhone: string, companyEmail: string, customerName: string, serviceAddress: string, description: string | null, notes: string | null, expiryDate: string | null, createdAt: string, groups: { id: string | null; name: string | null; items: { description: string | null; qty: string; rate: string; total: string }[] }[], grandTotal: string }
    - getEstimateForPdf(orgId: string, estimateId: string): Promise<EstimatePdfData | null> — loads estimate with line items and groups, joins to customer and service location for address, joins to companyProfiles table for business header. Returns null if estimate not found or does not belong to orgId. Uses withTenant for RLS enforcement. For ungrouped items, uses a synthetic group with id=null, name=null. Date fields: expiryDate via .toISOString().slice(0,10). Does NOT include internalNotes or cost/margin (PDF is customer-facing per D-15).
    - Export EstimatePdfData and getEstimateForPdf.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - src/lib/estimates/templates.ts exports listEstimateTemplates, createEstimateTemplate, updateEstimateTemplate, deleteEstimateTemplate, applyEstimateTemplate
    - src/lib/estimates/pdf-data.ts exports getEstimateForPdf and EstimatePdfData
    - pnpm vitest run tests/estimates/templates.test.ts exits 0 (templates test goes GREEN)
    - pnpm vitest run exits 0 (full suite green including all 7 estimates tests and tech tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA → createEstimateAction | PWA calls with simplified input shape; canonical action must accept both shapes without data leakage |
| Browser → convertEstimateToJobAction | Must verify estimate belongs to caller's tenant before creating job (T-06-03) |
| PDF data fetcher → client | Must NOT include cost/margin/internalNotes in EstimatePdfData (T-06-02 / T-06-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | estimates table (all queries) | mitigate | All queries run inside withTenant(orgId, ...) which sets app.current_tenant_id; RLS enforces tenant isolation at DB layer |
| T-06-02 | Information Disclosure | createEstimateAction / getEstimateForPdf | mitigate | Zod whitelist on all accepted fields; cost/margin/internalNotes excluded from EstimatePdfData per D-15 |
| T-06-03 | Tampering | convertEstimateToJobAction | mitigate | guardEstimateOwner inside withTenant before job creation; entire operation atomic — no partial state if tenant mismatch |
| T-06-04 | Information Disclosure | getEstimateForPdf | mitigate | auth() check at route entry (Plan 06-05); getEstimateForPdf uses withTenant(orgId) — if orgId doesn't own estimate, RLS returns 0 rows → null → 404 |
| T-06-05 | Tampering | computeEstimateTotals | mitigate | Totals computation is server-authoritative; PDF uses server-computed values; client display of totals is decorative |
</threat_model>

<verification>
After plan 06-02 completes: pnpm vitest run must exit 0 (all tests green including the 7 new estimate tests AND existing tests/tech/estimate-create.test.tsx and tests/tech/estimate-convert.test.ts). pnpm build exits 0.
</verification>

<success_criteria>
1. pnpm vitest run tests/estimates/ exits 0 — all 7 tests GREEN (converted from RED in Plan 06-01)
2. pnpm vitest run tests/tech/estimate-create.test.tsx exits 0 — PWA contract satisfied
3. pnpm vitest run tests/tech/estimate-convert.test.ts exits 0 — PWA convert contract satisfied
4. pnpm build exits 0
5. src/app/(app)/estimates/actions.ts exports all 9 canonical actions + CreateEstimateInput type
6. src/lib/estimates/templates.ts exports 5 template functions
7. src/lib/estimates/pdf-data.ts exports getEstimateForPdf + EstimatePdfData (no cost/margin/internalNotes in type)
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/lib/estimates/templates.ts` — exports `listEstimateTemplates`, `createEstimateTemplate`, `updateEstimateTemplate`, `deleteEstimateTemplate`, `applyEstimateTemplate`, `EstimateTemplate`, `TemplateLineItemInput`, `AppliedLineItem`, `AppliedGroup`, `AppliedTask`
- `src/lib/estimates/pdf-data.ts` — exports `getEstimateForPdf(orgId, estimateId): Promise<EstimatePdfData | null>`, `EstimatePdfData` interface

**Modified files:**
- `src/app/(app)/estimates/actions.ts` — replaces stub with: `createEstimateAction`, `updateEstimateAction`, `deleteEstimateAction`, `getEstimateAction`, `listEstimatesAction`, `updateEstimateStatusAction`, `convertEstimateToJobAction`, `sendEstimateAction` (stub), `countEstimatesByStatus`, `createEstimateTemplateAction`, `applyEstimateTemplateAction`, `listEstimateTemplatesAction`, `CreateEstimateInput` type

<output>
Create `.planning/phases/06-estimates/06-02-SUMMARY.md` when done
</output>
