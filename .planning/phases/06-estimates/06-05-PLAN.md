---
phase: 06-estimates
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - src/app/api/estimates/[id]/pdf/route.ts
  - src/components/pdf/estimate-pdf.tsx
  - src/app/(app)/settings/templates/page.tsx
  - src/app/(app)/settings/templates/template-form.tsx
autonomous: true
requirements:
  - EST-06
  - EST-09

must_haves:
  truths:
    - "GET /api/estimates/[id]/pdf returns 200 with Content-Type: application/pdf for an owned estimate"
    - "GET /api/estimates/[id]/pdf returns 401 for unauthenticated requests"
    - "GET /api/estimates/[id]/pdf returns 404 for an estimate not owned by the caller's org"
    - "PDF route uses export const runtime = 'nodejs' (never edge)"
    - "PDF route uses pdf().toBlob() (not renderToBuffer per pitfall 4)"
    - "Settings > Templates page shows two tabs: Job Templates and Estimate Templates"
    - "Estimate templates tab supports create, list, delete (same CRUD pattern as job templates tab)"
  artifacts:
    - path: "src/app/api/estimates/[id]/pdf/route.ts"
      provides: "GET handler: auth guard + ownership verify + @react-pdf/renderer pdf().toBlob() + Response(buffer, {Content-Type: application/pdf})"
      exports:
        - "GET"
        - "runtime = 'nodejs'"
    - path: "src/components/pdf/estimate-pdf.tsx"
      provides: "EstimatePdfDocument React component using @react-pdf/renderer — SF-style layout per D-15"
      exports:
        - "EstimatePdfDocument"
        - "EstimatePdfDocumentProps"
    - path: "src/app/(app)/settings/templates/page.tsx"
      provides: "Extended server component fetching both job templates and estimate templates in parallel"
    - path: "src/app/(app)/settings/templates/template-form.tsx"
      provides: "Extended client component with Tabs wrapper (Job Templates tab + Estimate Templates tab)"
  key_links:
    - from: "src/app/api/estimates/[id]/pdf/route.ts"
      to: "src/components/pdf/estimate-pdf.tsx"
      via: "import EstimatePdfDocument — server-only path, never imported by client components"
      pattern: "EstimatePdfDocument"
    - from: "src/app/api/estimates/[id]/pdf/route.ts"
      to: "src/lib/estimates/pdf-data.ts"
      via: "import getEstimateForPdf"
      pattern: "getEstimateForPdf"
    - from: "src/app/(app)/estimates/[id]/estimate-form.tsx"
      to: "src/app/api/estimates/[id]/pdf/route.ts"
      via: "Download PDF link: href=/api/estimates/[id]/pdf target=_blank"
      pattern: "/api/estimates.*pdf"
---

<objective>
Wave 4: PDF route + Settings estimate templates tab.

Purpose: Delivers the two remaining capabilities: downloadable estimate PDFs (EST-09 per D-14) and estimate template management in Settings (EST-06). The PDF route requires the @react-pdf/renderer Node.js workaround and an ownership security check. The Settings extension is a straightforward tab addition following the existing job templates pattern.

Output: Working /api/estimates/[id]/pdf route that streams a PDF, an EstimatePdfDocument React PDF component with SF-style layout, and an "Estimate Templates" tab in Settings > Templates. References D-14, D-15.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimates/06-CONTEXT.md
@.planning/phases/06-estimates/06-RESEARCH.md
@.planning/phases/06-estimates/06-PATTERNS.md
@.planning/phases/06-estimates/06-UI-SPEC.md
@.planning/phases/06-estimates/06-02-SUMMARY.md
@.planning/phases/06-estimates/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF route + EstimatePdfDocument component</name>
  <files>
    src/app/api/estimates/[id]/pdf/route.ts
    src/components/pdf/estimate-pdf.tsx
  </files>
  <read_first>
    src/lib/estimates/pdf-data.ts — EstimatePdfData interface and getEstimateForPdf function (created in Plan 06-02); understand the data shape the PDF component receives
    next.config.ts — confirm serverExternalPackages: ['@react-pdf/renderer'] is present (added in Plan 06-01)
    .planning/phases/06-estimates/06-RESEARCH.md — section 3 PDF Generation: full route.ts pattern (MUST use pdf().toBlob() NOT renderToBuffer), export const runtime = 'nodejs' requirement, pitfalls 3+4 (edge runtime crash + serverExternalPackages omission), PDF layout diagram (D-15 discretion)
    .planning/phases/06-estimates/06-PATTERNS.md — route.ts pattern (from PATTERNS no-analog section), src/components/pdf/ placement note
  </read_first>
  <action>
    Create src/components/pdf/ directory. Create src/components/pdf/estimate-pdf.tsx as a pure server-side React component file.

    In estimate-pdf.tsx: import Document, Page, Text, View, StyleSheet, Font from '@react-pdf/renderer'. Do NOT use React Context, useRouter, or any browser API — this file runs server-side only. Define EstimatePdfDocumentProps: { data: EstimatePdfData } where EstimatePdfData is imported from '@/lib/estimates/pdf-data'.

    PDF layout (D-15 — SF style, Claude's discretion for exact sizes):
    - Page: A4, 40pt padding all sides, font Helvetica (no custom font download — keeps bundle simple)
    - Header section: Two columns (flexDirection: 'row'). Left: company name (fontSize 16, fontWeight 'bold'), company address, phone, email (fontSize 9, color '#666'). Right aligned: "ESTIMATE" label (fontSize 20, fontWeight 'bold', color '#333'), estimate number "#EST-{estimateNo}" (fontSize 12), date (fontSize 9), expiry date (fontSize 9, labeled "Valid Until:").
    - Horizontal rule: thin View with borderBottom 1pt, color '#ddd', marginVertical 12.
    - Bill To section: "Bill To:" label (fontSize 9, fontWeight 'bold', color '#666', marginBottom 4), customer name (fontSize 11, fontWeight 'bold'), service address (fontSize 9, color '#444').
    - Description section (if data.description): label "Project Description:" (fontSize 9, fontWeight 'bold', color '#666'), description text (fontSize 10), marginTop 12.
    - Line items section: for each group in data.groups: if group.name is non-null, render group name row (fontSize 10, fontWeight 'bold', backgroundColor '#f5f5f5', padding 4 8). Then for each item in the group render a table row: description (flex 3), qty (flex 1, textAlign 'right'), rate (flex 1, textAlign 'right'), total (flex 1, textAlign 'right'). Column headers: Description | Qty | Rate | Total (fontSize 8, fontWeight 'bold', color '#666', borderBottom 1pt). Group subtotal row at bottom of each group (if group has >1 item): right-aligned "Subtotal: $X.XX" in fontSize 9 color '#555'.
    - Grand total row: "TOTAL" label bold, total amount bold, fontSize 12, borderTop 2pt solid '#333', marginTop 8, paddingTop 8, textAlign 'right'.
    - Notes section (if data.notes): "Notes:" label (fontSize 9, fontWeight 'bold'), notes text (fontSize 9, color '#444'), marginTop 16.
    - Do NOT include internalNotes, cost, or margin on the PDF (customer-facing only per D-15).

    Create src/app/api/estimates/[id]/pdf/route.ts:
    - First line after imports: export const runtime = 'nodejs' (MUST be the first export, before the handler)
    - Import pdf from '@react-pdf/renderer'. Import EstimatePdfDocument from '@/components/pdf/estimate-pdf'. Import auth from '@clerk/nextjs/server'. Import getEstimateForPdf from '@/lib/estimates/pdf-data'.
    - Export async function GET(req: Request, { params }: { params: Promise<{ id: string }> }): await auth() → { orgId } → if (!orgId) return new Response('Unauthorized', { status: 401 }).
    - Await params to get id.
    - Call getEstimateForPdf(orgId, id) — returns null if not found or not owned by orgId (withTenant enforces tenant isolation).
    - If null: return new Response('Not found', { status: 404 }).
    - Generate PDF: const blob = await pdf(<EstimatePdfDocument data={data} />).toBlob(). If toBlob() throws (Node.js compatibility issue), fall back to: const buffer2 = await pdf(<EstimatePdfDocument data={data} />).toBuffer(); return new Response(buffer2, headers).
    - const buffer = Buffer.from(await blob.arrayBuffer()).
    - return new Response(buffer, { headers: { 'Content-Type': 'application/pdf', 'Content-Disposition': 'attachment; filename="estimate-' + data.estimateNo + '.pdf"' } }).
    - Wrap the entire PDF generation in try/catch: on error, return new Response(JSON.stringify({ error: 'PDF generation failed' }), { status: 500, headers: { 'Content-Type': 'application/json' } }).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0 — PDF route and component compile without errors
    - src/app/api/estimates/[id]/pdf/route.ts exports GET and runtime = 'nodejs' (grep confirms)
    - src/components/pdf/estimate-pdf.tsx exports EstimatePdfDocument
    - No import of @react-pdf/renderer from any client component (server-only placement)
    - PDF route does not use renderToBuffer (grep on route.ts should return 0 matches for renderToBuffer)
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings > Templates — Estimate Templates tab</name>
  <files>
    src/app/(app)/settings/templates/page.tsx
    src/app/(app)/settings/templates/template-form.tsx
  </files>
  <read_first>
    src/app/(app)/settings/templates/page.tsx — full file: current server component structure, how it loads job templates, how it renders TemplatesPage client component, what props it passes
    src/app/(app)/settings/templates/template-form.tsx — full file: TemplatesPage client component structure, job template CRUD UI pattern (list, create form, delete), existing imports, any Tab wrappers that may already exist
    src/lib/estimates/templates.ts — listEstimateTemplates, createEstimateTemplate, deleteEstimateTemplate, applyEstimateTemplate (from Plan 06-02)
    src/app/(app)/estimates/actions.ts — createEstimateTemplateAction, listEstimateTemplatesAction signatures (from Plan 06-02)
    .planning/phases/06-estimates/06-PATTERNS.md — settings templates page extension pattern (parallel fetch, Tabs wrapper, two tab contents)
    .planning/phases/06-estimates/06-RESEARCH.md — section 5 Templates Architecture (separate tables, group structure via groupName inline)
  </read_first>
  <action>
    In src/app/(app)/settings/templates/page.tsx:
    1. Add import for listEstimateTemplates from '@/lib/estimates/templates'.
    2. Change the data fetch to parallel using Promise.all: const [templates, estimateTemplates, jobCategories] = await Promise.all([listJobTemplates(orgId), listEstimateTemplates(orgId), listJobCategories(orgId)]).
    3. Pass estimateTemplates as an additional prop to the TemplatesPage (or equivalent client component) alongside the existing templates and jobCategories props.

    In src/app/(app)/settings/templates/template-form.tsx:
    1. Read the current component structure carefully before modifying.
    2. Add import for Tabs, TabsList, TabsTrigger, TabsContent from '@/components/ui/tabs'.
    3. Add import for createEstimateTemplateAction, listEstimateTemplatesAction from '@/app/(app)/estimates/actions' (or the lib helpers directly).
    4. Wrap the existing job templates UI in a <TabsContent value="jobs"> section.
    5. Add a new <TabsContent value="estimates"> section with the same CRUD UI pattern: list of estimate templates (name + description + delete button), create form (name input + description textarea + save button). Estimate templates for Phase 6 are simpler than job templates — no category select needed. Line items and task presets are a stretch goal within this task: add them if the template-form.tsx job template UI has a line items section to mirror; otherwise a name + description only form is sufficient for EST-06 basic CRUD.
    6. Wrap both tabs in <Tabs defaultValue="jobs"><TabsList><TabsTrigger value="jobs">Job Templates</TabsTrigger><TabsTrigger value="estimates">Estimate Templates</TabsTrigger></TabsList>...</Tabs>.
    7. Update the component's props interface to accept initialEstimateTemplates: EstimateTemplate[].
    8. Apply the same optimistic update + router.refresh() pattern as job templates (React 19 form auto-reset fix from Phase 2).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - src/app/(app)/settings/templates/page.tsx fetches both listJobTemplates and listEstimateTemplates in parallel
    - src/app/(app)/settings/templates/template-form.tsx has Tabs wrapper with "Job Templates" and "Estimate Templates" tab triggers
    - Estimate Templates tab renders a list of templates and a create form
    - pnpm vitest run exits 0 — no regressions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 06-05 automated: PDF route at /api/estimates/[id]/pdf (Node.js runtime, pdf().toBlob(), auth guard, 404 for wrong tenant), EstimatePdfDocument with SF-style layout, and Estimate Templates tab in Settings.
  </what-built>
  <how-to-verify>
    1. Start the dev server: pnpm dev
    2. Navigate to /estimates and create a test estimate with a customer, description, and 2–3 line items
    3. Open the estimate detail page (/estimates/[id])
    4. Click "Download PDF" — verify the browser downloads a .pdf file that opens and shows: business header, estimate number, customer info, line items table, grand total
    5. Click "Email Estimate" — verify a sonner toast appears: "Email sending arrives in a later release. The estimate PDF is ready to download." (no error, no real email sent)
    6. Test the PDF route directly: open /api/estimates/[id]/pdf in a new browser tab — verify it streams the PDF without error
    7. Navigate to Settings > Templates — verify there are now two tabs: "Job Templates" and "Estimate Templates"
    8. In the Estimate Templates tab: create a template with a name. Verify it appears in the list. Delete it. Verify it disappears.
    9. Back in the estimate form: verify the star picker (opportunity rating) renders 5 stars and clicking a star fills it
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/estimates/[id]/pdf | Must verify auth AND estimate ownership before streaming PDF |
| PDF component → data | Must NOT receive internalNotes, cost, or margin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | GET /api/estimates/[id]/pdf | mitigate | auth() check returns 401 if no orgId; getEstimateForPdf(orgId, id) uses withTenant(orgId) — if estimate belongs to different tenant, RLS returns null → 404; no cross-tenant PDF possible |
| T-06-02 | Information Disclosure | EstimatePdfDocument data prop | mitigate | EstimatePdfData interface excludes internalNotes, cost, margin fields by design (Plan 06-02); PDF component only renders what data provides |
| T-06-01 | Information Disclosure | Settings > Templates (estimate templates) | mitigate | createEstimateTemplateAction uses withTenant(orgId) — templates are tenant-scoped; listEstimateTemplates filters by tenantId |
| T-06-SC | Tampering | @react-pdf/renderer in route | mitigate | Package installed in Plan 06-01 (approved); route uses export const runtime = 'nodejs' to prevent edge deployment |
</threat_model>

<verification>
After plan 06-05: pnpm build exits 0. PDF route returns 200 + application/pdf for owned estimates, 401 for unauthenticated, 404 for unowned. Settings > Templates shows two tabs. All estimate tests still green.
</verification>

<success_criteria>
1. pnpm build exits 0
2. pnpm vitest run exits 0 — no regressions in any test
3. GET /api/estimates/[id]/pdf returns 200 Content-Type: application/pdf for authenticated owner
4. GET /api/estimates/[id]/pdf returns 401 for unauthenticated request
5. route.ts contains: export const runtime = 'nodejs' (not 'edge')
6. route.ts does not contain renderToBuffer
7. Settings > Templates page has two tabs: "Job Templates" and "Estimate Templates"
8. Human verification: PDF downloads and opens with correct SF-style layout; Email stub fires toast; star picker works
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/app/api/estimates/[id]/pdf/route.ts` — exports `GET` handler + `runtime = 'nodejs'`; uses `pdf().toBlob()` + auth + ownership check; returns `application/pdf`
- `src/components/pdf/estimate-pdf.tsx` — exports `EstimatePdfDocument(props: EstimatePdfDocumentProps)`, `EstimatePdfDocumentProps`; @react-pdf/renderer Document/Page/Text/View; SF-style layout per D-15

**Modified files:**
- `src/app/(app)/settings/templates/page.tsx` — adds parallel `listEstimateTemplates(orgId)` fetch; passes `initialEstimateTemplates` prop
- `src/app/(app)/settings/templates/template-form.tsx` — adds `Tabs` wrapper with "Job Templates" + "Estimate Templates" tabs; Estimate Templates tab has list + create + delete CRUD

<output>
Create `.planning/phases/06-estimates/06-05-SUMMARY.md` when done
</output>
