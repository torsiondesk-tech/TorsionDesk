---
phase: 06-estimates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/estimates/estimate-number.test.ts
  - tests/estimates/totals.test.ts
  - tests/estimates/convert.test.ts
  - tests/estimates/status.test.ts
  - tests/estimates/templates.test.ts
  - tests/estimates/dashboard.test.ts
  - tests/estimates/grouped-line-items.test.tsx
  - src/db/schema.ts
  - src/lib/estimates/estimate-number.ts
  - src/lib/estimates/totals.ts
  - src/lib/estimates/status.ts
  - next.config.ts
autonomous: true
requirements:
  - EST-01
  - EST-02
  - EST-04
  - EST-05
  - EST-06
  - EST-07
  - EST-08
  - EST-09

must_haves:
  truths:
    - "pnpm vitest run tests/estimates/ exits non-zero (all 7 RED)"
    - "src/db/schema.ts exports estimateStatus enum and all 11 new/modified tables"
    - "src/lib/estimates/estimate-number.ts exports nextEstimateNo"
    - "src/lib/estimates/totals.ts exports computeEstimateTotals with EstimateTotals type including groupSubtotals"
    - "src/lib/estimates/status.ts exports estimateStatusBadgeVariant and estimateStatusLabel"
    - "next.config.ts contains serverExternalPackages with @react-pdf/renderer"
    - "pnpm db:push completes without error"
  artifacts:
    - path: "tests/estimates/estimate-number.test.ts"
      provides: "RED test: nextEstimateNo sequential + tenant-scoped (EST-01)"
    - path: "tests/estimates/totals.test.ts"
      provides: "RED test: computeEstimateTotals cent-exact math + group subtotals (EST-04)"
    - path: "tests/estimates/convert.test.ts"
      provides: "RED test: convertEstimateToJobAction creates job + 2 events + status=won (EST-05)"
    - path: "tests/estimates/status.test.ts"
      provides: "RED test: status write + customer_event write (EST-02)"
    - path: "tests/estimates/templates.test.ts"
      provides: "RED test: template CRUD + apply round-trip (EST-06)"
    - path: "tests/estimates/dashboard.test.ts"
      provides: "RED test: count-by-status query (EST-07)"
    - path: "tests/estimates/grouped-line-items.test.tsx"
      provides: "RED test: group collapse/expand UI state (EST-04)"
    - path: "src/db/schema.ts"
      provides: "estimateStatus enum + estimates + estimate_line_items + line_item_groups (retrofit job_line_items) + estimate_tags + estimate_assignees + estimate_tasks + estimate_reminders + estimate_templates + estimate_template_line_items + estimate_template_tasks tables"
    - path: "src/lib/estimates/estimate-number.ts"
      provides: "nextEstimateNo(tx, tenantId): Promise<number>"
    - path: "src/lib/estimates/totals.ts"
      provides: "computeEstimateTotals(items, groups): EstimateTotals"
    - path: "src/lib/estimates/status.ts"
      provides: "estimateStatusBadgeVariant(status) + estimateStatusLabel(status)"
    - path: "next.config.ts"
      provides: "serverExternalPackages: ['@react-pdf/renderer']"
  key_links:
    - from: "tests/estimates/*.test.ts"
      to: "src/lib/estimates/*.ts"
      via: "module import (RED — file does not exist yet)"
      pattern: "from '@/lib/estimates/"
    - from: "src/db/schema.ts"
      to: "estimate_line_items.group_id"
      via: "nullable FK to line_item_groups"
      pattern: "groupId.*line_item_groups"
    - from: "src/db/schema.ts"
      to: "job_line_items.group_id"
      via: "nullable FK retrofit"
      pattern: "groupId.*text\\('group_id'\\)"
---

<objective>
Wave 0: RED test scaffold, schema additions, helper library stubs, and @react-pdf/renderer install.

Purpose: Establish the Nyquist test floor (7 RED files) and the full database schema before any implementation so the executor can drive all subsequent waves to GREEN without planning gaps. The schema push is blocking — no downstream plan can verify against a live database until this completes.

Output: 7 RED test files in tests/estimates/, 11 new/modified tables in schema.ts (estimateStatus enum, estimates, estimate_line_items, line_item_groups, estimate_tags, estimate_assignees, estimate_tasks, estimate_reminders, estimate_templates, estimate_template_line_items, estimate_template_tasks; plus group_id retrofit on job_line_items), nextEstimateNo/computeEstimateTotals/status helpers, and pnpm db:push applied. References D-01, D-02, D-04, D-05, D-06, D-07 (schema shape enforces all group and status decisions).
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimates/06-CONTEXT.md
@.planning/phases/06-estimates/06-RESEARCH.md
@.planning/phases/06-estimates/06-PATTERNS.md
@.planning/phases/06-estimates/06-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test scaffold — 7 failing test files for EST-01, EST-02, EST-04, EST-05, EST-06, EST-07</name>
  <files>
    tests/estimates/estimate-number.test.ts
    tests/estimates/totals.test.ts
    tests/estimates/convert.test.ts
    tests/estimates/status.test.ts
    tests/estimates/templates.test.ts
    tests/estimates/dashboard.test.ts
    tests/estimates/grouped-line-items.test.tsx
  </files>
  <read_first>
    tests/jobs/totals.test.ts — exact structure to mirror for totals.test.ts
    tests/jobs/templates.test.ts — exact structure to mirror for templates.test.ts
    src/app/(tech)/tech/estimates/actions.ts — CreateEstimateInput interface (canonical action must satisfy this)
    src/app/(tech)/lib/dexie.ts — CachedEstimate type (listEstimatesAction must satisfy this)
    src/app/(tech)/lib/sync.ts — EstimateCreatePayload / syncEstimateCreate callers
    .planning/phases/06-estimates/06-VALIDATION.md — exact test contracts for all 7 files
    .planning/phases/06-estimates/06-PATTERNS.md — test file header pattern and mock pattern
  </read_first>
  <behavior>
    - estimate-number.test.ts: import nextEstimateNo from '@/lib/estimates/estimate-number' (does not exist — RED). Test: given tenant A has estimates with estimateNo values [1000, 1001], nextEstimateNo returns 1002. Test: returns 1001 for empty tenant (seed 1000).
    - totals.test.ts: import computeEstimateTotals from '@/lib/estimates/totals' (does not exist — RED). Mirror tests/jobs/totals.test.ts. Add test: groupSubtotals accumulation — 2 items in groupId 'g1' with rates 10.00 and 5.00 produces groupSubtotals['g1'] = '15.00'. Cent-exact: 3 × 19.99 = 59.97 (not 59.970000000000006).
    - convert.test.ts: import convertEstimateToJobAction from '@/app/(app)/estimates/actions' (does not exist — RED). Test: calling with a non-Lost estimate creates a job row, sets estimate status to estimate_won, writes 2 customer_events rows (titles match "moved to Won" and "converted to Job"). Test: throws when estimate status is estimate_lost.
    - status.test.ts: import updateEstimateStatusAction from '@/app/(app)/estimates/actions' (does not exist — RED). Test: writes exactly 1 customer_events row with kind='estimate' and title containing '#EST-' + the new status label.
    - templates.test.ts: import createEstimateTemplate, applyEstimateTemplate from '@/lib/estimates/templates' (does not exist — RED). Test: createEstimateTemplate + applyEstimateTemplate round-trip returns lineItems and tasks matching the template input.
    - dashboard.test.ts: import countEstimatesByStatus from '@/app/(app)/estimates/actions' (does not exist — RED). Test: groups by status correctly. Test: returns zero counts for statuses with no estimates (not missing keys).
    - grouped-line-items.test.tsx: import GroupedLineItems from '@/components/line-items/grouped-line-items' (does not exist — RED). Component test: clicking the group header CollapsibleTrigger toggles the collapsed state. Use @testing-library/react. Test only collapse/expand state — not the full catalog search.
  </behavior>
  <action>
    Create the tests/estimates/ directory. Write all 7 test files importing from modules that do not yet exist so the suite is RED. Use the file header pattern from 06-PATTERNS.md. Mock @clerk/nextjs/server with vi.mock (same pattern as tests/jobs/ analogs). Mock withTenant and the DB client to avoid real DB calls. For grouped-line-items.test.tsx use @testing-library/react with jsdom (check vitest.config.ts for the jsdom environment setting used in existing component tests). Each test must import from the exact path Plan 06-02 and 06-03 will create, so no path changes are needed when the modules ship.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm vitest run tests/estimates/ 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm vitest run tests/estimates/ exits non-zero with FAIL showing 7 test files all failing due to import errors (Cannot find module). No passing tests in the estimates suite yet.</done>
</task>

<task type="auto">
  <name>Task 2: Schema additions — estimateStatus enum + 10 new tables + job_line_items retrofit + helpers + next.config.ts + pnpm add @react-pdf/renderer</name>
  <files>
    src/db/schema.ts
    src/lib/estimates/estimate-number.ts
    src/lib/estimates/totals.ts
    src/lib/estimates/status.ts
    next.config.ts
  </files>
  <read_first>
    src/db/schema.ts — full file: existing table patterns (jobLineItems lines 724–770, jobTemplates lines 967–1071, jobTasks/jobReminders lines 900–965), eventKind enum, lineItemType enum, existing imports
    src/lib/jobs/job-number.ts — exact 17-line pattern to replicate as nextEstimateNo
    src/lib/jobs/totals.ts — computeJobTotals to mirror as computeEstimateTotals (add groupSubtotals)
    src/lib/jobs/transitions.ts — statusBadgeVariant + statusLabel pattern to replicate for estimates
    next.config.ts — current content to extend with serverExternalPackages
    .planning/phases/06-estimates/06-RESEARCH.md — section 2 Schema Design (full table definitions) and section 3 PDF Generation (next.config.ts change)
    .planning/phases/06-estimates/06-PATTERNS.md — schema table patterns, totals interface delta, status helper pattern
  </read_first>
  <action>
    Run: pnpm add @react-pdf/renderer

    In next.config.ts: add serverExternalPackages: ['@react-pdf/renderer'] at the top level of the nextConfig object (NOT inside experimental). Do not change any other config key.

    In src/db/schema.ts, add in this order after existing enums:
    1. estimateStatus pgEnum with values: estimate_requested, estimate_provided, estimate_accepted, estimate_won, estimate_lost.
    2. lineItemGroups table (shared by estimates and jobs): columns id, tenantId, estimateId (nullable), jobId (nullable), name, sortOrder, createdAt, updatedAt. RLS policy using current_setting('app.current_tenant_id', true). Two composite FK constraints: (tenantId, estimateId) → (estimates.tenantId, estimates.id) onDelete cascade; (tenantId, jobId) → (jobs.tenantId, jobs.id) onDelete cascade. Add a Drizzle custom CHECK constraint via sql that enforces (estimate_id IS NOT NULL)::int + (job_id IS NOT NULL)::int = 1 (exactly one owner). Use the enableRLS() pattern.
    3. estimates table: id, tenantId, estimateNo (integer notNull), customerId, contactId (nullable), serviceLocationId (nullable), categoryId (nullable), status (estimateStatus default estimate_requested), description, poNumber, opportunityRating (integer nullable 1–5), referralSourceId (nullable), expiryDate (date nullable), followUpDate (date nullable), onSiteDate (timestamp nullable), arrivalWindowStart, arrivalWindowEnd, notesForTechs, notes (customer-facing, shown on PDF), internalNotes (internal, NOT on PDF), assignedAgentId (nullable), createdAt, updatedAt. UNIQUE(tenantId, estimateNo). UNIQUE(tenantId, id). Indexes on status, customerId. Composite FKs for tenantId+customerId, tenantId+contactId, tenantId+serviceLocationId, tenantId+categoryId, tenantId+referralSourceId. RLS policy. enableRLS().
    4. estimateLineItems table: mirrors jobLineItems exactly, swap jobId → estimateId, add groupId text nullable (FK to line_item_groups). RLS. enableRLS().
    5. estimateTags table: mirrors job_tags pattern (estimateId FK + tagId FK + tenantId). RLS. enableRLS().
    6. estimateAssignees table: mirrors job_assignees (estimateId FK + userId text + tenantId). RLS. enableRLS().
    7. estimateTasks table: mirrors jobTasks swapping jobId → estimateId. RLS. enableRLS().
    8. estimateReminders table: mirrors jobReminders swapping jobId → estimateId. RLS. enableRLS().
    9. estimateTemplates table: mirrors jobTemplates without categoryId column. RLS. enableRLS().
    10. estimateTemplateLineItems table: mirrors jobTemplateLineItems, swap templateId FK to estimateTemplates, add groupName text nullable (inline group name for template structure). RLS. enableRLS().
    11. estimateTemplateTasks table: mirrors jobTemplateTasks swapping templateId FK to estimateTemplates. RLS. enableRLS().

    Also in src/db/schema.ts: add groupId: text('group_id') (nullable, no default) to the existing jobLineItems table definition. Add FK constraint for (tenantId, groupId) → (lineItemGroups.tenantId, lineItemGroups.id) onDelete set null. This is the retrofit per D-07.

    Create src/lib/estimates/estimate-number.ts: exact copy of src/lib/jobs/job-number.ts, rename function to nextEstimateNo, swap the job table import to estimates, swap column reference to estimateNo, update JSDoc to reference #EST-{N} and the Phase 10 reseed note.

    Create src/lib/estimates/totals.ts: mirror src/lib/jobs/totals.ts. Rename computeJobTotals to computeEstimateTotals. Replace JobTotals interface with EstimateTotals: remove expenses/driveLabor fields, add groupSubtotals: Record<string, string> (groupId → subtotal formatted string). In the accumulation loop, whenever item.groupId is non-null, accumulate lineCents into groupCents[item.groupId]. After the main loop, convert groupCents entries to formatted strings in the same toMoney helper pattern. Export EstimateTotals type and computeEstimateTotals function. Also export EstimateLineItemInput type (same shape as existing line item input but with optional groupId: string | null).

    Create src/lib/estimates/status.ts: export estimateStatusBadgeVariant(status: string) returning 'outline' | 'default' | 'secondary' | 'destructive' — map estimate_requested/estimate_provided to 'outline', estimate_accepted to 'default', estimate_won to 'secondary', estimate_lost to 'destructive'. Export estimateStatusLabel(status: string) returning display strings: Requested, Provided, Accepted, Won, Lost.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm build exits 0 (TypeScript compiles, all new schema exports resolve)
    - src/db/schema.ts exports estimateStatus, lineItemGroups, estimates, estimateLineItems, estimateTags, estimateAssignees, estimateTasks, estimateReminders, estimateTemplates, estimateTemplateLineItems, estimateTemplateTasks
    - jobLineItems in schema.ts contains groupId column
    - next.config.ts contains serverExternalPackages: ['@react-pdf/renderer']
    - src/lib/estimates/estimate-number.ts exports nextEstimateNo
    - src/lib/estimates/totals.ts exports computeEstimateTotals and EstimateTotals
    - src/lib/estimates/status.ts exports estimateStatusBadgeVariant and estimateStatusLabel
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] pnpm db:push — apply schema to Supabase</name>
  <files></files>
  <read_first>
    src/db/schema.ts — confirm the file compiles before pushing
  </read_first>
  <action>
    Run pnpm db:push to apply the new schema to Supabase. This creates the estimateStatus enum, all 10 new tables, and adds group_id to job_line_items. Drizzle will prompt for confirmation of destructive changes — there are none (only additive changes); the job_line_items column addition is backward-compatible (nullable, no default, existing rows get NULL). If drizzle-kit push shows any prompt, accept. This task MUST succeed before any downstream plan can verify against the live database.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm db:push 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm db:push exits 0. Output includes confirmation that estimate_status enum, line_item_groups, estimates, estimate_line_items, estimate_tags, estimate_assignees, estimate_tasks, estimate_reminders, estimate_templates, estimate_template_line_items, estimate_template_tasks tables were created and job_line_items was altered to add group_id column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → module | Tests import modules that don't exist — expected RED state |
| Schema push → Supabase | Schema changes are additive; backward-compatible (nullable columns, new tables) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | estimates table | mitigate | RLS policy using current_setting('app.current_tenant_id', true) on every new table — all 10 new tables must have the pgPolicy block and enableRLS() per schema pattern |
| T-06-SC | Tampering | pnpm add @react-pdf/renderer | mitigate | Package legitimacy audit in RESEARCH.md confirms [OK]; version 4.5.1 approved |
</threat_model>

<verification>
Full wave verification: pnpm vitest run tests/estimates/ must exit non-zero (all RED). pnpm build must exit 0. pnpm db:push must exit 0. After this plan, exactly 7 test files exist in tests/estimates/ all failing, schema is live in Supabase, and all helper libs export their signatures.
</verification>

<success_criteria>
1. pnpm vitest run tests/estimates/ exits non-zero — 7 test files, all FAIL (correct RED state)
2. pnpm build exits 0 — TypeScript compiles with all new schema exports and lib files
3. pnpm db:push exits 0 — schema applied to Supabase including all 10 new tables and job_line_items retrofit
4. next.config.ts contains serverExternalPackages: ['@react-pdf/renderer']
5. src/lib/estimates/ directory contains estimate-number.ts, totals.ts, status.ts with correct exports
</success_criteria>

## Artifacts this phase produces

**New files:**
- `tests/estimates/estimate-number.test.ts` — RED test: nextEstimateNo
- `tests/estimates/totals.test.ts` — RED test: computeEstimateTotals
- `tests/estimates/convert.test.ts` — RED test: convertEstimateToJobAction
- `tests/estimates/status.test.ts` — RED test: updateEstimateStatusAction
- `tests/estimates/templates.test.ts` — RED test: createEstimateTemplate/applyEstimateTemplate
- `tests/estimates/dashboard.test.ts` — RED test: countEstimatesByStatus
- `tests/estimates/grouped-line-items.test.tsx` — RED test: GroupedLineItems collapse/expand
- `src/lib/estimates/estimate-number.ts` — exports `nextEstimateNo(tx, tenantId): Promise<number>`
- `src/lib/estimates/totals.ts` — exports `computeEstimateTotals(items, groups): EstimateTotals`, `EstimateTotals`, `EstimateLineItemInput`
- `src/lib/estimates/status.ts` — exports `estimateStatusBadgeVariant(status): string`, `estimateStatusLabel(status): string`

**Modified files:**
- `src/db/schema.ts` — adds `estimateStatus` enum; adds `lineItemGroups`, `estimates`, `estimateLineItems`, `estimateTags`, `estimateAssignees`, `estimateTasks`, `estimateReminders`, `estimateTemplates`, `estimateTemplateLineItems`, `estimateTemplateTasks` tables; adds `groupId` column to `jobLineItems`
- `next.config.ts` — adds `serverExternalPackages: ['@react-pdf/renderer']`

**New DB tables (Supabase):**
- `line_item_groups` — polymorphic group owner (estimate_id XOR job_id), per-tenant RLS
- `estimates` — main estimate record with estimateStatus enum
- `estimate_line_items` — mirrors job_line_items + group_id FK
- `estimate_tags` — junction table
- `estimate_assignees` — junction table
- `estimate_tasks` — mirrors job_tasks
- `estimate_reminders` — mirrors job_reminders
- `estimate_templates` — mirrors job_templates without categoryId
- `estimate_template_line_items` — mirrors job_template_line_items + groupName inline
- `estimate_template_tasks` — mirrors job_template_tasks
- `job_line_items.group_id` column (nullable retrofit)

<output>
Create `.planning/phases/06-estimates/06-01-SUMMARY.md` when done
</output>
