---
phase: 06-estimates
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/components/line-items/grouped-line-items.tsx
  - src/components/line-items/star-picker.tsx
  - src/app/(app)/jobs/[id]/job-form.tsx
  - src/components/shell/nav-config.ts
autonomous: true
requirements:
  - EST-01
  - EST-04

must_haves:
  truths:
    - "GroupedLineItems renders groups with shadcn Collapsible — collapse/expand toggles show/hide items"
    - "GroupedLineItems renders flat (ungrouped) items when no groups exist"
    - "StarPicker renders 5 stars; clicking star N sets value N; filled stars use text-yellow-400"
    - "Job form renders GroupedLineItems in place of existing flat line items — existing job line items still display correctly (all have groupId=null)"
    - "Estimates nav item is visible in sidebar (enabled: true in nav-config.ts)"
    - "pnpm vitest run tests/estimates/grouped-line-items.test.tsx exits 0"
  artifacts:
    - path: "src/components/line-items/grouped-line-items.tsx"
      provides: "GroupedLineItems client component — groups with Collapsible, flat mode, subtotals, Add Group button, Add Line Item per group/flat, catalog search, CreateCatalogItemModal embed, margin column hidden for tech role"
      exports:
        - "GroupedLineItems"
        - "LineItemRow"
        - "LineItemGroup"
    - path: "src/components/line-items/star-picker.tsx"
      provides: "StarPicker client component — 1–5 clickable stars using Lucide Star + fill toggle"
      exports:
        - "StarPicker"
    - path: "src/app/(app)/jobs/[id]/job-form.tsx"
      provides: "job form retrofitted to use GroupedLineItems (backward-compatible — existing jobs with groupId=null display as flat ungrouped)"
    - path: "src/components/shell/nav-config.ts"
      provides: "estimates nav item enabled: true"
  key_links:
    - from: "src/app/(app)/estimates/[id]/estimate-form.tsx"
      to: "src/components/line-items/grouped-line-items.tsx"
      via: "import GroupedLineItems from '@/components/line-items/grouped-line-items'"
      pattern: "GroupedLineItems"
    - from: "src/app/(app)/jobs/[id]/job-form.tsx"
      to: "src/components/line-items/grouped-line-items.tsx"
      via: "import replaces existing line-items.tsx import"
      pattern: "from '@/components/line-items/grouped-line-items'"
---

<objective>
Wave 2: Build the two reusable shared components (GroupedLineItems + StarPicker), retrofit the job form to use grouped line items, and enable the Estimates sidebar nav item.

Purpose: GroupedLineItems is the most complex new UI primitive in this phase — it must handle both grouped and flat modes, catalog search, inline creation, margin visibility by role, and per-group subtotals. Building it standalone in its own plan (before the estimate form) means the executor focuses purely on the component contract rather than fighting with the full form layout. The job form retrofit here ensures existing jobs continue working without regressions.

Output: Two reusable components (src/components/line-items/), job-form.tsx retrofitted, and sidebar nav enabled. References D-04, D-05, D-06, D-07, D-08.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-estimates/06-CONTEXT.md
@.planning/phases/06-estimates/06-RESEARCH.md
@.planning/phases/06-estimates/06-PATTERNS.md
@.planning/phases/06-estimates/06-UI-SPEC.md
@.planning/phases/06-estimates/06-01-SUMMARY.md
@.planning/phases/06-estimates/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroupedLineItems + StarPicker shared components</name>
  <files>
    src/components/line-items/grouped-line-items.tsx
    src/components/line-items/star-picker.tsx
  </files>
  <read_first>
    src/app/(app)/jobs/[id]/line-items.tsx — full file: existing flat line items component; catalog search hook (useCatalogSearch), computeMargin helper, toMoney/parseMoney helpers, line item row JSX, Add Line Item dialog pattern, Tabs (Products & Services / Expenses), CreateCatalogItemModal usage — ALL of this is the base to extend with groups
    src/db/schema.ts — lineItemType enum values (to know valid type values for line items)
    src/components/ui/collapsible.tsx — Collapsible, CollapsibleTrigger, CollapsibleContent export shape (to understand if asChild is Radix or Base UI here — IMPORTANT: shadcn Collapsible uses Radix, so asChild is OK inside Collapsible only; Base UI dialog triggers still use render prop)
    src/lib/roles.ts — role constants to check for TECHNICIAN role guard (margin column hiding per D-08)
    src/lib/estimates/totals.ts — EstimateTotals interface (groupSubtotals: Record<string, string>)
    .planning/phases/06-estimates/06-PATTERNS.md — GroupedLineItems imports pattern, group collapse pattern, money helpers, catalog search hook, margin compute helper, StarPicker implementation
    .planning/phases/06-estimates/06-UI-SPEC.md — color contract (filled stars text-yellow-400, group header uses bg-muted, subtotals in muted-foreground), spacing scale
    .planning/phases/06-estimates/06-RESEARCH.md — "Don't Hand-Roll" table (use Lucide Star + shadcn Collapsible, NOT custom collapse logic)
  </read_first>
  <behavior>
    GroupedLineItems:
    - Receives props: groups (LineItemGroup[]), items (LineItemRow[]), onChange(groups, items), role (for margin visibility per D-08)
    - LineItemGroup type: { id: string; name: string; sortOrder: number }
    - LineItemRow type: { id: string; groupId: string | null; type: string; refId?: string; description?: string; qty: string; rate: string; cost: string; taxItemId?: string | null; sortOrder: number; title?: string }
    - When groups.length === 0: renders a flat list (no group headers) with an "Add Group" button below the items
    - When groups exist: each group renders as shadcn Collapsible with header (group name, subtotal when collapsed, ChevronDown/ChevronRight icon). Items with matching groupId render inside CollapsibleContent
    - Items with groupId=null render in an "Ungrouped" section above all groups (or below — Claude's discretion per specifics)
    - "Add Group" button: adds a new group with auto-name "Group 1", "Group 2", etc. via inline text input in the group header
    - Each group shows a subtotal row at the bottom (sum of rate × qty for items in that group). Uses toMoney helper from existing line-items.tsx
    - "Add Line Item" per group: opens the same Add Line Item dialog (catalog search combobox + type selector). Dialog uses Base UI DialogTrigger with render prop (NOT asChild) per CLAUDE.md
    - Margin column: rendered as a read-only computed cell using computeMargin helper. Hidden (not rendered) when role === 'technician' (per D-08)
    - Catalog search: debounced search using searchProductsAction / searchServicesAction (same as existing line-items.tsx hook)
    - CreateCatalogItemModal: embeds inline in the Add Line Item dialog (same as existing pattern)
    - Tabs (Products & Services / Expenses): same tab structure as existing line-items.tsx. Expenses do NOT get group support (estimates have no expense line items — only Products, Services, Discount types)
    - Export: GroupedLineItems component, LineItemRow type, LineItemGroup type

    StarPicker:
    - Props: value: number | null, onChange: (rating: number) => void, readOnly?: boolean
    - Renders 5 Lucide Star icons in a flex row
    - Stars 1–N (where N = value) render with fill='currentColor' className='text-yellow-400'
    - Stars N+1–5 render with fill='none' className='text-muted-foreground'
    - Non-readOnly: each star is a <button type="button"> with onClick={() => onChange(star)}. Clicking the current value again sets the same value (no toggle-to-null behavior needed)
    - readOnly: cursor-default, no onClick
    - Export: StarPicker component, StarPickerProps type
  </behavior>
  <action>
    Create src/components/line-items/ directory if it doesn't exist.

    Create src/components/line-items/star-picker.tsx first (simpler, no dependencies). Import Star from lucide-react, cn from @/lib/utils. Implement per the behavior spec above.

    Create src/components/line-items/grouped-line-items.tsx. This is a client component ('use client'). Copy the catalog search hook (useCatalogSearch), computeMargin, toMoney, parseMoney helpers from src/app/(app)/jobs/[id]/line-items.tsx verbatim. Import shadcn Collapsible, CollapsibleTrigger, CollapsibleContent from @/components/ui/collapsible. Import Plus, ChevronDown, ChevronRight, Trash2, GripVertical from lucide-react. Import Button, Input, Textarea, Label from shadcn. Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger from @/components/ui/dialog. Import Tabs, TabsContent, TabsList, TabsTrigger from @/components/ui/tabs. Import Select, SelectContent, SelectItem, SelectTrigger, SelectValue from @/components/ui/select. Import CreateCatalogItemModal from @/components/catalog/create-item-modal (verify this path from the existing line-items.tsx import).

    For the group collapse toggle: use shadcn Collapsible which is Radix-based (not Base UI), so CollapsibleTrigger with asChild is acceptable here. Only DialogTrigger uses the Base UI render prop pattern.

    In the component, maintain local state: open: Record<string, boolean> for each group's collapsed state (default true = expanded). When a group collapses, show subtotal in the header row.

    Add Group button (D-06): when no groups exist, render a single "Add Group" button below the flat item list. When groups exist, render it below all groups.

    For the "ungrouped" items section label: when items with groupId=null exist alongside grouped items, label the section "Ungrouped" in muted text.

    After creating both components, run pnpm vitest run tests/estimates/grouped-line-items.test.tsx to verify the component test goes GREEN (the test was written in Plan 06-01 to test collapse/expand state).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm vitest run tests/estimates/grouped-line-items.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm vitest run tests/estimates/grouped-line-items.test.tsx exits 0
    - pnpm build exits 0
    - src/components/line-items/grouped-line-items.tsx exports GroupedLineItems, LineItemRow, LineItemGroup
    - src/components/line-items/star-picker.tsx exports StarPicker
    - GroupedLineItems renders shadcn Collapsible per-group (verified by test)
    - StarPicker renders 5 Star icons with fill toggling (verified by build)
  </done>
</task>

<task type="auto">
  <name>Task 2: Job form retrofit + sidebar nav enable</name>
  <files>
    src/app/(app)/jobs/[id]/job-form.tsx
    src/components/shell/nav-config.ts
  </files>
  <read_first>
    src/app/(app)/jobs/[id]/job-form.tsx — full file: current line items section (which imports from line-items.tsx and renders a flat list), form state shape, how lineItems are structured and sent to updateJob action
    src/app/(app)/jobs/actions.ts — lineItemSchema Zod shape; how updateJob currently processes line items (replace-all pattern); the schema now needs optional groupId field; also need to add group mutation logic
    src/components/line-items/grouped-line-items.tsx — GroupedLineItems props interface (LineItemRow, LineItemGroup types)
    src/components/shell/nav-config.ts — full file: current nav items array, estimates entry (enabled: false at line ~29)
    .planning/phases/06-estimates/06-PATTERNS.md — nav-config.ts change pattern (single line change: enabled: false → true); job form retrofit notes from RESEARCH.md section 8
    .planning/phases/06-estimates/06-RESEARCH.md — section 8 Job Form Retrofit: what changes in job form, actions retrofit (lineItemSchema groupId extension, lineItemGroupSchema), backward compatibility note
  </read_first>
  <action>
    In src/components/shell/nav-config.ts: find the estimates nav entry (key: 'estimates', enabled: false) and change enabled: false to enabled: true. No other changes.

    In src/app/(app)/jobs/[id]/job-form.tsx:
    1. Replace the import of the flat line-items component (likely from '@/app/(app)/jobs/[id]/line-items' or similar) with: import { GroupedLineItems, type LineItemRow, type LineItemGroup } from '@/components/line-items/grouped-line-items'
    2. Update the form state to include groups: LineItemGroup[] (default: []). Existing jobs load from server with no groups (all items have groupId=null) — this is backward-compatible.
    3. Replace the flat line items section in the JSX with <GroupedLineItems groups={groups} items={lineItems} onChange={(newGroups, newItems) => { setGroups(newGroups); setLineItems(newItems) }} role={role} /> where role is the current user's role (already available or derive from the auth context passed to the form).
    4. In the form submission / updateJob call: include groups in the payload alongside lineItems. The updateJob action (in jobs/actions.ts) needs to be extended to accept and persist groups.

    In src/app/(app)/jobs/actions.ts:
    1. Extend lineItemSchema to add optional groupId: z.string().nullable().optional()
    2. Add lineItemGroupSchema: z.object({ id: z.string().optional(), name: z.string().min(1), sortOrder: z.number().default(0) })
    3. In updateJob action: before replacing job_line_items, replace line_item_groups for this job. First DELETE all lineItemGroups WHERE jobId = jobId (within withTenant tx). Then INSERT new groups if any, collect returned IDs, build a mapping from client-side temp ID to DB ID. Then INSERT job_line_items with resolved groupId (null for ungrouped items). The replace-all pattern is preserved — just expanded to handle groups.

    All existing jobs that open after this change will show flat ungrouped items (groups array is empty, all line items have groupId=null). The GroupedLineItems component handles this gracefully (renders flat mode with "Add Group" button).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0 — job form compiles with GroupedLineItems
    - src/components/shell/nav-config.ts has enabled: true for estimates entry
    - src/app/(app)/jobs/[id]/job-form.tsx imports GroupedLineItems (not old flat line-items.tsx)
    - src/app/(app)/jobs/actions.ts lineItemSchema includes optional groupId field
    - src/app/(app)/jobs/actions.ts updateJob action handles line_item_groups DELETE + INSERT before line items
    - pnpm vitest run exits 0 — no regressions in existing jobs tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → GroupedLineItems | Margin column visibility: role prop from server-rendered form; client cannot self-promote role |
| Job form retrofit → jobs/actions.ts | New groupId field is optional/nullable — no existing behavior changes for jobs without groups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | GroupedLineItems margin column | mitigate | Margin column hidden when role prop is 'technician' (D-08); role is server-set via form props, not client-controlled |
| T-06-05 | Tampering | GroupedLineItems totals | accept | Client-side subtotal display is decorative; server-authoritative computeEstimateTotals is the source of truth for all saved values |
</threat_model>

<verification>
After plan 06-03: pnpm build exits 0. pnpm vitest run exits 0 (all estimates tests still green, no job test regressions). Estimates nav item is visible in sidebar. Job form imports GroupedLineItems. GroupedLineItems collapse test passes.
</verification>

<success_criteria>
1. pnpm build exits 0
2. pnpm vitest run exits 0 — all tests green, no regressions
3. src/components/line-items/grouped-line-items.tsx exports GroupedLineItems with collapse/expand via shadcn Collapsible
4. src/components/line-items/star-picker.tsx exports StarPicker with Lucide Star + fill toggle
5. src/components/shell/nav-config.ts has enabled: true for estimates
6. src/app/(app)/jobs/[id]/job-form.tsx imports from @/components/line-items/grouped-line-items
7. src/app/(app)/jobs/actions.ts lineItemSchema includes optional groupId field
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/components/line-items/grouped-line-items.tsx` — exports `GroupedLineItems`, `LineItemRow`, `LineItemGroup`
- `src/components/line-items/star-picker.tsx` — exports `StarPicker`, `StarPickerProps`

**Modified files:**
- `src/app/(app)/jobs/[id]/job-form.tsx` — swaps flat line-items import for `GroupedLineItems`; adds groups state; passes groups in updateJob payload
- `src/app/(app)/jobs/actions.ts` — adds `groupId` to `lineItemSchema`; adds `lineItemGroupSchema`; extends `updateJob` to handle `line_item_groups` replace-all
- `src/components/shell/nav-config.ts` — flips `estimates` entry `enabled: false` → `enabled: true`

<output>
Create `.planning/phases/06-estimates/06-03-SUMMARY.md` when done
</output>
