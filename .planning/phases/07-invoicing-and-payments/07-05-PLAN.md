---
phase: 07-invoicing-and-payments
plan: 05
type: execute
wave: 4
depends_on:
  - 07-04
files_modified:
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/components/invoices/invoice-pdf.tsx
  - src/app/(app)/dispatch/popup/dispatch-popup.tsx
  - src/app/(app)/jobs/[id]/page.tsx
  - src/app/(app)/jobs/jobs-sidebar.tsx
autonomous: true
requirements:
  - INV-01
  - INV-08
  - INV-10
  - INV-11
  - INV-13

must_haves:
  truths:
    - "GET /api/invoices/[id]/pdf returns 200 with Content-Type: application/pdf"
    - "Invoice PDF contains company header, Invoice# prefix 'INV-', line items table, totals, Balance Due highlighted"
    - "Work order variant (?workOrder=true) includes line items + completion notes + signature image"
    - "Dispatch popup 'Close & Invoice' onClick calls createInvoiceFromJobAction and navigates to /invoices/[id] + router.refresh()"
    - "Dispatch popup 'Deposits' onClick navigates to /payments/new?jobId=X&type=deposit"
    - "Job detail action bar shows 'Create Invoice' button when job.status === 'completed'; shows invoice link when status === 'invoiced'"
    - "Jobs sidebar 'To Be Invoiced' bucket count queries completed jobs without an existing invoice"
  artifacts:
    - path: "src/app/api/invoices/[id]/pdf/route.ts"
      provides: "PDF API route — GET handler, Node.js runtime, streams InvoicePdfDocument via @react-pdf/renderer"
    - path: "src/components/invoices/invoice-pdf.tsx"
      provides: "InvoicePdfDocument component — mirrors EstimatePdfDocument; adds payment history table and optional work-order section"
  key_links:
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/lib/invoices/pdf-data.ts getInvoiceForPdf"
      via: "Route fetches invoice data then renders InvoicePdfDocument"
      pattern: "getInvoiceForPdf"
    - from: "src/app/(app)/dispatch/popup/dispatch-popup.tsx"
      to: "createInvoiceFromJobAction + router.refresh()"
      via: "Close & Invoice onClick: MUST call router.refresh() per CLAUDE.md dispatch board auto-refresh pattern (self:false)"
      pattern: "router.refresh"
    - from: "src/app/(app)/jobs/[id]/page.tsx"
      to: "createInvoiceFromJobAction"
      via: "Create Invoice button on job action bar; visible only when status === 'completed'"
      pattern: "createInvoiceFromJobAction"
---

<objective>
Wave 4: Invoice PDF route + InvoicePdfDocument component + wire dispatch popup + wire job detail page + fix jobs sidebar 'To Be Invoiced' bucket.

Purpose: Completes all entry points for invoice creation and all download paths for invoice PDF. The dispatch popup and job detail page are the primary office entry points for createInvoiceFromJobAction. The PDF route closes INV-08.

Output: /api/invoices/[id]/pdf route, InvoicePdfDocument component, dispatch popup wired, job detail page wired, jobs sidebar bucket fixed. References INV-01 (createInvoiceFromJobAction entry points), INV-08 (PDF), INV-10 (Create Invoice button), INV-11 (invoice link in job status), INV-13 (Record Deposit entry points).
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-invoicing-and-payments/07-CONTEXT.md
@.planning/phases/07-invoicing-and-payments/07-UI-SPEC.md
@.planning/phases/07-invoicing-and-payments/07-PATTERNS.md
@.planning/phases/07-invoicing-and-payments/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoice PDF route + InvoicePdfDocument component</name>
  <files>
    src/app/api/invoices/[id]/pdf/route.ts
    src/components/invoices/invoice-pdf.tsx
  </files>
  <read_first>
    src/app/api/estimates/[id]/pdf/route.ts — complete file: Node.js runtime declaration, pdf() call pattern, blob vs buffer fallback, Content-Disposition filename, auth + notFound pattern
    src/components/pdf/estimate-pdf.tsx — complete file: Document/Page/Text/View/StyleSheet pattern, header layout (company info left, estimate number right), line items table, totals section, React.createElement usage
    src/lib/invoices/pdf-data.ts — InvoicePdfData type (created in Plan 02): contains invoice, lineItems, customer, job (for jobNo + completion notes), jobSignatures (for signature URL), paymentAllocations
    next.config.ts — serverExternalPackages containing @react-pdf/renderer (already set from Phase 6, confirm it is present)
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — invoice-pdf route pattern (exact copy with substitutions), InvoicePdfDocument additions beyond estimate PDF (INVOICE label, INV- prefix, payment terms/due date in meta block, payments received table, balance due highlighted, work order section)
    .planning/phases/07-invoicing-and-payments/07-UI-SPEC.md — PDF is excluded from the office UI styling contract but must include: 'INVOICE' label, Invoice# with INV- prefix, Invoice Total/Amount Paid/Balance Due labels from copywriting contract
  </read_first>
  <action>
    Create src/app/api/invoices/[id]/pdf/route.ts as a Node.js runtime route. First line: export const runtime = 'nodejs' (REQUIRED — @react-pdf/renderer is Node-only; never Edge). Mirror src/app/api/estimates/[id]/pdf/route.ts exactly with these substitutions: getEstimateForPdf → getInvoiceForPdf (from '@/lib/invoices/pdf-data'), EstimatePdfDocument → InvoicePdfDocument (from '@/components/invoices/invoice-pdf'), filename: 'invoice-${data.invoiceNo}.pdf'. Read the workOrder boolean from URL searchParams: const { searchParams } = new URL(req.url); const workOrder = searchParams.get('workOrder') === 'true'. Pass workOrder to InvoicePdfDocument. Keep the blob/buffer fallback pattern verbatim.

    Create src/components/invoices/invoice-pdf.tsx. Mirror src/components/pdf/estimate-pdf.tsx completely. Import: React, Document, Page, Text, View, StyleSheet from '@react-pdf/renderer'. Import type InvoicePdfData. Prop: { data: InvoicePdfData; workOrder?: boolean }.

    Structure differences from estimate PDF:
    - Header label: 'INVOICE' instead of 'ESTIMATE'. Invoice# prefixed 'INV-{invoiceNo}' instead of 'EST-{estimateNo}'.
    - Meta block (right side of header): add Invoice Date (formatted), Due Date (formatted), Payment Terms ('Net {paymentTermsDays} days'), Sent By (if present).
    - Line items table: same structure as estimate PDF (Title | Qty | Rate | Amount columns). Discount rows render amount in red (same pattern as existing estimate PDF discount handling).
    - Totals section: rows in order: Subtotal, Tax Total (if > 0), Discount (if > 0 as negative), Invoice Total (bolded), Amount Paid (sum of allocations), Balance Due (bolded, underlined if > 0 to draw attention).
    - Payments received table (always rendered if there are allocations): below totals, a simple table: Payment# | Method | Amount | Received On. Header row 'PAYMENTS RECEIVED'.
    - Work order section (conditional on workOrder === true): horizontal rule separator, heading 'WORK ORDER', line items table (same as invoice line items above — but rendered again in work order section as service record), completion notes (from data.job.completionNotes if present), signature image (if data.signature?.imageUrl — use Image from @react-pdf/renderer; handle missing image gracefully with a placeholder box and 'Customer Signature' label).
    - Footer: company address + phone (from data.customer.tenantProfile if available, or omit if not fetched).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - src/app/api/invoices/[id]/pdf/route.ts exists with export const runtime = 'nodejs' as first line
    - src/components/invoices/invoice-pdf.tsx exports InvoicePdfDocument
    - InvoicePdfDocument renders 'INVOICE' label, 'INV-{N}' prefix, Invoice Total/Amount Paid/Balance Due rows
    - workOrder prop controls conditional work order section
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatch popup + job detail page + fix jobs sidebar bucket</name>
  <files>
    src/app/(app)/dispatch/popup/dispatch-popup.tsx
    src/app/(app)/jobs/[id]/page.tsx
    src/app/(app)/jobs/jobs-sidebar.tsx
  </files>
  <read_first>
    src/app/(app)/dispatch/popup/dispatch-popup.tsx — complete file: find ActionButton components ~lines 414–421 ('Deposits' and 'Close & Invoice' stubs), existing imports (router, toast/sonner, orgId source), local job state shape, CLAUDE.md dispatch auto-refresh pattern (router.refresh() is CRITICAL — self:false means own broadcast never received)
    src/app/(app)/jobs/[id]/page.tsx — complete file: existing action bar buttons, job status display area, existing imports; find where 'Create Invoice' button should go (conditional on job.status === 'completed'); find where invoice link should appear (conditional on job having an invoice — query invoices WHERE jobId = id)
    src/app/(app)/jobs/jobs-sidebar.tsx — complete file: find 'to_be_invoiced' bucket or 'Completed & Ready to Invoice' bucket; its count query currently uses status='completed' — needs to exclude jobs that already have an invoice
    src/app/(app)/invoices/actions.ts — createInvoiceFromJobAction signature (orgId, jobId) → { invoiceId?, invoiceNo?, error? }
    CLAUDE.md — Supabase Realtime Broadcast section: dispatch board self:false pattern; router.refresh() MUST be called after createInvoiceFromJobAction in dispatch popup; Dispatch Board Auto-Refresh Pattern section
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — dispatch-popup.tsx wiring pattern (exact onClick implementations for both buttons), job detail page invoice button pattern (INV-10, INV-11)
    .planning/phases/07-invoicing-and-payments/07-UI-SPEC.md — button copy: 'Create Invoice' (primary, job action bar), 'Record Deposit' (secondary, dispatch popup), 'Close & Invoice' label stays unchanged (dispatch popup)
    .planning/phases/07-invoicing-and-payments/07-CONTEXT.md — INV-10 (Create Invoice on job bar), INV-11 (invoice link in job status row), INV-13 (Record Deposit entry points)
  </read_first>
  <action>
    In src/app/(app)/dispatch/popup/dispatch-popup.tsx:
    1. Import createInvoiceFromJobAction from '@/app/(app)/invoices/actions'. Import useRouter from 'next/navigation' if not already imported. Import toast from 'sonner' if not already imported.
    2. Find the 'Deposits' ActionButton (~line 416) which is currently disabled. Wire its onClick: onClick={() => router.push('/payments/new?jobId=' + localJob.id + '&type=deposit')}. Remove disabled prop (keep disabled only when isClosed || isCancelled).
    3. Find the 'Close & Invoice' ActionButton (~line 418). It already has: disabled={isClosed || isCancelled || localJob.status !== 'completed'}. Add onClick handler: async onClick that calls createInvoiceFromJobAction(orgId, localJob.id). On error: toast.error(result.error). On success: toast.success('Invoice #INV-' + result.invoiceNo + ' created.'); router.push('/invoices/' + result.invoiceId); router.refresh(). The router.refresh() call is CRITICAL per CLAUDE.md dispatch board pattern — self:false means this tab never receives its own broadcast events, so router.refresh() is the only way the board updates after Close & Invoice.

    In src/app/(app)/jobs/[id]/page.tsx:
    1. Import createInvoiceFromJobAction from '@/app/(app)/invoices/actions'. Import useRouter (if client component) or use server-side redirect after action.
    2. Query invoices for this job: fetch invoices WHERE jobId = job.id in the Server Component data fetch. Pass invoice (or null) as a prop.
    3. In the action bar: render 'Create Invoice' button (primary) when job.status === 'completed' and !invoice. The button calls createInvoiceFromJobAction(orgId, job.id) — since this is likely a Client Component action trigger, use a client-side button with the server action call, navigate to /invoices/[invoiceId] on success, show error toast on failure.
    4. INV-11 — invoice cross-link in status area: when invoice exists, render a link in the job's current status display area: '#INV-{invoiceNo} (created {invoiceDate})' as a clickable link to /invoices/[invoiceId]. This satisfies the requirement that after invoice creation the job status row shows the invoice reference.

    In src/app/(app)/jobs/jobs-sidebar.tsx:
    1. Find the 'To Be Invoiced' or 'Completed & Ready to Invoice' sidebar bucket. Its count query currently is likely `count(jobs) WHERE status = 'completed'`.
    2. Update the count query to also exclude jobs that already have an invoice: add a NOT EXISTS (SELECT 1 FROM invoices WHERE invoices.job_id = jobs.id AND invoices.tenant_id = jobs.tenant_id AND invoices.status != 'void') condition. This prevents completed jobs from staying in the 'To Be Invoiced' bucket after an invoice is created. Import invoices from schema if not already imported.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - dispatch-popup.tsx 'Deposits' ActionButton onClick navigates to /payments/new?jobId=X&type=deposit
    - dispatch-popup.tsx 'Close & Invoice' ActionButton onClick calls createInvoiceFromJobAction and calls router.refresh() after navigation
    - jobs/[id]/page.tsx shows 'Create Invoice' button when job.status === 'completed'
    - jobs/[id]/page.tsx shows invoice link '#INV-{N} (created date)' in status area when invoice exists
    - jobs-sidebar.tsx 'To Be Invoiced' bucket excludes jobs with existing non-void invoices
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PDF route | Auth check required before rendering invoice PDF — no unauthenticated PDF download |
| Dispatch popup → createInvoiceFromJobAction | orgId from Clerk auth on server; jobId from URL params; server validates job ownership via withTenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information Disclosure | /api/invoices/[id]/pdf | mitigate | auth() check at route entry — redirect 401 if no orgId; getInvoiceForPdf uses withTenant (RLS active) |
| T-07-16 | Tampering | dispatch popup createInvoiceFromJobAction | mitigate | Server action validates job.status is 'completed' or 'partially_completed' before proceeding; org scoping via withTenant |
| T-07-17 | Information Disclosure | PDF work order signature image | mitigate | Signature image URL comes from job_signatures which is RLS-protected; URL is a presigned Supabase Storage URL with time-limited access |
</threat_model>

<verification>
pnpm build exits 0. /api/invoices/[id]/pdf route exists with export const runtime = 'nodejs'. dispatch-popup.tsx 'Close & Invoice' has onClick + router.refresh(). Job detail page has conditional 'Create Invoice' button and invoice cross-link. Jobs sidebar 'To Be Invoiced' excludes invoiced jobs.
</verification>

<success_criteria>
1. pnpm build exits 0
2. src/app/api/invoices/[id]/pdf/route.ts exists with export const runtime = 'nodejs' as first line
3. src/components/invoices/invoice-pdf.tsx exports InvoicePdfDocument with work-order conditional section
4. dispatch-popup.tsx 'Close & Invoice' calls createInvoiceFromJobAction + router.push + router.refresh()
5. dispatch-popup.tsx 'Deposits' navigates to /payments/new?jobId=X&type=deposit
6. jobs/[id]/page.tsx shows 'Create Invoice' button for completed jobs (INV-10)
7. jobs/[id]/page.tsx shows '#INV-{N}' link in status row after invoice created (INV-11)
8. jobs-sidebar.tsx 'To Be Invoiced' bucket uses NOT EXISTS invoice query
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/app/api/invoices/[id]/pdf/route.ts` — PDF API route (Node.js runtime, mirrors estimates PDF route)
- `src/components/invoices/invoice-pdf.tsx` — exports `InvoicePdfDocument({ data: InvoicePdfData; workOrder?: boolean })`

**Modified files:**
- `src/app/(app)/dispatch/popup/dispatch-popup.tsx` — 'Close & Invoice' wired to createInvoiceFromJobAction + router.refresh(); 'Deposits' wired to /payments/new?type=deposit
- `src/app/(app)/jobs/[id]/page.tsx` — 'Create Invoice' button (INV-10); invoice cross-link in status row (INV-11)
- `src/app/(app)/jobs/jobs-sidebar.tsx` — 'To Be Invoiced' bucket query excludes jobs with non-void invoices

**Key symbols exported:**
- `InvoicePdfDocument({ data, workOrder? })` — @react-pdf/renderer Document component

<output>
Create `.planning/phases/07-invoicing-and-payments/07-05-SUMMARY.md` when done
</output>
