---
phase: 07-invoicing-and-payments
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/invoices/invoice-number.test.ts
  - tests/invoices/create-invoice.test.ts
  - tests/invoices/ar-aging.test.ts
  - tests/invoices/list-invoices.test.ts
  - tests/invoices/payment-allocation.test.ts
  - tests/invoices/record-payment.test.ts
  - tests/invoices/stripe-webhook.test.ts
  - tests/invoices/deposits.test.ts
  - tests/invoices/invoice-totals.test.ts
  - src/db/schema.ts
  - src/lib/invoices/invoice-number.ts
  - src/lib/invoices/totals.ts
  - src/lib/invoices/status.ts
  - src/lib/invoices/ar-aging.ts
  - package.json
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
  - INV-06
  - INV-07
  - INV-13
  - SET-08

must_haves:
  truths:
    - "pnpm vitest run tests/invoices/ exits non-zero (all 9 RED)"
    - "src/db/schema.ts exports paymentMethods, invoices, invoiceLineItems, payments, paymentAllocations tables with RLS + enableRLS()"
    - "src/lib/invoices/invoice-number.ts exports nextInvoiceNo and nextPaymentNo"
    - "src/lib/invoices/totals.ts exports computeInvoiceTotals with InvoiceTotals type"
    - "src/lib/invoices/status.ts exports invoiceStatusBadgeVariant and invoiceStatusLabel"
    - "src/lib/invoices/ar-aging.ts exports computeArAging returning ArAgingResult"
    - "pnpm db:push completes without error"
  artifacts:
    - path: "tests/invoices/invoice-number.test.ts"
      provides: "RED test: nextInvoiceNo sequential + tenant-scoped (INV-01)"
    - path: "tests/invoices/create-invoice.test.ts"
      provides: "RED test: createInvoiceFromJobAction copies line items + transitions job (INV-01)"
    - path: "tests/invoices/ar-aging.test.ts"
      provides: "RED test: AR aging bucket SQL aggregation (INV-02)"
    - path: "tests/invoices/list-invoices.test.ts"
      provides: "RED test: invoice list balance-computed status filter (INV-03)"
    - path: "tests/invoices/payment-allocation.test.ts"
      provides: "RED test: over-application guard rejects totalApplied > paymentAmount (INV-04)"
    - path: "tests/invoices/record-payment.test.ts"
      provides: "RED test: payment row + allocations + audit trail (INV-05)"
    - path: "tests/invoices/stripe-webhook.test.ts"
      provides: "RED test: Stripe webhook deduplication by stripe_event_id (INV-06)"
    - path: "tests/invoices/deposits.test.ts"
      provides: "RED test: deposit auto-allocation on createInvoiceFromJobAction (INV-13)"
    - path: "tests/invoices/invoice-totals.test.ts"
      provides: "RED test: balance is computed (never stored); recompute matches allocations (D-05)"
    - path: "src/db/schema.ts"
      provides: "5 new tables: paymentMethods, invoices, invoiceLineItems, payments, paymentAllocations"
    - path: "src/lib/invoices/invoice-number.ts"
      provides: "nextInvoiceNo(tx, tenantId): Promise<number>; nextPaymentNo(tx, tenantId): Promise<number>"
    - path: "src/lib/invoices/totals.ts"
      provides: "computeInvoiceTotals(items): InvoiceTotals"
    - path: "src/lib/invoices/status.ts"
      provides: "invoiceStatusBadgeVariant(balance, total, dueDate); invoiceStatusLabel(balance, total, dueDate)"
    - path: "src/lib/invoices/ar-aging.ts"
      provides: "computeArAging(orgId): Promise<ArAgingResult>"
  key_links:
    - from: "tests/invoices/*.test.ts"
      to: "src/lib/invoices/*.ts and src/app/(app)/invoices/actions.ts"
      via: "module import (RED — files do not exist yet)"
      pattern: "from '@/lib/invoices/"
    - from: "src/db/schema.ts"
      to: "payments.stripeEventId"
      via: "UNIQUE constraint — D-06 idempotency key"
      pattern: "stripeEventId.*unique"
    - from: "src/db/schema.ts"
      to: "payment_allocations"
      via: "D-03 join table — UNIQUE(tenant_id, payment_id, invoice_id)"
      pattern: "paymentAllocations"
---

<objective>
Wave 0: RED test scaffold (9 files), schema additions (5 new tables), helper library creation, pnpm add stripe square, and [BLOCKING] pnpm db:push.

Purpose: Establish the Nyquist test floor (9 RED files) and the full database schema before any implementation, so every subsequent wave drives tests to GREEN without planning gaps. The schema push is blocking — no downstream plan can verify against a live database until this completes.

Output: 9 RED test files in tests/invoices/, 5 new tables in schema.ts (paymentMethods, invoices, invoiceLineItems, payments, paymentAllocations with RLS on all), helper libs (nextInvoiceNo, nextPaymentNo, computeInvoiceTotals, invoiceStatusBadgeVariant, computeArAging), and pnpm db:push applied. References D-01, D-02, D-03, D-04, D-05, D-06 (schema enforces all payment ledger decisions).
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-invoicing-and-payments/07-CONTEXT.md
@.planning/phases/07-invoicing-and-payments/07-RESEARCH.md
@.planning/phases/07-invoicing-and-payments/07-PATTERNS.md
@.planning/phases/07-invoicing-and-payments/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test scaffold — 9 failing test files for INV-01 through INV-06, INV-13, D-05</name>
  <files>
    tests/invoices/invoice-number.test.ts
    tests/invoices/create-invoice.test.ts
    tests/invoices/ar-aging.test.ts
    tests/invoices/list-invoices.test.ts
    tests/invoices/payment-allocation.test.ts
    tests/invoices/record-payment.test.ts
    tests/invoices/stripe-webhook.test.ts
    tests/invoices/deposits.test.ts
    tests/invoices/invoice-totals.test.ts
  </files>
  <read_first>
    tests/estimates/estimate-number.test.ts — exact mock pattern and file structure to replicate
    tests/estimates/totals.test.ts — exact test structure for totals tests
    tests/estimates/convert.test.ts — job creation round-trip mock pattern for create-invoice test
    tests/estimates/dashboard.test.ts — countBy pattern for list-invoices test
    src/app/(tech)/lib/dexie.ts — CachedInvoice interface (listInvoicesAction must satisfy this type contract)
    src/app/(tech)/tech/invoices/actions.ts — PWA wrapper signatures: createInvoiceFromJobAction(orgId, jobId), processSquarePaymentAction — canonical actions must match
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — test file header pattern, mock factory, and per-file requirement mapping table
    .planning/phases/07-invoicing-and-payments/07-VALIDATION.md — Wave 0 test contracts and exact behaviors for all 9 files
    .planning/phases/07-invoicing-and-payments/07-RESEARCH.md — Pattern 5 (over-application guard math), common pitfalls section
  </read_first>
  <behavior>
    - invoice-number.test.ts: import nextInvoiceNo from '@/lib/invoices/invoice-number' (does not exist — RED). Test 1: given tenant has invoices with max invoiceNo = 1042, nextInvoiceNo returns 1043. Test 2: empty tenant returns 1001 (seed base 1000 + 1). Test 3: nextPaymentNo same behavior with payments table.
    - invoice-totals.test.ts: import computeInvoiceTotals from '@/lib/invoices/totals' (does not exist — RED). Test 1: 3 items at 19.99 each = total '59.97' (cent-exact, not floating-point). Test 2: discount line item (negative rate) reduces invoiceTotal. Test 3: tax items correctly applied to taxable items only.
    - create-invoice.test.ts: import createInvoiceFromJobAction from '@/app/(app)/invoices/actions' (does not exist — RED). Test 1: calling with a completed job copies job_line_items rows to invoice_line_items, inserts invoices row, transitions job status to 'invoiced', writes 1 customerEvents row with kind='invoice'. Test 2: throws/errors when job status is not 'completed' or 'partially_completed'.
    - ar-aging.test.ts: import computeArAging from '@/lib/invoices/ar-aging' (does not exist — RED). Test 1: invoice $500 overdue 45 days appears in bucket_60 ($31–60), not bucket_30 or grand_unpaid from other buckets. Test 2: paid invoice (balance=0) does NOT appear in any bucket. Test 3: returns all zero values when no unpaid invoices exist.
    - list-invoices.test.ts: import listInvoicesAction from '@/app/(app)/invoices/actions' (does not exist — RED). Test 1: balance subquery: invoice.total=200, allocations sum=50 → balance='150.00'. Test 2: balance=0 → status label 'Paid in Full'. Test 3: balance>0 and dueDate in past → status label 'Past Due'.
    - payment-allocation.test.ts: import recordPaymentAction from '@/app/(app)/payments/actions' (does not exist — RED). Test 1: allocations totaling $600 against payment amount $500 returns error "Total to Be Applied can't exceed the Amount of Payment." Test 2: allocations totaling $500 against payment amount $500 succeeds (boundary). Test 3: allocations totaling $499 against $500 succeeds.
    - record-payment.test.ts: import recordPaymentAction from '@/app/(app)/payments/actions' (does not exist — RED). Test 1: successful call inserts 1 payments row and N payment_allocations rows matching input. Test 2: enteredAt and enteredByUserId are set from auth(). Test 3: paymentNo is sequential per tenant.
    - stripe-webhook.test.ts: import handleStripeWebhook from '@/lib/stripe-webhook' (does not exist — RED). Test 1: duplicate stripe_event_id (unique violation code '23505') returns Response with status 200 (idempotent no-op). Test 2: invalid signature returns 400. Test 3: valid event with unknown type returns 200 'Ignored'.
    - deposits.test.ts: import createInvoiceFromJobAction from '@/app/(app)/invoices/actions' (does not exist — RED). Test 1: job has 1 unallocated deposit payment ($100), createInvoiceFromJobAction auto-creates 1 payment_allocations row with amountApplied='100.00'. Test 2: deposit already allocated (has payment_allocations row) is NOT double-allocated. Test 3: invoice balance after deposit = invoiceTotal - depositAmount.
  </behavior>
  <action>
    Create the tests/invoices/ directory. Write all 9 test files importing from modules that do not yet exist so the suite is RED. Use the file header mock pattern from 07-PATTERNS.md tests section: vi.mock('@clerk/nextjs/server', () => ({ auth: vi.fn(async () => ({ orgId: 'org_inv_test', userId: 'user_1' })) })); vi.mock('@/db/with-tenant', () => ({ withTenant: vi.fn(async (_id, fn) => fn(buildMockTx())) })). Build per-file mock transactions using vi.fn() chains that return fixture data matching the behavior contracts. For stripe-webhook.test.ts, mock the stripe module and simulate the constructEvent + unique-violation path. Do NOT use watch mode flags. Import paths must match exactly what Plans 07-02 and 07-03 will create.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm vitest run tests/invoices/ 2>&1 | tail -30</automated>
  </verify>
  <done>pnpm vitest run tests/invoices/ exits non-zero with FAIL showing all 9 test files failing due to import errors (Cannot find module). No passing tests in the invoices suite.</done>
</task>

<task type="auto">
  <name>Task 2: Schema + helpers — 5 new tables + helper libs + pnpm add stripe square</name>
  <files>
    src/db/schema.ts
    src/lib/invoices/invoice-number.ts
    src/lib/invoices/totals.ts
    src/lib/invoices/status.ts
    src/lib/invoices/ar-aging.ts
  </files>
  <read_first>
    src/db/schema.ts — full file: existing table patterns (jobLineItems, jobs, customers, customerEvents, jobTemplates), eventKind enum (confirms 'invoice' and 'payment' already exist), jobStatus enum (confirms 'invoiced' already exists), existing imports and RLS policy pattern, enableRLS() calls
    src/lib/estimates/estimate-number.ts — exact 17-line pattern to replicate for nextInvoiceNo and nextPaymentNo
    src/lib/estimates/totals.ts — computeEstimateTotals to mirror as computeInvoiceTotals (remove grossProfit/grossProfitPct, keep groupSubtotals)
    src/lib/estimates/status.ts — estimateStatusBadgeVariant pattern to replicate as invoiceStatusBadgeVariant (different signature: takes balance/total/dueDate not a status string)
    .planning/phases/07-invoicing-and-payments/07-RESEARCH.md — Schema Design section (full column specs for all 5 tables), Pattern 3 (AR aging SQL), Pattern 4 (balance computation)
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — ar-aging.ts implementation, invoice-number.ts implementation, status.ts implementation, totals.ts delta from estimate version
    .planning/phases/07-invoicing-and-payments/07-CONTEXT.md — D-01 through D-06 (schema decisions)
  </read_first>
  <action>
    Run: pnpm add stripe square

    In src/db/schema.ts, after the existing tables, add 5 new tables in this order:

    1. paymentMethods table: columns id (text PK), tenantId (text NOT NULL), name (text NOT NULL), isSystem (boolean default false), isActive (boolean default true), sortOrder (integer default 0), createdAt, updatedAt. UNIQUE(tenantId, id). pgPolicy using current_setting('app.current_tenant_id', true). enableRLS(). Per D-01 / SET-08.

    2. invoices table: id (text PK), tenantId (text NOT NULL), invoiceNo (integer NOT NULL), jobId (text NOT NULL), customerId (text NOT NULL), contactId (text nullable), serviceLocationId (text nullable), invoiceDate (date NOT NULL default CURRENT_DATE), dueDate (date nullable), paymentTermsDays (integer default 30), notes (text nullable), internalNotes (text nullable), paymentLinkUrl (text nullable), sentBy (text nullable), sentOn (timestamp nullable), emailOpenedAt (timestamp nullable), total (numeric NOT NULL), createdBy (text nullable), createdAt, updatedAt. UNIQUE(tenantId, invoiceNo). UNIQUE(tenantId, id). Composite FK: (tenantId, jobId) -> (jobs.tenantId, jobs.id) onDelete restrict. Composite FK: (tenantId, customerId) -> (customers.tenantId, customers.id). pgPolicy. enableRLS(). Per D-05 (total is a snapshot, balance is NOT stored).

    3. invoiceLineItems table: mirrors jobLineItems exactly, replacing jobId with invoiceId (text NOT NULL). UNIQUE(tenantId, id). Composite FK: (tenantId, invoiceId) -> (invoices.tenantId, invoices.id) onDelete cascade. pgPolicy. enableRLS(). Columns: id, tenantId, invoiceId, type (lineItemType enum), refId (nullable), title, description, qty (numeric), rate (numeric), cost (numeric), taxItemId (nullable), sortOrder, groupId (text nullable), createdAt, updatedAt.

    4. payments table: id (text PK), tenantId (text NOT NULL), paymentNo (integer NOT NULL), jobId (text nullable — for deposits per D-04), customerId (text NOT NULL), method (text NOT NULL), checkRefNo (text nullable), receivedBy (text nullable), receivedOn (date nullable), memo (text nullable), amount (numeric NOT NULL), stripeEventId (text nullable), last4 (text nullable), expiry (text nullable), transactionToken (text nullable), authCode (text nullable), billingAddress (text nullable), squarePaymentId (text nullable), enteredAt (timestamp default now()), enteredByUserId (text nullable), createdAt, updatedAt. UNIQUE(tenantId, paymentNo). UNIQUE(tenantId, id). unique('stripe_event_id').unique() on stripeEventId alone (global UNIQUE, NULL excluded by Postgres — D-06). pgPolicy. enableRLS(). Per D-01, D-02, D-06.

    5. paymentAllocations table: id (text PK), tenantId (text NOT NULL), paymentId (text NOT NULL), invoiceId (text NOT NULL), amountApplied (numeric NOT NULL), createdAt. UNIQUE(tenantId, paymentId, invoiceId). UNIQUE(tenantId, id). Composite FKs: (tenantId, paymentId) -> (payments.tenantId, payments.id) onDelete cascade; (tenantId, invoiceId) -> (invoices.tenantId, invoices.id) onDelete cascade. pgPolicy. enableRLS(). Per D-03.

    Create src/lib/invoices/invoice-number.ts: exact copy of src/lib/estimates/estimate-number.ts, renamed to export nextInvoiceNo(tx, tenantId) using invoices table and invoices.invoiceNo column; seed base 1000 (returns 1001 for empty tenant). In the same file add nextPaymentNo(tx, tenantId) using payments table and payments.paymentNo, same seed.

    Create src/lib/invoices/totals.ts: mirror src/lib/estimates/totals.ts. Export InvoiceLineItemInput (same shape as EstimateLineItemInput with groupId nullable). Export InvoiceTotals: { invoiceTotal: string; taxTotal: string; subtotal: string; discountTotal: string; groupSubtotals: Record<string, string> }. Export computeInvoiceTotals(items: InvoiceLineItemInput[]): InvoiceTotals. Remove grossProfit/grossProfitPct. Keep toCents/fromCents helpers. Keep groupSubtotals accumulation.

    Create src/lib/invoices/status.ts: export invoiceStatusBadgeVariant(balance: number, total: number, dueDate: Date | null): 'outline' | 'secondary' | 'default' | 'destructive'. Logic: balance <= 0 → 'default' (Paid in Full); dueDate && new Date() > dueDate && balance > 0 → 'destructive' (Past Due); balance < total → 'secondary' (Partially Paid); else 'outline' (Unpaid). Export invoiceStatusLabel(balance, total, dueDate): string with the matching text labels per UI-SPEC.

    Create src/lib/invoices/ar-aging.ts: export ArAgingResult interface with fields grandUnpaid, grandPastDue, bucket30, bucket60, bucket90, bucket91plus (all string). Export computeArAging(orgId: string): Promise<ArAgingResult> using withTenant + tx.execute(sql\`...\`) with the exact SQL from PATTERNS.md (the COALESCE FILTER WHERE subquery pattern). Cast rows result as { rows: Record<string, string>[] }. Per RESEARCH.md Pattern 3.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm build exits 0 (TypeScript compiles, all new schema exports and lib files resolve)
    - src/db/schema.ts exports paymentMethods, invoices, invoiceLineItems, payments, paymentAllocations
    - payments table has stripeEventId column with UNIQUE constraint
    - paymentAllocations has UNIQUE(tenantId, paymentId, invoiceId)
    - src/lib/invoices/ contains invoice-number.ts, totals.ts, status.ts, ar-aging.ts
    - pnpm add stripe square completed (package.json updated)
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] pnpm db:push — apply 5 new tables to Supabase</name>
  <files></files>
  <read_first>
    src/db/schema.ts — confirm Task 2 completed and the 5 new tables are present before pushing
  </read_first>
  <action>
    Run pnpm db:push to apply the new schema to Supabase. This creates paymentMethods, invoices, invoiceLineItems, payments, and paymentAllocations tables plus the global UNIQUE index on payments.stripe_event_id. All changes are additive (new tables, new indexes) — no destructive migrations. If drizzle-kit push shows any confirmation prompt, accept. This task MUST succeed before any downstream plan can verify against the live database. Do NOT use npm or npx — use pnpm db:push exactly.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm db:push 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm db:push exits 0. Output confirms payment_methods, invoices, invoice_line_items, payments, payment_allocations tables were created in Supabase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → module | Tests import modules that do not exist — expected RED state |
| Schema push → Supabase | Schema changes are additive: new tables, new indexes — backward-compatible |
| pnpm add → registry | stripe and square packages were slopchecked in RESEARCH.md — [OK] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | invoices, payments, paymentAllocations tables | mitigate | RLS policy using current_setting('app.current_tenant_id', true) + enableRLS() on all 5 new tables — enforced in schema definition |
| T-07-02 | Spoofing | payments.stripeEventId | mitigate | UNIQUE constraint on stripe_event_id catches duplicates; webhook handler verifies Stripe HMAC before writing (Wave 1) |
| T-07-SC | Tampering | pnpm add stripe square | mitigate | Package legitimacy audit in RESEARCH.md confirms stripe [OK] and square [OK]; versions 22.3.0 and 44.2.0 verified |
</threat_model>

<verification>
Full wave verification: pnpm vitest run tests/invoices/ must exit non-zero (all 9 RED). pnpm build must exit 0. pnpm db:push must exit 0. After this plan, exactly 9 test files exist in tests/invoices/ all failing, schema is live in Supabase with 5 new tables, all helper libs export their signatures.
</verification>

<success_criteria>
1. pnpm vitest run tests/invoices/ exits non-zero — 9 test files, all FAIL (correct RED state)
2. pnpm build exits 0 — TypeScript compiles with all new schema exports and lib files
3. pnpm db:push exits 0 — schema applied to Supabase including all 5 new tables
4. stripe and square in package.json dependencies
5. src/lib/invoices/ directory contains invoice-number.ts, totals.ts, status.ts, ar-aging.ts
</success_criteria>

## Artifacts this phase produces

**New files:**
- `tests/invoices/invoice-number.test.ts` — RED test: nextInvoiceNo sequential + tenant-scoped (INV-01)
- `tests/invoices/invoice-totals.test.ts` — RED test: computeInvoiceTotals cent-exact (D-05)
- `tests/invoices/create-invoice.test.ts` — RED test: createInvoiceFromJobAction copies line items + transitions job (INV-01)
- `tests/invoices/ar-aging.test.ts` — RED test: AR aging bucket SQL aggregation (INV-02)
- `tests/invoices/list-invoices.test.ts` — RED test: invoice list balance-computed status (INV-03)
- `tests/invoices/payment-allocation.test.ts` — RED test: over-application guard (INV-04)
- `tests/invoices/record-payment.test.ts` — RED test: payment row + allocations + audit (INV-05)
- `tests/invoices/stripe-webhook.test.ts` — RED test: Stripe webhook dedup (INV-06)
- `tests/invoices/deposits.test.ts` — RED test: deposit auto-allocation (INV-13)
- `src/lib/invoices/invoice-number.ts` — exports `nextInvoiceNo(tx, tenantId): Promise<number>`, `nextPaymentNo(tx, tenantId): Promise<number>`
- `src/lib/invoices/totals.ts` — exports `computeInvoiceTotals(items): InvoiceTotals`, `InvoiceTotals`, `InvoiceLineItemInput`
- `src/lib/invoices/status.ts` — exports `invoiceStatusBadgeVariant(balance, total, dueDate)`, `invoiceStatusLabel(balance, total, dueDate)`
- `src/lib/invoices/ar-aging.ts` — exports `computeArAging(orgId): Promise<ArAgingResult>`, `ArAgingResult`

**Modified files:**
- `src/db/schema.ts` — adds `paymentMethods`, `invoices`, `invoiceLineItems`, `payments`, `paymentAllocations` tables with RLS
- `package.json` — adds `stripe` ^22.3.0 and `square` ^44.2.0

**New DB tables (Supabase):**
- `payment_methods` — SET-08 configurable list, is_system boolean
- `invoices` — main invoice record with total snapshot (D-05)
- `invoice_line_items` — line item snapshot at invoice creation
- `payments` — canonical ledger for all methods (D-01); stripe_event_id UNIQUE (D-06)
- `payment_allocations` — multi-invoice allocation join table (D-03)

<output>
Create `.planning/phases/07-invoicing-and-payments/07-01-SUMMARY.md` when done
</output>
