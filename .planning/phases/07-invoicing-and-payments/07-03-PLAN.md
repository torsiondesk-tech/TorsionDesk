---
phase: 07-invoicing-and-payments
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - src/app/(app)/invoices/page.tsx
  - src/app/(app)/invoices/invoices-sidebar.tsx
  - src/app/(app)/invoices/invoices-table.tsx
  - src/components/nav/nav-links.tsx
autonomous: true
requirements:
  - INV-02
  - INV-03

must_haves:
  truths:
    - "Invoices dashboard renders at /invoices with AR aging sidebar and Unpaid/Paid/All views"
    - "AR aging sidebar shows Grand Total Unpaid, Grand Total Past Due, and 4 color-coded aging buckets"
    - "Invoice list is a TanStack Table with search, sort, pagination, and balance-derived status badges"
    - "Invoices nav item is enabled and navigates to /invoices"
    - "Empty state: heading 'No invoices yet' with correct body copy from UI-SPEC"
  artifacts:
    - path: "src/app/(app)/invoices/page.tsx"
      provides: "Invoices dashboard Server Component — fetches countInvoicesByStatus + computeArAging + renders InvoicesSidebar + Suspense(InvoicesTable)"
    - path: "src/app/(app)/invoices/invoices-sidebar.tsx"
      provides: "AR aging panel (Grand Total Unpaid/Past Due + 4 color-coded bucket tiles) + status-folder nav (Unpaid/Partially Paid/Paid in Full/Past Due/All)"
    - path: "src/app/(app)/invoices/invoices-table.tsx"
      provides: "TanStack Table client component: InvoiceRow columns (Invoice#, Customer, Job#, Invoice Date, Due Date, Total, Balance, Status badge), search/sort/pagination, empty state"
  key_links:
    - from: "src/app/(app)/invoices/page.tsx"
      to: "src/lib/invoices/ar-aging.ts computeArAging"
      via: "Server Component fetches AR aging data and passes as prop to InvoicesSidebar"
      pattern: "computeArAging"
    - from: "src/app/(app)/invoices/invoices-sidebar.tsx"
      to: "invoiceStatusBadgeVariant"
      via: "AR aging bucket color scale from UI-SPEC (yellow/orange/red gradient)"
      pattern: "bucket30|bucket60|bucket90|bucket91plus"
    - from: "src/app/(app)/invoices/invoices-table.tsx"
      to: "invoiceStatusBadgeVariant"
      via: "Status badge computed from balance + total + dueDate (D-05)"
      pattern: "invoiceStatusBadgeVariant"
---

<objective>
Wave 2: Invoices dashboard — AR aging sidebar + TanStack invoice table + enable Invoices nav item.

Purpose: The owner's most-used screen. The AR aging sidebar must match Service Fusion's layout exactly (Grand Total Unpaid + Grand Total Past Due prominently at top, four color-coded aging buckets below). The table mirrors the estimates table pattern with invoice-specific columns and balance-derived status badges.

Output: /invoices page with sidebar + table, Invoices nav activated. References INV-02 (AR aging sidebar), INV-03 (Unpaid/Paid/All views, search, sort, pagination).
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-invoicing-and-payments/07-CONTEXT.md
@.planning/phases/07-invoicing-and-payments/07-UI-SPEC.md
@.planning/phases/07-invoicing-and-payments/07-PATTERNS.md
@.planning/phases/07-invoicing-and-payments/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: InvoicesSidebar — AR aging panel + status folder nav</name>
  <files>
    src/app/(app)/invoices/invoices-sidebar.tsx
  </files>
  <read_first>
    src/app/(app)/estimates/estimates-sidebar.tsx — complete file: useQueryStates hook usage, STATUS_FOLDERS array, sidebar link rendering pattern, count badge display
    src/lib/invoices/ar-aging.ts — ArAgingResult type (created in Plan 01): { grandUnpaid, grandPastDue, bucket30, bucket60, bucket90, bucket91plus: string }
    src/lib/invoices/status.ts — invoiceStatusBadgeVariant (used in table, not sidebar directly)
    .planning/phases/07-invoicing-and-payments/07-UI-SPEC.md — AR aging color scale (yellow/orange/red gradient per bucket), Grand Total sizes (text-3xl semibold), bucket tile layout, spacing scale
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — invoices-sidebar.tsx pattern section (STATUS_FOLDERS definition, AR aging panel HTML structure, useQueryStates pattern to copy)
    .planning/phases/07-invoicing-and-payments/07-UI-SPEC.md — copywriting contract: sidebar meta labels (Total Unpaid, Total Past Due), bucket labels (1–30 days, 31–60 days, etc.)
  </read_first>
  <action>
    Create src/app/(app)/invoices/invoices-sidebar.tsx as a Client Component ('use client'). Mirror estimates-sidebar.tsx structure exactly but with invoice-specific content.

    Import: useQueryStates from 'nuqs', lucide icons (FileText, Clock, AlertCircle, CheckCircle, AlertTriangle, ChevronRight), cn from '@/lib/utils', ArAgingResult from '@/lib/invoices/ar-aging'. Props: { statusCounts: { unpaid: number; partial: number; paid: number; pastDue: number }; arAging: ArAgingResult }.

    STATUS_FOLDERS constant: [{ key: 'all', label: 'All', icon: FileText }, { key: 'unpaid', label: 'Unpaid', icon: Clock }, { key: 'partial', label: 'Partially Paid', icon: AlertCircle }, { key: 'paid', label: 'Paid in Full', icon: CheckCircle }, { key: 'past_due', label: 'Past Due', icon: AlertTriangle }].

    useQueryStates(['status']) — same pattern as estimates-sidebar.tsx lines 41–47.

    Render two sections in order:
    1. AR aging panel: div with border-b mb-2 px-2 py-3 space-y-2. Top: two rows — Grand Total Unpaid (text-3xl font-semibold text-foreground tabular-nums) and Grand Total Past Due (text-3xl font-semibold text-destructive tabular-nums). Format values as '$' + parseFloat(arAging.grandUnpaid).toFixed(2). Below those: 4 bucket tiles. Each tile: div with rounded-lg border p-2 flex items-center justify-between. The color classes per UI-SPEC: bucket30 = bg-yellow-50 text-yellow-800 border-yellow-200 dark:bg-yellow-950/40 dark:text-yellow-300; bucket60 = bg-orange-50 text-orange-800 border-orange-200 dark:bg-orange-950/40 dark:text-orange-300; bucket90 = bg-orange-100 text-orange-900 border-orange-300 dark:bg-orange-900/40 dark:text-orange-200; bucket91plus = bg-red-50 text-red-800 border-red-200 dark:bg-red-950/40 dark:text-red-300. If bucket value is '0.00' or 0, render bg-muted text-muted-foreground instead (no severity color). Each tile has a small leading swatch dot (size-2 rounded-full in the bucket's solid color: yellow-400, orange-400, orange-600, red-600) + label text ('1–30 days', '31–60 days', '61–90 days', '91+ days') + dollar amount right-aligned tabular-nums.

    2. Status folders section: map STATUS_FOLDERS the same way estimates-sidebar.tsx maps its folders. Count from statusCounts prop. Active state from useQueryStates. On click: setStatus(folder.key). Apply bg-sidebar-accent for active.
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - src/app/(app)/invoices/invoices-sidebar.tsx renders AR aging panel with 4 color-coded bucket tiles
    - STATUS_FOLDERS has 5 entries (All, Unpaid, Partially Paid, Paid in Full, Past Due)
    - Grand Total Unpaid and Grand Total Past Due render at text-3xl font-semibold
    - Zero-value buckets render bg-muted (no severity color)
  </done>
</task>

<task type="auto">
  <name>Task 2: InvoicesTable + InvoicesPage + enable Invoices nav</name>
  <files>
    src/app/(app)/invoices/invoices-table.tsx
    src/app/(app)/invoices/page.tsx
    src/components/nav/nav-links.tsx
  </files>
  <read_first>
    src/app/(app)/estimates/estimates-table.tsx — complete file: TanStack Table setup, ColumnDef array, search/sort/pagination client state, formatMoney helper, EstimateRow interface, row action menu, empty state rendering
    src/app/(app)/estimates/page.tsx — complete file: Server Component pattern (auth + redirect + data fetch + layout with aside + Suspense)
    src/components/nav/nav-links.tsx — full file: find the Invoices nav entry to enable (check phase stub if it exists; enable it by ensuring href is set)
    src/lib/invoices/status.ts — invoiceStatusBadgeVariant and invoiceStatusLabel
    src/app/(app)/invoices/actions.ts — listInvoicesAction, countInvoicesByStatus return types; InvoiceRow interface
    src/lib/invoices/ar-aging.ts — computeArAging return type ArAgingResult
    .planning/phases/07-invoicing-and-payments/07-UI-SPEC.md — copywriting (empty state copy, column labels), badge variants per status, typography (tabular-nums for amounts), spacing scale
    .planning/phases/07-invoicing-and-payments/07-PATTERNS.md — invoices-table.tsx InvoiceRow interface definition, column definitions delta from estimates-table, invoices/page.tsx structure
  </read_first>
  <action>
    Create src/app/(app)/invoices/invoices-table.tsx as a Client Component ('use client'). Mirror estimates-table.tsx completely. Replace EstimateRow with InvoiceRow interface: { id, tenantId, invoiceNo: number, customerId, customerName: string|null, jobId, invoiceDate: string|null, dueDate: string|null, total: string, balance: string, paymentLinkUrl: string|null, createdAt: string|null }.

    Column definitions: Invoice# (invoiceNo formatted as '#INV-{N}', clickable link to /invoices/[id]); Customer (customerName, link to /customers/[id]); Job# (jobId formatted as '#JOB-{N}' — fetch jobNo from the row if available, else show jobId truncated); Invoice Date (invoiceDate formatted MM/DD/YYYY); Due Date (dueDate — if past due and balance > 0, render in text-destructive); Total (total as '$X.XX', tabular-nums right-aligned); Balance (balance as '$X.XX', tabular-nums right-aligned; zero shown as '—' muted); Status (Badge using invoiceStatusBadgeVariant(parseFloat(balance), parseFloat(total), dueDate ? new Date(dueDate) : null) with label from invoiceStatusLabel).

    Row action menu: "View Invoice" (navigates to /invoices/[id]), "Receive Payment" (navigates to /payments/new?invoiceId=[id]&customerId=[customerId]), "Download PDF" (links to /api/invoices/[id]/pdf), "Copy Payment Link" (if paymentLinkUrl, copies to clipboard + toast 'Payment link copied.'), separator, "Delete Invoice" (calls deleteInvoiceAction, shows confirmation — Base UI Dialog with render prop per UI-SPEC).

    Search filters: free-text search on customerName and invoiceNo. Pagination: same pattern as estimates-table (10 per page). TanStack useReactTable with sorting on invoiceDate, dueDate, total, balance.

    Empty state: if no rows after filter, render centered empty-state. If unfiltered: heading 'No invoices yet', body 'Invoices you create from completed jobs will appear here. Close a job to generate its first invoice.' (exact UI-SPEC copy — no inline create button). If filtered: 'No invoices match this filter.' or 'No open invoices — everything\'s paid.' for unpaid tab.

    Create src/app/(app)/invoices/page.tsx as a Server Component. Mirror estimates/page.tsx structure. Import { auth } from '@clerk/nextjs/server', redirect from 'next/navigation', countInvoicesByStatus from invoices/actions, computeArAging from lib/invoices/ar-aging, InvoicesSidebar, InvoicesTable, Suspense. Parse searchParams.status for filter. Fetch countInvoicesByStatus(orgId) and computeArAging(orgId) in parallel via Promise.all. Render: div flex gap-6 animate-in fade-in-0 duration-300. Aside: hidden lg:block w-60 shrink-0 with InvoicesSidebar(statusCounts, arAging). Main: flex-1 min-w-0 space-y-6. Header: h1 'Invoices' text-3xl font-semibold. Below: Suspense fallback(skeleton) wrapping InvoicesTable (server-fetches rows inside Suspense or passes as prop). No 'New Invoice' button — invoices only originate from jobs.

    In src/components/nav/nav-links.tsx: find the Invoices entry. If it has phase: 7 or disabled state, activate it by setting href: '/invoices' and removing any disabled/phase condition. Use the same nav item pattern as Estimates (which was activated in Phase 6).
  </action>
  <verify>
    <automated>cd "C:/Users/marco/Desktop/HOME SERVICE CRM" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - /invoices page renders with InvoicesSidebar + InvoicesTable
    - InvoicesTable has columns: Invoice#, Customer, Job#, Invoice Date, Due Date, Total, Balance, Status
    - Invoices nav item is active and links to /invoices
    - Empty state copy matches UI-SPEC exactly
    - Row actions include Receive Payment link and PDF download
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → server action | Table row actions (delete, PDF) go through server actions validated with withTenant |
| Clipboard API | Copy payment link uses browser clipboard API — only fires on user gesture, no data leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Information Disclosure | /invoices page | mitigate | auth() redirect to /sign-in if no orgId; all data fetched inside withTenant (RLS active) |
| T-07-10 | Information Disclosure | AR aging SQL | mitigate | computeArAging uses withTenant which sets app.current_tenant_id GUC — RLS filters to current tenant only |
</threat_model>

<verification>
pnpm build exits 0. /invoices page exists at app/(app)/invoices/page.tsx with Server Component pattern. AR aging panel component renders all 4 bucket tiles. Invoices nav item active.
</verification>

<success_criteria>
1. pnpm build exits 0
2. src/app/(app)/invoices/page.tsx is a Server Component fetching AR aging + status counts
3. src/app/(app)/invoices/invoices-sidebar.tsx renders AR aging panel with UI-SPEC color scale
4. src/app/(app)/invoices/invoices-table.tsx is a TanStack Table with Invoice#/Customer/Balance/Status columns
5. Invoices nav item links to /invoices (no longer disabled/stubbed)
</success_criteria>

## Artifacts this phase produces

**New files:**
- `src/app/(app)/invoices/page.tsx` — Invoices dashboard Server Component
- `src/app/(app)/invoices/invoices-sidebar.tsx` — AR aging panel + status-folder nav (Client Component)
- `src/app/(app)/invoices/invoices-table.tsx` — TanStack Table invoice list (Client Component)

**Modified files:**
- `src/components/nav/nav-links.tsx` — Invoices nav item enabled with href '/invoices'

**Key symbols exported:**
- `InvoicesSidebar({ statusCounts, arAging }: { statusCounts: InvoiceStatusCounts; arAging: ArAgingResult })`
- `InvoicesTable({ rows, filter }: { rows: InvoiceRow[]; filter?: string })`
- `InvoiceRow` interface: `{ id, tenantId, invoiceNo, customerId, customerName, jobId, invoiceDate, dueDate, total, balance, paymentLinkUrl, createdAt }`

<output>
Create `.planning/phases/07-invoicing-and-payments/07-03-SUMMARY.md` when done
</output>
