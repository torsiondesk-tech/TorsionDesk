---
phase: 08-communications-and-notifications
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - src/app/(app)/communications/reminders.ts
  - src/app/api/cron/sms-reminders/route.ts
  - vercel.json
  - src/app/(app)/jobs/actions.ts
  - src/app/(app)/jobs/[id]/job-info-panel.tsx
autonomous: true
requirements: [COMM-07, COMM-08]

must_haves:
  truths:
    - "A dispatcher can set a reminder lead time (e.g. 24h before start) on a job; saving the job writes/updates a scheduled_sms row with the correct fire_at"
    - "The hourly cron dispatches every overdue, unsent, non-cancelled scheduled_sms row, sets sent_at on success and error_message on failure, and skips already-sent or cancelled rows"
    - "The cron endpoint rejects any request without the correct CRON_SECRET bearer token (401)"
    - "Cancelling or rescheduling a job cancels or recomputes its pending scheduled_sms so no reminder fires for a cancelled/moved appointment"
  artifacts:
    - path: "src/app/(app)/communications/reminders.ts"
      provides: "scheduleAppointmentReminder + cancelAppointmentReminders"
      exports: ["scheduleAppointmentReminder", "cancelAppointmentReminders"]
    - path: "src/app/api/cron/sms-reminders/route.ts"
      provides: "GET cron dispatcher with CRON_SECRET bearer auth"
      exports: ["GET"]
    - path: "vercel.json"
      provides: "hourly cron entry for /api/cron/sms-reminders"
      contains: "sms-reminders"
  key_links:
    - from: "src/app/api/cron/sms-reminders/route.ts"
      to: "scheduled_sms + Twilio"
      via: "query overdue rows + getTwilio().messages.create"
      pattern: "scheduledSms"
    - from: "src/app/(app)/jobs/actions.ts"
      to: "src/app/(app)/communications/reminders.ts"
      via: "scheduleAppointmentReminder / cancelAppointmentReminders on save/cancel"
      pattern: "scheduleAppointmentReminder|cancelAppointmentReminders"
    - from: "vercel.json"
      to: "/api/cron/sms-reminders"
      via: "crons entry"
      pattern: "0 \\* \\* \\* \\*"
---

<objective>
Build the scheduled appointment-reminder SMS path (COMM-07, D-05): a per-job reminder-lead-time control that writes a `scheduled_sms` row, an hourly Vercel Cron route that dispatches overdue rows through Twilio (auth-gated by CRON_SECRET), and cancellation/recompute of pending reminders when a job is cancelled or rescheduled. This is the async send path, separated from Wave 2's synchronous event wiring so the cron and scheduling logic do not block those bindings.

Purpose: Completes the SMS half of COMM-07 (the "appointment reminder SMS configurable per job" clause). The consent gate and Twilio client from earlier waves are reused; this plan adds only the schedule + dispatch + cancel lifecycle.
Output: Working per-job reminders + hourly cron; the reminders-cron test goes GREEN.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-communications-and-notifications/08-CONTEXT.md
@.planning/phases/08-communications-and-notifications/08-RESEARCH.md
@.planning/phases/08-communications-and-notifications/08-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates: `scheduleAppointmentReminder`, `cancelAppointmentReminders` (src/app/(app)/communications/reminders.ts), the cron route `GET` handler (src/app/api/cron/sms-reminders/route.ts), the vercel.json crons entry, and a `reminderLeadHours` field on createJobSchema + a reminder-lead-time control in the job info panel. See 08-01-PLAN.md for the full phase symbol list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reminder scheduler + cancellation/recompute lifecycle</name>
  <files>src/app/(app)/communications/reminders.ts, src/app/(app)/jobs/actions.ts</files>
  <read_first>
    - src/db/schema.ts (scheduledSms table added in Wave 0: jobId, contactId, phone, messageBody, fireAt, sentAt, cancelledAt, errorMessage; jobs table startDate/arrivalWindowStart line 713; contacts smsConsent line 153/bookingContact line 155; contact_phones table)
    - src/app/(app)/communications/recipients.ts (resolveSmsRecipient — reuse the consent gate to get the reminder phone; if no consented contact, do not schedule)
    - src/app/(app)/jobs/actions.ts (createJob action; updateJob line 712; createJobSchema fields arrivalWindowStart line 185, startDate line 183, status line 176; the cancel path where status becomes 'cancelled' — the transition/update flow)
    - src/db/with-tenant.ts (withTenant scoping)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§D-05; Pitfall 6 cancel/reschedule; §Date handling note — fire_at is a UTC instant timestamp, not a calendar date)
    - CLAUDE.md (§Date Handling — fire_at/arrivalWindow are timestamp instants; the local-vs-UTC-midnight rule does NOT apply to these instant columns)
  </read_first>
  <action>
    Create src/app/(app)/communications/reminders.ts (a plain async module, not 'use server') exporting:

    `scheduleAppointmentReminder(orgId, jobId, leadHours)` — inside withTenant: resolve the job's scheduled start instant (prefer arrivalWindowStart, else startDate at the arrival window) and the consented SMS recipient via resolveSmsRecipient. Compute `fireAt = startInstant - leadHours hours`. Upsert the pending scheduled_sms row for this job: if a pending (sentAt IS NULL, cancelledAt IS NULL) row already exists for the job, update its fireAt/phone/messageBody; else insert a new row with the reminder message body (Claude's discretion, e.g. "Reminder: your garage door service is scheduled for {date/time}. — {companyName}"). If leadHours is null/0 or no consented recipient exists, do NOT schedule (and cancel any existing pending row). fire_at is stored as a timestamp instant — no calendar-date extraction.

    `cancelAppointmentReminders(orgId, jobId)` — inside withTenant: set cancelledAt = now() on all pending (sentAt IS NULL, cancelledAt IS NULL) scheduled_sms rows for the job.

    Then wire the lifecycle in src/app/(app)/jobs/actions.ts:
    - In createJob and updateJob, after the job row commits, call `after(() => scheduleAppointmentReminder(orgId, jobId, reminderLeadHours).catch((e) => logger.error('schedule reminder', e)))` using the new reminderLeadHours field (added in Task 3). On updateJob a changed start/arrival window recomputes fireAt (upsert covers this — Pitfall 6 reschedule).
    - On the cancel path (status transition to 'cancelled', or updateJob setting status cancelled), call `after(() => cancelAppointmentReminders(orgId, jobId).catch(...))` so no reminder fires for a cancelled appointment (Pitfall 6).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && grep -c "scheduleAppointmentReminder\|cancelAppointmentReminders" src/app/(app)/jobs/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - reminders.ts exports both scheduleAppointmentReminder and cancelAppointmentReminders and contains no `'use server'`.
    - scheduleAppointmentReminder computes fireAt as (start instant − leadHours) and upserts the pending row rather than inserting duplicates.
    - `grep -c "scheduleAppointmentReminder\|cancelAppointmentReminders" src/app/(app)/jobs/actions.ts` returns at least 2 (schedule on save, cancel on cancel).
    - scheduleAppointmentReminder uses resolveSmsRecipient's consent gate and does not schedule for a non-consented contact.
    - `pnpm build` clean.
  </acceptance_criteria>
  <done>Reminder scheduling upserts a scheduled_sms row on save, recomputes on reschedule, and cancels on job cancel — all consent-gated and non-blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Hourly cron route (CRON_SECRET-gated) + vercel.json crons entry</name>
  <files>src/app/api/cron/sms-reminders/route.ts, vercel.json</files>
  <read_first>
    - src/app/api/webhooks/stripe/route.ts (route shell shape)
    - src/db/schema.ts (scheduledSms columns fireAt/sentAt/cancelledAt/errorMessage/phone/messageBody/tenantId)
    - src/db/with-tenant.ts and src/db/client.ts (privileged db for the unscoped distinct-tenant scan; withTenant per-tenant dispatch)
    - src/lib/comms/twilio.ts (getTwilio from Wave 1)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/app/api/cron/sms-reminders/route.ts route shell + auth; §vercel.json crons addition)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 5 cron auth + schedule; Pitfall 5 public trigger / plan cadence; Open Q1 hourly plan)
    - vercel.json (existing installCommand/buildCommand — the crons entry must MERGE, not replace)
  </read_first>
  <action>
    Create src/app/api/cron/sms-reminders/route.ts with `export const dynamic = 'force-dynamic'` and `export async function GET(req: Request): Promise<Response>`:
    - Auth gate: if `req.headers.get('authorization') !== \`Bearer ${process.env.CRON_SECRET}\`` return `new Response('Unauthorized', { status: 401 })` (Pitfall 5 — endpoint must not be publicly triggerable).
    - Query scheduled_sms rows WHERE `fireAt <= now() AND sentAt IS NULL AND cancelledAt IS NULL`. Because there is no session, scan distinct tenant_ids on the privileged connection, then process each tenant's due rows inside `withTenant(tenantId, ...)`.
    - For each due row: `getTwilio().messages.create({ body: messageBody, from: communication_settings.sms_phone_number, to: phone })`; on success set `sentAt = now()`; on failure set `errorMessage`. sent_at guards against re-send if the cron overlaps (idempotent).
    - Return `new Response('OK', { status: 200 })` with a small processed count.

    Update vercel.json: MERGE a `"crons": [{ "path": "/api/cron/sms-reminders", "schedule": "0 * * * *" }]` array alongside the existing `installCommand` and `buildCommand` keys (do not drop them). Note in the SUMMARY that hourly cadence requires a Vercel plan that supports hourly crons (Open Q1) — on Hobby this degrades to daily, which the `fire_at <= now()` query tolerates.

    Do NOT add rate limiting (cron is under /api, not /api/webhooks — but the CRON_SECRET bearer is the gate here; do not touch src/middleware.ts).
  </action>
  <verify>
    <automated>pnpm test -- comms/reminders-cron 2>&1 | tail -15 && grep -c "sms-reminders" vercel.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CRON_SECRET" src/app/api/cron/sms-reminders/route.ts` returns at least 1 and the 401 path is present.
    - `pnpm test -- comms/reminders-cron` passes: overdue row → sent_at set; cancelled_at / already-sent rows skipped; failure → error_message set.
    - vercel.json still contains `installCommand` and `buildCommand` AND now contains a `crons` array with path `/api/cron/sms-reminders` and schedule `0 * * * *` (valid JSON — `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` succeeds).
    - The cron query filters `sentAt IS NULL AND cancelledAt IS NULL AND fireAt <= now()`.
  </acceptance_criteria>
  <done>Hourly, CRON_SECRET-gated cron dispatches overdue reminders through Twilio; vercel.json has the merged crons entry; reminders-cron test green.</done>
</task>

<task type="auto">
  <name>Task 3: Reminder-lead-time control on the job form (dispatcher sets the window)</name>
  <files>src/app/(app)/jobs/actions.ts, src/app/(app)/jobs/[id]/job-info-panel.tsx</files>
  <read_first>
    - src/app/(app)/jobs/actions.ts (createJobSchema lines 145-208 — add reminderLeadHours field; arrivalWindowStart line 185, estimatedDuration line 187 as sibling numeric-preprocess examples)
    - src/app/(app)/jobs/[id]/job-info-panel.tsx (the Job Info right-panel form where arrival window / scheduling controls live — add the reminder-lead-time select here; confirm exact filename by listing src/app/(app)/jobs/[id]/ if this name differs)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (A6 — the reminder-window control location is planner's choice; job form / dispatch popup; D-05 "set per job by the dispatcher")
    - CLAUDE.md (§Base UI render prop not asChild; §React 19 form auto-reset — use router.refresh + prop sync, not post-action FormData reads)
  </read_first>
  <action>
    Add a `reminderLeadHours` field to createJobSchema in src/app/(app)/jobs/actions.ts using the numeric-preprocess pattern (like estimatedDuration line 187): `z.preprocess((v) => (v === '' || v == null ? null : Number(v)), z.number().int().min(0).nullable().optional())`. This value flows into scheduleAppointmentReminder (Task 1).

    Add a reminder-lead-time control to the Job Info panel (src/app/(app)/jobs/[id]/job-info-panel.tsx — confirm exact path during read_first). Use a select named `reminderLeadHours` with options: None (empty → no reminder), 1 hour, 2 hours, 4 hours, 24 hours, 48 hours before start. Follow Base UI `render` prop conventions (never asChild) and the React 19 form pattern already used in this panel. Place it near the arrival-window / scheduling fields. The dispatcher selecting a value and saving the job schedules the reminder; selecting None cancels any pending reminder (handled by scheduleAppointmentReminder's leadHours-null branch in Task 1).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && grep -c "reminderLeadHours" src/app/(app)/jobs/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reminderLeadHours" src/app/(app)/jobs/actions.ts` returns at least 1 (schema field present).
    - The Job Info panel renders a `reminderLeadHours` select with a None option and at least the 24-hours option.
    - The control uses Base UI `render` prop (no `asChild`) — `grep -c "asChild" <panel file>` returns 0 for any newly added element.
    - `pnpm build` clean; `pnpm test -- comms` shows no regressions.
  </acceptance_criteria>
  <done>Dispatcher can set a per-job reminder lead time on the Job Info panel; the value drives scheduleAppointmentReminder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/sms-reminders | Cron route is a public HTTP endpoint |
| scheduled_sms → Twilio | Bulk SMS dispatch must respect consent + not double-send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Elevation/DoS | /api/cron/sms-reminders | mitigate | `Authorization: Bearer ${CRON_SECRET}` check → 401 otherwise (Pattern 5, Pitfall 5); endpoint not publicly triggerable |
| T-08-13 | Info disclosure | scheduled_sms cross-tenant | mitigate | Cron processes each tenant's rows inside withTenant (RLS); distinct-tenant scan is the only unscoped read |
| T-08-14 | Repudiation/Compliance | reminder SMS to non-consented / cancelled job | mitigate | scheduleAppointmentReminder consent-gates via resolveSmsRecipient; cancelAppointmentReminders on job cancel; cron filters cancelled_at IS NULL (Pitfall 6) |
| T-08-15 | Tampering | duplicate reminder on cron overlap | mitigate | sent_at set on dispatch; cron query excludes sentAt IS NOT NULL (idempotent re-run) |
</threat_model>

<verification>
- `pnpm test -- comms/reminders-cron` — GREEN.
- `pnpm build` — clean; vercel.json is valid JSON with merged crons entry.
- Cron 401s without the CRON_SECRET bearer.
</verification>

<success_criteria>
- Per-job reminder lead time schedulable from the Job Info panel; writes/updates a scheduled_sms row (COMM-07).
- Hourly CRON_SECRET-gated cron dispatches overdue reminders through Twilio, idempotently, consent-gated (COMM-07, COMM-08).
- Cancelling or rescheduling a job cancels/recomputes pending reminders.
</success_criteria>

<output>
Create `.planning/phases/08-communications-and-notifications/08-04-SUMMARY.md` when done.
</output>
