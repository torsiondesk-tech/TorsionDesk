---
phase: 08-communications-and-notifications
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/lib/comms/resend.ts
  - src/lib/comms/twilio.ts
  - src/lib/comms/triggers.ts
  - src/app/(app)/communications/recipients.ts
  - src/lib/emails/job-confirmation.tsx
  - src/lib/emails/tech-notify.tsx
  - src/lib/emails/estimate-send.tsx
  - src/lib/emails/invoice-send.tsx
  - src/lib/emails/payment-receipt.tsx
  - src/lib/comms/send.ts
  - src/lib/comms/resend-webhook.ts
  - src/app/api/webhooks/resend/route.ts
  - src/app/(app)/communications/actions.ts
autonomous: true
requirements: [COMM-01, COMM-02, COMM-06, COMM-08, COMM-09]

must_haves:
  truths:
    - "A caller invoking sendCommunication for a disabled trigger makes no provider call and writes no log"
    - "A successful send writes both a communication_logs row (status='sent', provider_message_id) and a customer_events row (kind='email'); a failed send writes only communication_logs (status='failed')"
    - "sendCustomerCommunicationAction accepts the existing SendCommunicationInput shape unchanged and the PWA wrapper resolves it without error"
    - "An estimate/invoice email carries a base64 PDF attachment rendered in-process (no HTTP fetch of /api/*/pdf)"
    - "POST /api/webhooks/resend with a bad signature returns 400; email.opened updates opened_at on the matching communication_logs row"
    - "SMS is never sent to a contact whose sms_consent is false"
  artifacts:
    - path: "src/lib/comms/send.ts"
      provides: "Core sendCommunication orchestration (trigger gate, resolve, render, provider call, dual-table log)"
      exports: ["sendCommunication"]
      min_lines: 80
    - path: "src/app/(app)/communications/actions.ts"
      provides: "Canonical sendCustomerCommunicationAction honoring SendCommunicationInput"
      exports: ["sendCustomerCommunicationAction"]
    - path: "src/lib/comms/resend-webhook.ts"
      provides: "handleResendWebhook (svix verify + delivered/opened update)"
      exports: ["handleResendWebhook"]
    - path: "src/app/api/webhooks/resend/route.ts"
      provides: "POST route delegating to handleResendWebhook"
      exports: ["POST"]
  key_links:
    - from: "src/app/(app)/communications/actions.ts"
      to: "src/lib/comms/send.ts"
      via: "sendCommunication call"
      pattern: "sendCommunication\\("
    - from: "src/lib/comms/send.ts"
      to: "communication_logs + customer_events"
      via: "tx.insert on success/failure"
      pattern: "insert\\(communicationLogs\\)"
    - from: "src/app/api/webhooks/resend/route.ts"
      to: "src/lib/comms/resend-webhook.ts"
      via: "handleResendWebhook(req)"
      pattern: "handleResendWebhook"
    - from: "src/app/(tech)/tech/invoices/actions.ts"
      to: "src/app/(app)/communications/actions.ts"
      via: "dynamic import of sendCustomerCommunicationAction"
      pattern: "sendCustomerCommunicationAction"
---

<objective>
Build the canonical communications engine that every send trigger routes through. Deliver the lazy provider clients (`getResend`, `getTwilio`), the trigger-lookup/default-subject helpers, recipient resolution (email + SMS-with-consent-gate), the five React Email templates, the core `sendCommunication` orchestrator (trigger gate → resolve → render → attach PDF → provider call → dual-table log), the canonical `sendCustomerCommunicationAction` that honors the existing `SendCommunicationInput` contract, and the `/api/webhooks/resend` route with svix verification for delivery/open tracking.

Purpose: This is the core (COMM-01, COMM-06, COMM-08, COMM-09) that Waves 2–4 wire into job events, settings, and the reminder cron. Getting the contract and the dual-table log correct here is what makes the rest simple wiring.
Output: Working send pipeline + Resend webhook; comms/send, resend-webhook, pdf-attachment, sms-consent, and pwa-delegation tests go GREEN.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-communications-and-notifications/08-CONTEXT.md
@.planning/phases/08-communications-and-notifications/08-RESEARCH.md
@.planning/phases/08-communications-and-notifications/08-PATTERNS.md
@CLAUDE.md
@src/lib/stripe-webhook.ts
@src/app/(tech)/tech/invoices/actions.ts
</context>

<artifacts_this_phase_produces>
This plan creates: `getResend` (src/lib/comms/resend.ts), `getTwilio` (src/lib/comms/twilio.ts), `triggerLookup`/`defaultSubjectFor`/`mapKindToTrigger` (src/lib/comms/triggers.ts), `resolveEmailRecipient`/`resolveSmsRecipient` (src/app/(app)/communications/recipients.ts), `sendCommunication` + `InternalSendInput` type (src/lib/comms/send.ts), `handleResendWebhook` (src/lib/comms/resend-webhook.ts), the resend webhook `POST` route, the canonical `sendCustomerCommunicationAction` (src/app/(app)/communications/actions.ts), and email components `JobConfirmationEmail`/`TechNotifyEmail`/`EstimateSendEmail`/`InvoiceSendEmail`/`PaymentReceiptEmail` (src/lib/emails/*.tsx). See 08-01-PLAN.md for the full phase symbol list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Provider clients, trigger helpers, recipient resolution (with SMS consent gate), and 5 email templates</name>
  <files>src/lib/comms/resend.ts, src/lib/comms/twilio.ts, src/lib/comms/triggers.ts, src/app/(app)/communications/recipients.ts, src/lib/emails/job-confirmation.tsx, src/lib/emails/tech-notify.tsx, src/lib/emails/estimate-send.tsx, src/lib/emails/invoice-send.tsx, src/lib/emails/payment-receipt.tsx</files>
  <read_first>
    - src/lib/stripe-webhook.ts (lines 8-17 — lazy getStripe() pattern to mirror for getResend/getTwilio)
    - src/db/schema.ts (contacts table: firstName/lastName/smsConsent line 153/bookingContact line 155; contact_emails and contact_phones tables for recipient lookup; tenants.companyName line 35; communicationTriggers/communicationSettings added in Wave 0)
    - src/app/(tech)/tech/invoices/actions.ts (lines 8-15 SendCommunicationInput — kind is 'estimate'|'invoice' only)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/lib/comms/resend.ts, §twilio.ts, §src/lib/emails/*.tsx template data table)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 9 recipient resolution; Pitfall 3 sms_consent hard gate; Pitfall 4 async render; §Standard Stack @react-email versions)
    - src/lib/estimates/pdf-data.ts and src/lib/invoices/pdf-data.ts (getEstimateForPdf / getInvoiceForPdf signatures for the template data shape; used for attachments in Task 2)
  </read_first>
  <action>
    Create src/lib/comms/resend.ts exporting `getResend()` — a lazy dynamic import of Resend instantiated with `process.env.RESEND_API_KEY ?? ''` (mirror getStripe). Never reference the key via NEXT_PUBLIC_. Add a clear thrown error if the key is empty when a send is actually attempted (mirror getStripeSecret guard style).

    Create src/lib/comms/twilio.ts exporting `getTwilio()` — lazy dynamic import of `twilio` default export instantiated with `process.env.TWILIO_ACCOUNT_SID ?? ''` and `process.env.TWILIO_AUTH_TOKEN ?? ''`.

    Create src/lib/comms/triggers.ts exporting: `mapKindToTrigger(kind: 'estimate'|'invoice')` → 'estimate_send'|'invoice_send'; `defaultSubjectFor(triggerType, vars)` returning the default subject per trigger (Claude's discretion per D-01, e.g. "Your appointment is scheduled — {jobNo}", "Estimate #{estimateNo} from {companyName}", "Invoice #{invoiceNo} — Payment received"); and a `triggerLookup(tx, tenantId, triggerType, channel)` that selects the single communication_triggers row for that key (returns `{ enabled, subject, footerText }` or null).

    Create src/app/(app)/communications/recipients.ts exporting `resolveEmailRecipient` (given tenantId + refKind + refId + optional explicit `to`, returns an email address — prefer the explicit `to` from SendCommunicationInput, else the ref's contact's primary email from contact_emails) and `resolveSmsRecipient` (returns the booking contact with sms_consent=true, JOIN contact_phones for the E.164 number; fall back to the primary contact if no booking contact; return null if no consented contact). The consent gate is a HARD gate: never return a phone for a contact whose `sms_consent` is false, regardless of the column default being true (Pitfall 3, D-06, COMM-08).

    Create the five React Email templates in src/lib/emails/ using @react-email/components primitives (Html, Body, Container, Section, Text). Each exports a named component and includes the data columns from the PATTERNS template table plus an optional `footerText` prop appended below the system content block (never via dangerouslySetInnerHTML — plain Text node). Company name comes from `tenants.companyName` (NOT a company_profile table — RESEARCH A1):
    - JobConfirmationEmail: jobNo, customerName, scheduledDate, address, companyName, footerText?
    - TechNotifyEmail: jobNo, customerName, address, assignedTechName, companyName
    - EstimateSendEmail: estimateNo, customerName, total, companyName, footerText?
    - InvoiceSendEmail: invoiceNo, customerName, total, balance, companyName, footerText?
    - PaymentReceiptEmail: paymentNo, invoiceNo, amountPaid, companyName
  </action>
  <verify>
    <automated>pnpm test -- comms/sms-consent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEXT_PUBLIC" src/lib/comms/resend.ts src/lib/comms/twilio.ts` returns 0 (keys never client-exposed).
    - `resolveSmsRecipient` returns null (or refuses) for a contact with sms_consent=false — `pnpm test -- comms/sms-consent` passes.
    - All five files under src/lib/emails/ export a component and accept `companyName`; the four customer-facing templates accept an optional `footerText`.
    - getResend and getTwilio use dynamic import and read keys from non-NEXT_PUBLIC env vars.
    - `pnpm build` compiles the new modules.
  </acceptance_criteria>
  <done>Provider clients, trigger helpers, consent-gated recipient resolver, and 5 email templates exist; sms-consent test green.</done>
</task>

<task type="auto">
  <name>Task 2: Core sendCommunication orchestrator + in-process PDF attachment</name>
  <files>src/lib/comms/send.ts</files>
  <read_first>
    - src/lib/stripe-webhook.ts (recordStripePayment lines 59-164 — withTenant tx, early-return guard, customerEvents insert at 155-163)
    - src/lib/invoices/pdf-data.ts (getInvoiceForPdf signature) and src/components/invoices/invoice-pdf.tsx (InvoicePdfDocument props)
    - src/lib/estimates/pdf-data.ts (getEstimateForPdf signature) and src/components/pdf/estimate-pdf.tsx (EstimatePdfDocument props)
    - src/app/api/invoices/[id]/pdf/route.ts (lines ~25-30 — in-process React.createElement + pdf(element) pattern to copy)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/lib/comms/send.ts; §Customer Activity Feed write; critical note 1: send.ts is NOT 'use server')
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 8 in-process PDF; Pitfall 1 no HTTP fetch; Pitfall 4 await render)
    - src/db/schema.ts (communicationLogs + communicationSettings + customerEvents column names)
  </read_first>
  <action>
    Create src/lib/comms/send.ts (a plain async module — NOT 'use server'; the server boundary stays in actions.ts per PATTERNS critical note 1). Define an internal superset input type `InternalSendInput = { triggerType: TriggerType; channel: 'email'|'sms'; refKind: 'estimate'|'invoice'|'job'; refId: string; customerId?: string; to?: string; subject?: string; body?: string }` — do NOT widen the PWA-facing SendCommunicationInput; this is the broader internal shape.

    Export `sendCommunication(tenantId, input: InternalSendInput)` that runs inside `withTenant(tenantId, async tx => { ... })`:
    1. `triggerLookup(tx, tenantId, input.triggerType, input.channel)`; if null or `enabled === false`, return `{ skipped: true }` WITHOUT any provider call or log write (COMM-01/02 gate).
    2. Resolve recipient via resolveEmailRecipient / resolveSmsRecipient. If SMS and no consented recipient, log status='failed' (error_message 'no consented recipient') and return.
    3. Read communicationSettings for the tenant (email_sender_name, sms_phone_number) and tenants.companyName.
    4. Render content: for email, `await render(<Template {...vars} footerText={trigger.footerText}/>)` (async — Pitfall 4) using the template mapped from triggerType; for SMS, a plain string body (e.g. on_the_way body from RESEARCH Pattern 4).
    5. For estimate_send/invoice_send email, attach the PDF via an in-process render helper: `getInvoiceForPdf`/`getEstimateForPdf` → `React.createElement(<Document>)` → `pdf(el).toBlob()` → Buffer → base64 (Pattern 8). Do NOT HTTP-fetch /api/*/pdf (401 — Pitfall 1). Pass as Resend `attachments: [{ filename, content: base64 }]`.
    6. Call provider: email → `getResend().emails.send({ from: `"${senderName}" <contact@infantinosgaragedoor.com>`, to, subject, html, attachments })`; SMS → `getTwilio().messages.create({ body, from: smsPhoneNumber, to })`.
    7. On success: insert communicationLogs (status='sent', providerMessageId = Resend `data.id` or Twilio `sid`, plus refKind/refId/triggerType/channel/toAddress/customerId), AND insert customerEvents (kind='email', title per D-09 e.g. "Email sent: Job Confirmation" / "SMS sent: On The Way notification", refId, actor='comms'). On failure: insert communicationLogs ONLY (status='failed', errorMessage) — NO customer_events (D-10).

    Export a small `senderFrom(senderName)` helper if useful. Use `logger` for errors. Sender format exactly `"{email_sender_name}" <contact@infantinosgaragedoor.com>` (D-03, COMM-01).
  </action>
  <verify>
    <automated>pnpm test -- comms/send comms/pdf-attachment 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use server'" src/lib/comms/send.ts` returns 0 (send.ts is a plain module).
    - `grep -c "api/invoices\|api/estimates" src/lib/comms/send.ts` returns 0 (no HTTP fetch of PDF routes — in-process only).
    - `pnpm test -- comms/send` passes: disabled trigger → no provider call + no log; success → both tables written; failure → communication_logs only.
    - `pnpm test -- comms/pdf-attachment` passes: attachment content is a base64 string.
    - The `from` string literal in send.ts contains `contact@infantinosgaragedoor.com`.
    - `await render(` appears in the email render path (async render — Pitfall 4).
  </acceptance_criteria>
  <done>sendCommunication gates on trigger, resolves recipient, renders + attaches PDF in-process, and dual-table logs correctly; send + pdf-attachment tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Canonical sendCustomerCommunicationAction + Resend webhook (svix verify) + route</name>
  <files>src/app/(app)/communications/actions.ts, src/lib/comms/resend-webhook.ts, src/app/api/webhooks/resend/route.ts</files>
  <read_first>
    - src/app/(tech)/tech/invoices/actions.ts (lines 8-15 SendCommunicationInput; lines 80-108 PWA wrapper that dynamically imports and calls sendCustomerCommunicationAction(orgId, input) — signature MUST match, do NOT change the wrapper)
    - src/lib/stripe-webhook.ts (handleStripeWebhook lines 166-212 — raw body via req.text(), signature verify, 23505 dedup, 200-on-dedup, 400 on bad signature, 500 on error)
    - src/app/api/webhooks/stripe/route.ts (10-line route shell delegating to lib)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§communications/actions.ts fill-stub; §resend-webhook.ts full pattern map; §webhooks/resend/route.ts; critical note 2: webhook has no Clerk session — tenant scoping via provider_message_id lookup)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Resend webhook verification code example; Open Q4 tenant scoping via unscoped lookup by provider_message_id; §Security — do NOT add rate limiting)
    - src/db/schema.ts (communicationLogs columns providerMessageId/deliveredAt/openedAt/tenantId)
    - src/db/client.ts (the privileged `db` connection used unscoped, as in provision-tenant.ts)
  </read_first>
  <action>
    In src/app/(app)/communications/actions.ts (currently an empty stub) add `'use server'` and export the canonical:
    `export async function sendCustomerCommunicationAction(orgId: string, input: SendCommunicationInput): Promise<{ success: boolean; error?: string }>`. Import `SendCommunicationInput` FROM src/app/(tech)/tech/invoices/actions.ts — do NOT redefine or widen it. The action maps the narrow input (kind → triggerType via `mapKindToTrigger`) onto InternalSendInput and delegates to `sendCommunication(orgId, {...})`. Do NOT call `auth()` inside it (orgId is passed by the PWA wrapper which already authed — PATTERNS shared-auth note). Wrap in try/catch returning `{ success: false, error }` on throw, `{ success: true }` on success/skip.

    Create src/lib/comms/resend-webhook.ts exporting `handleResendWebhook(req: Request): Promise<Response>` — a near-verbatim structural copy of handleStripeWebhook using svix:
    - Guard: if `process.env.RESEND_WEBHOOK_SECRET` is empty → 500.
    - Read raw body via `await req.text()`; collect svix-id / svix-timestamp / svix-signature headers.
    - Verify with `new Webhook(secret).verify(payload, headers)`; on throw → 400.
    - For `email.delivered` / `email.opened`: update communication_logs setting deliveredAt / openedAt = now() WHERE providerMessageId = evt.data.email_id (D-11 signature verify, D-12 tie events to the log row by provider_message_id). The webhook has NO Clerk session (critical note 2): first look up the row's tenant_id unscoped via the privileged `db` connection keyed on the globally-unique providerMessageId, then perform the update (scoped via withTenant using that tenant_id, or unscoped privileged update keyed on providerMessageId). Return 200 for handled/ignored/unknown event types so svix stops retrying.

    Create src/app/api/webhooks/resend/route.ts as a 3-line delegate: `import { handleResendWebhook }` and `export async function POST(req: Request) { return handleResendWebhook(req) }`. Do NOT add any rate-limit check (CLAUDE.md — /api/webhooks/* is excluded; do not touch src/middleware.ts).
  </action>
  <verify>
    <automated>pnpm test -- comms/resend-webhook comms/pwa-delegation 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `sendCustomerCommunicationAction` signature is `(orgId: string, input: SendCommunicationInput)` and `SendCommunicationInput` is imported from src/app/(tech)/tech/invoices/actions.ts (not redefined) — `grep -c "interface SendCommunicationInput" src/app/(app)/communications/actions.ts` returns 0.
    - `pnpm test -- comms/pwa-delegation` passes (input shape honored end to end).
    - `pnpm test -- comms/resend-webhook` passes: bad signature → 400; email.opened → opened_at set on the row matched by provider_message_id.
    - `grep -c "rate" src/app/api/webhooks/resend/route.ts` returns 0 (no rate limiting added).
    - `new Webhook(` from svix appears in resend-webhook.ts.
  </acceptance_criteria>
  <done>Canonical action honors the PWA contract; Resend webhook verifies + updates delivery/open by provider_message_id; route delegates with no rate limiting; both tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resend → /api/webhooks/resend | Untrusted inbound webhook (public route, no session) |
| PWA/office → sendCustomerCommunicationAction | Caller-supplied recipient/refId crosses into provider send |
| server → provider APIs | Secret API keys must stay server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing | /api/webhooks/resend | mitigate | `new Webhook(RESEND_WEBHOOK_SECRET).verify(rawBody, svix headers)` before any DB write; 400 on failure (Task 3) |
| T-08-04 | Info disclosure | getResend/getTwilio keys | mitigate | Lazy server-side clients read RESEND_API_KEY / TWILIO_* from non-NEXT_PUBLIC env; grep gate asserts 0 NEXT_PUBLIC references |
| T-08-05 | Info disclosure | communication_logs cross-tenant | mitigate | sendCommunication writes inside withTenant (RLS); webhook update keyed on globally-unique provider_message_id then scoped by looked-up tenant_id |
| T-08-06 | Repudiation/Compliance | SMS to non-consented contact | mitigate | resolveSmsRecipient hard consent gate (D-06); never returns a phone for sms_consent=false; comms/sms-consent test enforces |
| T-08-07 | Injection | admin subject/footer_text into email | mitigate | React Email escapes Text nodes; footer_text rendered as plain Text, never dangerouslySetInnerHTML |
| T-08-SC | Tampering | (carried from 08-01) | mitigate | Packages already vetted + installed in Wave 0 |
</threat_model>

<verification>
- `pnpm test -- comms` — send, pdf-attachment, sms-consent, resend-webhook, pwa-delegation go GREEN.
- `pnpm build` — clean.
- No NEXT_PUBLIC references to provider keys; no rate-limit added to the webhook route; no HTTP fetch of PDF routes.
</verification>

<success_criteria>
- Canonical sendCommunication engine gates on trigger, resolves consented recipients, renders templates, attaches in-process PDFs, and logs to both tables on success (log-only on failure).
- sendCustomerCommunicationAction honors the unchanged SendCommunicationInput contract; PWA wrapper works.
- /api/webhooks/resend verifies svix signatures and updates delivered_at/opened_at by provider_message_id.
- COMM-01, COMM-06, COMM-08, COMM-09 core behaviors implemented and tested.
</success_criteria>

<output>
Create `.planning/phases/08-communications-and-notifications/08-02-SUMMARY.md` when done.
</output>
