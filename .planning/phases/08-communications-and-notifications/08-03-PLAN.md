---
phase: 08-communications-and-notifications
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - src/app/(app)/jobs/actions.ts
  - src/app/(app)/communications/recipients.ts
  - src/lib/stripe-webhook.ts
  - src/app/(app)/invoices/actions.ts
  - src/app/(app)/estimates/actions.ts
autonomous: true
requirements: [COMM-03, COMM-04, COMM-05, COMM-06, COMM-07]

must_haves:
  truths:
    - "Creating a job fires the job_confirmation email trigger (only when enabled) without blocking the create response"
    - "Assigning/modifying a job's techs fires the tech_notify email to the assigned technician's resolved email (only when enabled)"
    - "A job transition to on_the_way attempts the customer SMS after the status commits; a Twilio failure logs status='failed' and does NOT roll back the transition"
    - "A confirmed Stripe payment fires exactly one payment_receipt email — only on the first non-duplicate processing path"
    - "sendInvoiceAction and sendEstimateAction call the canonical action instead of console.log; the PWA send buttons work through the same path"
  artifacts:
    - path: "src/app/(app)/jobs/actions.ts"
      provides: "job_confirmation + tech_notify + on_the_way SMS fires via after()"
      contains: "sendCommunication"
    - path: "src/lib/stripe-webhook.ts"
      provides: "payment_receipt fire on non-duplicate path"
      contains: "payment_receipt"
    - path: "src/app/(app)/invoices/actions.ts"
      provides: "sendInvoiceAction wired to canonical action"
    - path: "src/app/(app)/estimates/actions.ts"
      provides: "sendEstimateAction wired to canonical action"
  key_links:
    - from: "src/app/(app)/jobs/actions.ts"
      to: "src/lib/comms/send.ts"
      via: "after(() => sendCommunication(...))"
      pattern: "sendCommunication\\("
    - from: "src/lib/stripe-webhook.ts"
      to: "src/lib/comms/send.ts"
      via: "after(() => sendCommunication payment_receipt)"
      pattern: "payment_receipt"
    - from: "src/app/(app)/invoices/actions.ts"
      to: "src/app/(app)/communications/actions.ts"
      via: "sendCustomerCommunicationAction call"
      pattern: "sendCustomerCommunicationAction"
---

<objective>
Wire the canonical send engine into the existing event flows: fire job_confirmation on job create (COMM-04), tech_notify on job assignment/modification (COMM-05), on_the_way SMS bound to the status transition (COMM-07, D-04, fire-and-forget), payment_receipt from the Stripe webhook on the first non-duplicate path (COMM-03), and replace the sendInvoiceAction/sendEstimateAction console.log stubs with real canonical-action calls (COMM-06). All event sends use `after()` so the primary mutation is never blocked.

Purpose: These are the synchronous event bindings — the moment Phase 8 becomes visible in real workflows. Each side effect is non-blocking and failure-isolated so a provider outage never breaks job/payment operations.
Output: Live sends on job create/assign/transition + Stripe payment + invoice/estimate send buttons; job-confirmation, tech-notify, on-the-way, payment-receipt tests go GREEN.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-communications-and-notifications/08-CONTEXT.md
@.planning/phases/08-communications-and-notifications/08-RESEARCH.md
@.planning/phases/08-communications-and-notifications/08-PATTERNS.md
@CLAUDE.md
@src/lib/stripe-webhook.ts
</context>

<artifacts_this_phase_produces>
This plan wires existing symbols; it adds `resolveTechEmail` to src/app/(app)/communications/recipients.ts and modifies event call sites in jobs/actions.ts, stripe-webhook.ts, invoices/actions.ts, estimates/actions.ts. No new tables/enums. See 08-01-PLAN.md for the full phase symbol list; the canonical engine symbols come from 08-02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fire job_confirmation (create), tech_notify (assign), and on_the_way SMS (transition)</name>
  <files>src/app/(app)/jobs/actions.ts, src/app/(app)/communications/recipients.ts</files>
  <read_first>
    - src/app/(app)/jobs/actions.ts (line 4 `import { after } from 'next/server'` already present; transitionJobStatusAction lines 1052-1076 incl. the existing `after(() => broadcastJobEvent(...))` at 1064; the job-create action and the update/assignment action — locate by searching for the createJob and updateJob exports; job columns customerId, assignedTechs, arrivalWindowStart/End)
    - src/db/schema.ts (jobs table columns: jobNo, customerId, serviceLocationId, arrivalWindowStart line 713, arrivalWindowEnd line 714, assigned-tech linkage table; tenants.companyName line 35)
    - src/app/(app)/communications/recipients.ts (from Wave 1 — add resolveTechEmail here)
    - src/lib/comms/send.ts (sendCommunication + InternalSendInput from Wave 1)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/app/(app)/jobs/actions.ts — exact after() fire snippets for on_the_way and job_confirmation; §Pattern 2)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 4 on_the_way SMS body; Open Q2 tech email source — use the same source as listTechnicians from Phase 4)
    - Phase 4 listTechnicians source (search src/app/(app)/dispatch for listTechnicians to find where a technician's email lives — Clerk user vs users table)
  </read_first>
  <action>
    In src/app/(app)/jobs/actions.ts:

    (a) In the job-create action, after the job row is committed and its id + customerId are known, add a non-blocking fire:
    `after(() => sendCommunication(orgId, { triggerType: 'job_confirmation', channel: 'email', refKind: 'job', refId: jobId, customerId }).catch((e) => logger.error('job_confirmation send', e)))` (COMM-04). The trigger-enabled gate lives inside sendCommunication, so no enabled check is needed here.

    (b) In the assignment/modify path (the updateJob action where assignedTechs changes), add a non-blocking `after(() => sendCommunication(orgId, { triggerType: 'tech_notify', channel: 'email', refKind: 'job', refId: jobId, customerId }).catch(...))` (COMM-05). tech_notify's recipient is the assigned technician's email, NOT a customer contact — add `resolveTechEmail(orgId, jobId)` to recipients.ts that reads the assigned technician's email from the SAME source Phase 4's listTechnicians uses (Clerk user or users table — confirm during read_first). Ensure sendCommunication's recipient resolution branches to resolveTechEmail when triggerType='tech_notify' (email channel) rather than the customer contact_emails path.

    (c) In transitionJobStatusAction, immediately after the existing `after(() => broadcastJobEvent(...))` at line ~1064 and only when `toStatus === 'on_the_way'`, add: `after(() => sendCommunication(orgId, { triggerType: 'on_the_way', channel: 'sms', refKind: 'job', refId: jobId, customerId }).catch((e) => logger.error('on_the_way sms', e)))` (COMM-07, D-04). This is fire-and-forget AFTER the status commit — a Twilio failure is logged (status='failed' inside sendCommunication) and MUST NOT roll back the transition. The SMS body (Claude's discretion) is "Your tech is on the way! Estimated arrival: {arrivalWindow}. — {companyName}" built from arrivalWindowStart/End + tenants.companyName; assemble this in sendCommunication's on_the_way SMS branch, not at the call site.

    Import `sendCommunication` from '@/lib/comms/send'. `after` and `logger` are already available in this file.
  </action>
  <verify>
    <automated>pnpm test -- comms/job-confirmation comms/tech-notify comms/on-the-way 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "triggerType: 'job_confirmation'\|triggerType: 'tech_notify'\|triggerType: 'on_the_way'" src/app/(app)/jobs/actions.ts` returns 3.
    - The on_the_way fire is guarded by `toStatus === 'on_the_way'` and placed after the status-commit / broadcast `after()` call (not before transitionJobStatus succeeds).
    - `resolveTechEmail` exists in recipients.ts and reads the technician email from the listTechnicians source.
    - `pnpm test -- comms/job-confirmation comms/tech-notify comms/on-the-way` all pass; on-the-way test confirms no rollback on Twilio failure.
    - All three fires use `after(() => ...catch(...))` (non-blocking, failure-isolated).
  </acceptance_criteria>
  <done>job_confirmation, tech_notify, and on_the_way SMS fire non-blocking from the correct event points; three tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Fire payment_receipt from the Stripe webhook (non-duplicate path only)</name>
  <files>src/lib/stripe-webhook.ts</files>
  <read_first>
    - src/lib/stripe-webhook.ts (recordStripePayment lines 59-164; the two dedup early-returns at 72-74 and 91-93; the customerEvents insert at 155-163 which runs only on the non-duplicate path; note `after` is NOT yet imported in this file)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/lib/stripe-webhook.ts MODIFY — add payment_receipt fire after customerEvents insert on non-duplicate path; add `after` import)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 3; Pitfall 2 duplicate receipts — fire ONLY on first non-duplicate path)
    - src/lib/comms/send.ts (sendCommunication)
  </read_first>
  <action>
    In src/lib/stripe-webhook.ts, add `import { after } from 'next/server'` to the import list. At the END of recordStripePayment's withTenant callback — AFTER the `customerEvents` insert at lines 155-163, which is already past both dedup early-returns (the `existing.length > 0` guard at 72 and the `existingByToken.length > 0` guard at 91) — add:
    `after(() => sendCommunication(tenantId, { triggerType: 'payment_receipt', channel: 'email', refKind: 'invoice', refId: info.invoiceId ?? '', customerId }).catch((e) => logger.error('payment_receipt send', e)))` (COMM-03).

    Placing it after the customerEvents insert guarantees it runs only on the first, non-duplicate processing path (Pitfall 2). Do NOT place the fire before the dedup guards and do NOT let a send failure throw out of the webhook (the `.catch` + `after` ensure Stripe is not made to retry the whole event). Import `sendCommunication` from '@/lib/comms/send'.
  </action>
  <verify>
    <automated>pnpm test -- comms/payment-receipt 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "payment_receipt" src/lib/stripe-webhook.ts` returns at least 1.
    - The payment_receipt `after(...)` call appears AFTER the `customerEvents` insert (lexically past both dedup early-returns).
    - `import { after } from 'next/server'` present in stripe-webhook.ts.
    - `pnpm test -- comms/payment-receipt` passes: fires once on first path; duplicate delivery does not re-fire.
    - The fire is wrapped in `.catch(...)` so a provider failure cannot throw out of the webhook.
  </acceptance_criteria>
  <done>payment_receipt fires exactly once from the Stripe webhook on the non-duplicate path; payment-receipt test green.</done>
</task>

<task type="auto">
  <name>Task 3: Replace sendInvoiceAction and sendEstimateAction stubs with canonical-action calls</name>
  <files>src/app/(app)/invoices/actions.ts, src/app/(app)/estimates/actions.ts</files>
  <read_first>
    - src/app/(app)/invoices/actions.ts (sendInvoiceAction stub at lines 585-591 — signature `(orgId: string, invoiceId: string): Promise<{ success: boolean }>`, called from invoice-detail-shell.tsx line 335 as `sendInvoiceAction(invoice.tenantId, invoice.id)`; getInvoiceDetail email/contact resolution nearby for resolveInvoiceEmail)
    - src/app/(app)/estimates/actions.ts (sendEstimateAction stub at lines 1438-1444, called from estimate-form.tsx line 510 as `sendEstimateAction(orgId, estimateId)`)
    - src/app/(app)/communications/actions.ts (canonical sendCustomerCommunicationAction from Wave 1 — signature `(orgId, input: SendCommunicationInput)`)
    - src/app/(app)/communications/recipients.ts (resolveEmailRecipient — reuse to get the customer email for the invoice/estimate)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§invoices/actions.ts MODIFY; §estimates/actions.ts MODIFY; §Pattern 6)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Pattern 6 replace stubs; PWA delegates)
  </read_first>
  <action>
    In src/app/(app)/invoices/actions.ts replace the sendInvoiceAction stub body (remove the console.log). Keep the exact existing signature `(orgId: string, invoiceId: string): Promise<{ success: boolean }>` (invoice-detail-shell.tsx calls it with `(invoice.tenantId, invoice.id)`). Resolve the recipient email for the invoice's customer (reuse resolveEmailRecipient with refKind='invoice', refId=invoiceId, or a small local resolveInvoiceEmail reading contact_emails), then `return sendCustomerCommunicationAction(orgId, { kind: 'invoice', refId: invoiceId, channel: 'email', to })` and map its result to `{ success }` (COMM-06).

    In src/app/(app)/estimates/actions.ts replace the sendEstimateAction stub body the same way — keep signature `(orgId, estimateId)` (estimate-form.tsx line 510), resolve the estimate customer's email, then `return sendCustomerCommunicationAction(orgId, { kind: 'estimate', refId: estimateId, channel: 'email', to })` mapped to `{ success }`.

    Both now route through the canonical action, so the trigger-enabled gate, PDF attachment, and dual-table logging all apply automatically, and the Phase 5 PWA send wrapper (which dynamically imports sendCustomerCommunicationAction) starts working with no change to the wrapper.
  </action>
  <verify>
    <automated>grep -c "console.log" src/app/(app)/invoices/actions.ts src/app/(app)/estimates/actions.ts && pnpm test -- comms 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sendCustomerCommunicationAction" src/app/(app)/invoices/actions.ts` and `... src/app/(app)/estimates/actions.ts` each return at least 1.
    - The old `console.log('sendInvoiceAction called — stub...')` and `console.log('sendEstimateAction called — stub...')` lines are gone.
    - sendInvoiceAction signature stays `(orgId: string, invoiceId: string)` and sendEstimateAction stays `(orgId, estimateId)` — callers in invoice-detail-shell.tsx line 335 and estimate-form.tsx line 510 still typecheck.
    - `pnpm build` clean; full `pnpm test -- comms` shows no regressions.
  </acceptance_criteria>
  <done>Both send stubs route through the canonical action; signatures unchanged; PWA send path works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → recordStripePayment | At-least-once webhook delivery can duplicate side effects |
| status transition → SMS side effect | A failing side effect must not corrupt the primary mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering/Repudiation | payment_receipt on Stripe retry | mitigate | Fire placed after customerEvents insert (past both dedup guards); fires once only (Pitfall 2); comms/payment-receipt test enforces |
| T-08-09 | Availability | on_the_way SMS failure | mitigate | Fire-and-forget via after() with .catch; status commit is never rolled back (D-04); comms/on-the-way test enforces no rollback |
| T-08-10 | Info disclosure | tech_notify recipient | mitigate | resolveTechEmail reads only the assigned tech's email from the authorized listTechnicians source; send runs inside withTenant/RLS |
| T-08-11 | Availability | provider failure throwing out of webhook | mitigate | All event fires use after(() => ...catch()); a Resend/Twilio outage never throws out of the Stripe webhook or the mutation |
</threat_model>

<verification>
- `pnpm test -- comms` — job-confirmation, tech-notify, on-the-way, payment-receipt GREEN; no regressions in send/webhook tests.
- `pnpm build` — clean.
- Callers of sendInvoiceAction / sendEstimateAction still typecheck (signatures unchanged).
</verification>

<success_criteria>
- job_confirmation (COMM-04), tech_notify (COMM-05), on_the_way SMS (COMM-07, D-04), and payment_receipt (COMM-03) all fire non-blocking from their correct event points.
- Duplicate Stripe deliveries do not duplicate receipts.
- sendInvoiceAction / sendEstimateAction (COMM-06) route through the canonical action; the PWA send buttons work.
</success_criteria>

<output>
Create `.planning/phases/08-communications-and-notifications/08-03-SUMMARY.md` when done.
</output>
