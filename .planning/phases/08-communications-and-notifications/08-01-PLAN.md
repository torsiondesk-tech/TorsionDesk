---
phase: 08-communications-and-notifications
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - src/db/schema.ts
  - tests/comms/mocks/providers.ts
  - tests/comms/send.test.ts
  - tests/comms/triggers-schema.test.ts
  - tests/comms/payment-receipt.test.ts
  - tests/comms/job-confirmation.test.ts
  - tests/comms/tech-notify.test.ts
  - tests/comms/on-the-way.test.ts
  - tests/comms/reminders-cron.test.ts
  - tests/comms/sms-consent.test.ts
  - tests/comms/pdf-attachment.test.ts
  - tests/comms/resend-webhook.test.ts
  - tests/comms/pwa-delegation.test.ts
  - tests/comms/inv12-opened.test.ts
autonomous: false
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, COMM-05, COMM-06, COMM-07, COMM-08, COMM-09]
user_setup:
  - service: resend
    why: "Transactional email provider (COMM-01–06, COMM-09)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
      - name: RESEND_WEBHOOK_SECRET
        source: "Resend Dashboard -> Webhooks -> (endpoint) -> Signing Secret"
  - service: twilio
    why: "SMS provider, A2P 10DLC (COMM-07, COMM-08)"
    env_vars:
      - name: TWILIO_ACCOUNT_SID
        source: "Twilio Console -> Account Info"
      - name: TWILIO_AUTH_TOKEN
        source: "Twilio Console -> Account Info"
  - service: vercel-cron
    why: "Appointment reminder cron auth (COMM-07, D-05)"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret; set in Vercel project env + used by cron route bearer check"

must_haves:
  truths:
    - "resend, twilio, @react-email/components, and @react-email/render are installed and importable"
    - "trigger_type enum and 4 new tables exist in the live Supabase database after db:push"
    - "Running `pnpm test -- comms` shows the 12 comms test files failing (RED) because the modules under test do not exist yet"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "trigger_type enum + communicationTriggers, communicationLogs, communicationSettings, scheduledSms tables with RLS"
      contains: "communicationTriggers"
    - path: "tests/comms/mocks/providers.ts"
      provides: "Shared vi.mock helpers for resend, twilio, svix"
    - path: "tests/comms/send.test.ts"
      provides: "RED test for trigger gate + dual-table logging"
  key_links:
    - from: "src/db/schema.ts"
      to: "drizzle/ (migration)"
      via: "pnpm db:push"
      pattern: "communication_triggers"
---

<objective>
Lay the Phase 8 foundation: install the four communications packages, add the `trigger_type` enum plus the four new tables (`communication_triggers`, `communication_logs`, `communication_settings`, `scheduled_sms`) to the Drizzle schema with full RLS, write the twelve RED test files (Nyquist floor) that specify every phase behavior, and push the schema to Supabase so downstream waves have live types AND a live database.

Purpose: Everything in Waves 1–4 imports these table types and is validated against these tests. Without the [BLOCKING] `pnpm db:push`, build/type-check would pass against config-derived types while the live database lacks the tables — a false-positive verification state.
Output: 4 new tables + 1 enum in the live DB, 4 installed packages, 12 failing test files + shared provider mocks.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-communications-and-notifications/08-CONTEXT.md
@.planning/phases/08-communications-and-notifications/08-RESEARCH.md
@.planning/phases/08-communications-and-notifications/08-PATTERNS.md
@.planning/phases/08-communications-and-notifications/08-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols and files created across all of Phase 8 (for source-grounding / drift exclusion). This plan creates the bolded items:

- **`triggerType`** (pgEnum 'trigger_type') — src/db/schema.ts
- **`communicationTriggers`**, **`communicationLogs`**, **`communicationSettings`**, **`scheduledSms`** (pgTable) — src/db/schema.ts
- **`tests/comms/mocks/providers.ts`** and 12 **`tests/comms/*.test.ts`** files
- `getResend`, `getTwilio` — src/lib/comms/resend.ts, twilio.ts (Wave 1, plan 08-02)
- `sendCommunication`, `InternalSendInput` — src/lib/comms/send.ts (Wave 1, plan 08-02)
- `resolveEmailRecipient`, `resolveSmsRecipient`, `resolveTechEmail` — src/app/(app)/communications/recipients.ts (Waves 1-2, plans 08-02/08-03)
- `handleResendWebhook` — src/lib/comms/resend-webhook.ts (Wave 1, plan 08-02)
- `triggerLookup`, `defaultSubjectFor`, `mapKindToTrigger` — src/lib/comms/triggers.ts (Wave 1, plan 08-02)
- `JobConfirmationEmail`, `TechNotifyEmail`, `EstimateSendEmail`, `InvoiceSendEmail`, `PaymentReceiptEmail` — src/lib/emails/*.tsx (Wave 1, plan 08-02)
- `sendCustomerCommunicationAction` (canonical) — src/app/(app)/communications/actions.ts (Wave 1, plan 08-02)
- `listEmailTriggersAction`, `listSmsTriggersAction`, `updateTriggerAction`, `updateCommunicationSettingsAction`, `backfillCommunicationDefaultsAction` — src/app/(app)/communications/actions.ts (Wave 4, plan 08-05)
- `scheduleAppointmentReminder`, `cancelAppointmentReminders` — src/app/(app)/communications/reminders.ts (Wave 3, plan 08-04)
- Cron route GET handler — src/app/api/cron/sms-reminders/route.ts (Wave 3, plan 08-04)
- Resend webhook POST route — src/app/api/webhooks/resend/route.ts (Wave 1, plan 08-02)
- `EmailSettingsForm`, `SmsSettingsForm` — src/app/(app)/settings/email|sms/*.tsx (Wave 4, plan 08-05)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — resend, twilio, @react-email/components, @react-email/render</name>
  <files>package.json (install target — no edit at this step)</files>
  <read_first>
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Package Legitimacy Audit — all four Approved)
    - CLAUDE.md (§Package Manager — pnpm only, never npm/npx/yarn)
  </read_first>
  <action>
    Do NOT install anything in this task. Present the four packages (resend, twilio, @react-email/components, @react-email/render) and their RESEARCH §Package Legitimacy Audit findings for human sign-off, then pause for approval. Installation happens in Task 2 only after approval is given. This gate is not auto-approvable.
  </action>
  <what-built>
    RESEARCH.md §Package Legitimacy Audit verified all four packages against the npm registry and official source repos (resend/resend-node, twilio/twilio-node, resend/react-email) and marked each "Approved". `svix` and `@react-pdf/renderer` are already installed. No package is flagged [ASSUMED]/[SUS]/[SLOP]. This checkpoint records human confirmation before any install runs, per the package legitimacy gate.
  </what-built>
  <how-to-verify>
    1. Confirm each package resolves to its official org on npmjs.com:
       - https://www.npmjs.com/package/resend  (org: resend)
       - https://www.npmjs.com/package/twilio  (org: twilio)
       - https://www.npmjs.com/package/@react-email/components  (org: resend / react-email)
       - https://www.npmjs.com/package/@react-email/render  (org: resend / react-email)
    2. Confirm the versions in RESEARCH match (resend 6.16.0, twilio 6.0.2, @react-email/components 1.0.12, @react-email/render 2.0.9) or are the current stable releases.
  </how-to-verify>
  <acceptance_criteria>
    - Human approves the four packages OR names specific concerns.
    - No package proceeds to install without this approval (gate is not auto-approvable).
  </acceptance_criteria>
  <verify>
    <human-check>Reviewer confirms all four packages are legitimate and approves the install.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe concerns.</resume-signal>
  <done>Human has approved the four packages for install.</done>
</task>

<task type="auto">
  <name>Task 2: Install packages and add trigger_type enum + 4 RLS tables to schema</name>
  <files>package.json, pnpm-lock.yaml, src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (existing eventKind enum at line 59; customerEvents table at lines 399-430 — copy its pgTable+pgPolicy+enableRLS shape; contacts.smsConsent at line 153, contacts.bookingContact at line 155; tenants.companyName at line 35)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§src/db/schema.ts — exact enum + table + RLS + unique() patterns to copy)
    - .planning/phases/08-communications-and-notifications/08-RESEARCH.md (§Table schema (D-02) code example; §Common Pitfalls Pitfall 3 sms_consent default)
    - CLAUDE.md (§Multi-tenancy — every table has tenant_id + pgPolicy + enableRLS; §Package Manager)
  </read_first>
  <action>
    Run `pnpm add resend twilio @react-email/components @react-email/render` (pnpm only — never npm/npx/yarn). Then edit src/db/schema.ts:

    Add a `triggerType` pgEnum named 'trigger_type' with exactly these seven values in order: 'job_confirmation', 'tech_notify', 'estimate_send', 'invoice_send', 'payment_receipt', 'on_the_way', 'appointment_reminder' (mirror the eventKind pgEnum shape at line 59).

    Add four pgTable definitions, each copying the customerEvents RLS shape (a `pgPolicy('<name>_tenant_isolation', { for: 'all', to: 'authenticated', using/withCheck: sql\`${t.tenantId} = current_setting('app.current_tenant_id', true)\` })` followed by `.enableRLS()`), each with `id: text('id').primaryKey().default(sql\`gen_random_uuid()\`)` and `tenantId: text('tenant_id').notNull()`:

    - `communicationTriggers` ('communication_triggers') per D-02: columns `triggerType: triggerType('trigger_type').notNull()`, `channel: text('channel').notNull()` ('email'|'sms'), `enabled: boolean('enabled').notNull().default(true)`, `subject: text('subject')` (nullable), `footerText: text('footer_text')` (nullable). Add `unique('comm_triggers_unique').on(t.tenantId, t.triggerType, t.channel)`.
    - `communicationLogs` ('communication_logs') per D-08: columns `customerId: text('customer_id')`, `refKind: text('ref_kind')` ('estimate'|'invoice'|'job'), `refId: text('ref_id')`, `triggerType: triggerType('trigger_type')`, `channel: text('channel').notNull()`, `toAddress: text('to_address')`, `status: text('status').notNull()` ('sent'|'failed'), `providerMessageId: text('provider_message_id')`, `errorMessage: text('error_message')`, `sentAt: timestamp('sent_at').defaultNow()`, `deliveredAt: timestamp('delivered_at')` (nullable), `openedAt: timestamp('opened_at')` (nullable). Add `index('comm_logs_provider_msg_idx').on(t.providerMessageId)` and `index('comm_logs_ref_idx').on(t.refKind, t.refId)`.
    - `communicationSettings` ('communication_settings') per D-03: columns `emailSenderName: text('email_sender_name')`, `smsPhoneNumber: text('sms_phone_number')`. Add `unique('comm_settings_tenant_unique').on(t.tenantId)` so there is one row per tenant.
    - `scheduledSms` ('scheduled_sms') per D-05: columns `jobId: text('job_id').notNull()`, `contactId: text('contact_id')`, `phone: text('phone').notNull()`, `messageBody: text('message_body').notNull()`, `fireAt: timestamp('fire_at').notNull()`, `sentAt: timestamp('sent_at')` (nullable), `cancelledAt: timestamp('cancelled_at')` (nullable), `errorMessage: text('error_message')`. Add `index('scheduled_sms_due_idx').on(t.fireAt)` and `index('scheduled_sms_job_idx').on(t.jobId)`.

    Ensure `timestamp`, `unique`, `index`, `boolean`, `pgEnum`, `pgPolicy`, `sql` are imported (most already are — add only what is missing). Do NOT alter the existing `contacts.smsConsent` default in this task (consent-gate handling is enforced in the Wave 1 resolver per Pitfall 3). Do NOT run db:push here — that is Task 4.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5 && grep -c "communication_triggers\|communication_logs\|communication_settings\|scheduled_sms" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm ls resend twilio @react-email/components @react-email/render` lists all four as installed.
    - `grep -v '^#' src/db/schema.ts | grep -c "pgTable('communication_triggers'\|pgTable('communication_logs'\|pgTable('communication_settings'\|pgTable('scheduled_sms'"` returns 4.
    - `grep -c "trigger_type" src/db/schema.ts` is at least 1 (the pgEnum declaration).
    - Each of the four new tables ends with `.enableRLS()` and contains a `pgPolicy(...tenant_isolation...)` using `current_setting('app.current_tenant_id', true)`.
    - `pnpm build` completes without type errors.
  </acceptance_criteria>
  <done>Four packages installed; trigger_type enum + 4 RLS-enabled tables present in schema.ts; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write 12 RED comms test files + shared provider mocks</name>
  <files>tests/comms/mocks/providers.ts, tests/comms/send.test.ts, tests/comms/triggers-schema.test.ts, tests/comms/payment-receipt.test.ts, tests/comms/job-confirmation.test.ts, tests/comms/tech-notify.test.ts, tests/comms/on-the-way.test.ts, tests/comms/reminders-cron.test.ts, tests/comms/sms-consent.test.ts, tests/comms/pdf-attachment.test.ts, tests/comms/resend-webhook.test.ts, tests/comms/pwa-delegation.test.ts, tests/comms/inv12-opened.test.ts</files>
  <read_first>
    - .planning/phases/08-communications-and-notifications/08-VALIDATION.md (§Wave 0 Requirements — the exact 12 files + their behaviors; §Per-Task Verification Map)
    - .planning/phases/08-communications-and-notifications/08-PATTERNS.md (§tests/comms/*.test.ts — test header pattern, vi.mock('stripe'/'resend'/'twilio'/'svix') pattern, withTenant mock pattern, Test file → behavior map)
    - tests/invoices/stripe-webhook.test.ts (vi.mock for stripe webhook + withTenant mock — the closest analog)
    - tests/webhook.test.ts (test file header + describe/it pattern)
    - src/app/(tech)/tech/invoices/actions.ts (lines 8-15 SendCommunicationInput shape for the pwa-delegation test)
  </read_first>
  <action>
    Create tests/comms/mocks/providers.ts exporting reusable `vi.mock` factories / helper builders for `resend` (Resend class with `emails.send` returning `{ data: { id: 'msg_test' }, error: null }`), `twilio` (default export function returning `{ messages: { create: async () => ({ sid: 'SM_test' }) } }`), and `svix` (Webhook class whose `verify` throws on a 'bad_sig' payload and otherwise returns the parsed event). Also export a `mockWithTenant(txShape)` helper mirroring the withTenant mock in tests/invoices/stripe-webhook.test.ts.

    Create the twelve test files listed in VALIDATION §Wave 0, each importing from vitest and asserting the behavior named in the VALIDATION Per-Task Verification Map. These are RED tests — they import the not-yet-existing modules (e.g. `@/lib/comms/send`, `@/lib/comms/resend-webhook`, `@/app/(app)/communications/actions`, `@/app/(app)/communications/recipients`) and MUST fail until Waves 1–4 implement them:
    - tests/comms/send.test.ts — trigger enabled=false → no provider call; success → both communication_logs + customer_events written; failure → communication_logs only (COMM-01/02, D-07/10).
    - tests/comms/triggers-schema.test.ts — the 4 tables + trigger_type enum exist and CRUD is tenant-scoped (COMM-02/08). Assert exported table objects have the expected column keys.
    - tests/comms/payment-receipt.test.ts — stripe webhook fires payment_receipt once; duplicate delivery path does NOT re-fire (COMM-03).
    - tests/comms/job-confirmation.test.ts — job create fires job_confirmation when trigger enabled (COMM-04).
    - tests/comms/tech-notify.test.ts — tech_notify resolves the assigned technician's email and fires (COMM-05).
    - tests/comms/on-the-way.test.ts — on_the_way transition attempts SMS; Twilio failure logs status='failed', no rollback (COMM-07, D-04).
    - tests/comms/reminders-cron.test.ts — overdue scheduled_sms fired + sent_at set; cancelled_at rows skipped (COMM-07, D-05).
    - tests/comms/sms-consent.test.ts — resolver refuses a contact with sms_consent=false regardless of column default (D-06, Pitfall 3).
    - tests/comms/pdf-attachment.test.ts — estimate/invoice email attachment content is a base64 string produced in-process (COMM-09).
    - tests/comms/resend-webhook.test.ts — bad signature → 400; email.opened → opened_at set on the matching communication_logs row (D-11/12).
    - tests/comms/pwa-delegation.test.ts — canonical sendCustomerCommunicationAction accepts the SendCommunicationInput shape unchanged (COMM-06).
    - tests/comms/inv12-opened.test.ts — invoice detail derives Email Opened by joining latest communication_logs (ref_kind='invoice') (D-13/INV-12).

    Use the `@/` alias (→ src/). Files live in tests/comms/ (mirror-directory, not colocated).
  </action>
  <verify>
    <automated>pnpm test -- comms 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 12 files exist under tests/comms/ and tests/comms/mocks/providers.ts exists.
    - `pnpm test -- comms` runs and reports failures/errors (RED) for the 12 files — none pass, because the target modules do not exist yet.
    - No test uses a watch-mode flag; all run under `vitest run`.
    - Each test file's first line comment names its REQ-ID(s) per the VALIDATION map.
  </acceptance_criteria>
  <done>12 RED comms test files + shared mocks exist and fail as expected under `pnpm test -- comms`.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 4: [BLOCKING] Push schema to Supabase (pnpm db:push)</name>
  <files>drizzle/meta/_journal.json, drizzle/meta/ (new snapshot)</files>
  <read_first>
    - src/db/schema.ts (the 4 tables + enum added in Task 2 must be present before pushing)
    - CLAUDE.md (§Package Manager — pnpm db:push runs drizzle-kit push)
    - drizzle/meta/_journal.json (existing migration journal — a new snapshot will be appended)
  </read_first>
  <action>
    Run `pnpm db:push` to apply the `trigger_type` enum and the four new tables to the live Supabase database. This is BLOCKING: the phase cannot pass verification without it because build and type checks derive types from config, not the live DB, and would pass even if the tables were never created. Accept the drizzle-kit prompts to create the new enum and tables (there are no destructive changes — this is a pure addition). Confirm the push reports the new objects created.
  </action>
  <verify>
    <automated>pnpm db:push 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:push` completes without error and reports the `trigger_type` enum and `communication_triggers`, `communication_logs`, `communication_settings`, `scheduled_sms` tables applied.
    - No destructive/data-loss warnings are accepted (this is additive only).
    - A new snapshot appears under drizzle/meta/ and _journal.json is updated.
  </acceptance_criteria>
  <done>Enum + 4 tables live in Supabase; drizzle snapshot committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | Installing third-party packages introduces supply-chain exposure |
| Drizzle schema → live DB | Schema push mutates production database structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | pnpm add resend/twilio/@react-email/* | mitigate | Task 1 blocking-human legitimacy checkpoint; RESEARCH §Package Legitimacy Audit verified all four against official repos; no [ASSUMED]/[SUS]/[SLOP] packages |
| T-08-01 | Info disclosure | communication_triggers/logs/settings/scheduled_sms tables | mitigate | Every table carries tenant_id + pgPolicy tenant_isolation using current_setting('app.current_tenant_id', true) + .enableRLS(); RLS is the enforcement layer |
| T-08-02 | Tampering | pnpm db:push | mitigate | Additive-only migration (no drops); Task 4 rejects any destructive-change prompt; snapshot journaled for audit |
</threat_model>

<verification>
- `pnpm ls resend twilio @react-email/components @react-email/render` — all four installed.
- `pnpm build` — type-check clean (schema types compile).
- `pnpm db:push` — 4 tables + enum applied to Supabase.
- `pnpm test -- comms` — 12 RED test files fail (targets not yet implemented).
</verification>

<success_criteria>
- Four communications packages installed via pnpm.
- trigger_type enum + 4 RLS-enabled tables in schema.ts AND in the live Supabase DB.
- 12 RED comms test files + shared provider mocks exist and fail as expected.
- Build passes; no destructive schema changes.
</success_criteria>

<output>
Create `.planning/phases/08-communications-and-notifications/08-01-SUMMARY.md` when done.
</output>
