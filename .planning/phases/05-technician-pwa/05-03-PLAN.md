---
phase: 05-technician-pwa
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/app/(tech)/jobs/[id]/page.tsx
  - src/app/(tech)/jobs/actions.ts
  - src/app/(tech)/components/photo-uploader.tsx
  - src/app/(tech)/components/tech-signature-pad.tsx
  - src/app/(tech)/components/completion-notes.tsx
  - src/app/(tech)/components/equipment-section.tsx
  - src/app/(tech)/lib/sync.ts
  - src/lib/jobs/signatures.ts
  - tests/tech/photo-sync.test.ts
  - tests/tech/signature-pad.test.tsx
  - tests/tech/notes-sync.test.ts
  - tests/tech/equipment.test.tsx
autonomous: true
requirements: [TECH-04, TECH-05, TECH-06, TECH-07]
user_setup: []

must_haves:
  truths:
    - "A tech captures a before/after photo from the phone camera; it queues in Dexie and uploads via presigned URL on sync"
    - "A tech draws a customer signature on a touch canvas and saves it; it queues and uploads to Storage, recorded in job_signatures"
    - "A tech adds completion notes that persist to the job (queued offline, synced on reconnect)"
    - "A tech views the service location's equipment and spring specs from the job detail Summary/Equipment view"
  artifacts:
    - path: "src/app/(tech)/components/photo-uploader.tsx"
      provides: "Camera-capture file input + thumbnail + queued badge"
      contains: "capture"
    - path: "src/app/(tech)/components/tech-signature-pad.tsx"
      provides: "signature_pad canvas wrapper with touch-none, Clear, Save Signature"
      contains: "touch-none"
    - path: "src/lib/jobs/signatures.ts"
      provides: "Signature signed-URL upload + confirm to job_signatures (server-only)"
      exports: ["createJobSignatureSignedUploadUrl", "confirmJobSignature"]
    - path: "src/app/(tech)/jobs/actions.ts"
      provides: "saveCompletionNotesAction, signature signed-URL + confirm actions"
      contains: "saveCompletionNotesAction"
  key_links:
    - from: "src/app/(tech)/lib/sync.ts"
      to: "getJobPhotoUploadUrlAction + confirmJobPhotoAction"
      via: "photo outbox flush: signed URL → PUT blob → confirm"
      pattern: "getJobPhotoUploadUrlAction"
    - from: "src/app/(tech)/components/tech-signature-pad.tsx"
      to: "job_signatures"
      via: "save → outbox → signed URL → PUT → confirmJobSignatureAction"
      pattern: "confirmJobSignature"
    - from: "src/app/(tech)/components/equipment-section.tsx"
      to: "getEquipmentByServiceLocation"
      via: "cached Dexie equipment / server fetch"
      pattern: "equipment"
---

<objective>
Complete the on-site job runtime: camera photo capture with presigned-URL sync, touchscreen signature capture stored in the new job_signatures table, completion notes, and read-only equipment/spring specs for the service location. These are the four high-friction field actions that require the phone.

Purpose: A technician can now fully complete a job in the field offline — document with photos, capture the customer signature, write completion notes, and reference equipment specs to quote/order parts on site — with everything queued and auto-synced.

Output: The Photos / Sign / Notes job-detail tabs become functional, an Equipment section renders specs, the signature server module + actions exist, and the sync loop handles photo and signature outbox types.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-technician-pwa/05-CONTEXT.md
@.planning/phases/05-technician-pwa/05-PATTERNS.md
@.planning/phases/05-technician-pwa/05-UI-SPEC.md
@.planning/phases/05-technician-pwa/05-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):
- `PhotoUploader` — React component (src/app/(tech)/components/photo-uploader.tsx)
- `TechSignaturePad` — React component (src/app/(tech)/components/tech-signature-pad.tsx)
- `CompletionNotes` — React component (src/app/(tech)/components/completion-notes.tsx)
- `EquipmentSection` — React component (src/app/(tech)/components/equipment-section.tsx)
- `createJobSignatureSignedUploadUrl(orgId, jobId, filename, fileSize)` / `confirmJobSignature(orgId, jobId, path, signedBy, capturedBy)` (src/lib/jobs/signatures.ts, server-only)
- `getJobSignatureUploadUrlAction`, `confirmJobSignatureAction`, `saveCompletionNotesAction(jobId, notes)` (src/app/(tech)/jobs/actions.ts)
- `syncPhoto`, `syncSignature`, `syncNote` flush handlers (added to src/app/(tech)/lib/sync.ts)
</artifacts_this_phase_produces>

<context_interfaces>
Existing contracts this plan consumes (do NOT redefine):
- `getJobPhotoUploadUrlAction(jobId, filename, fileSize): Promise<{ error?; signedUrl?; path? }>` — src/app/(app)/jobs/actions.ts (lines 1440-1463).
- `confirmJobPhotoAction(jobId, path): Promise<{ error?; success? }>` — src/app/(app)/jobs/actions.ts (lines 1468-1487).
- `createJobPhotoSignedUploadUrl(orgId, jobId, filename, fileSize)` / `confirmJobPhoto(orgId, jobId, path, userId)` — src/lib/jobs/photos.ts (the canonical server-only implementation to MIRROR for signatures, including BUCKET 'tenant-assets', 10 MB cap, ext whitelist, path `${orgId}/jobs/${jobId}/${uuid}.${ext}`).
- `getEquipmentByServiceLocation(orgId, serviceLocationId): Promise<equipment[]>` — src/lib/customers.ts (line 366).
- `jobs.completionNotes` — src/db/schema.ts (text column, line 679).
- `jobSignatures` table + `NewJobSignature` — src/db/schema.ts (added in Plan 01).
- `withTenant(orgId, tx => ...)` — src/db/with-tenant.ts.
- `enqueueOutboxItem`, `flushOutbox` dispatch map — src/app/(tech)/lib/sync.ts (Plan 02).
- `OUTBOX_TYPES` includes 'job_photo', 'job_signature', 'job_note' — src/app/(tech)/lib/dexie.ts (Plan 01).
</context_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Photo capture component + photo outbox sync via presigned URL</name>
  <files>src/app/(tech)/components/photo-uploader.tsx, src/app/(tech)/lib/sync.ts, src/app/(tech)/jobs/[id]/page.tsx, tests/tech/photo-sync.test.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/lib/jobs/photos.ts (createJobPhotoSignedUploadUrl lines 51-86; confirmJobPhoto lines 91-109 — the exact contract)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(app)/jobs/actions.ts (getJobPhotoUploadUrlAction + confirmJobPhotoAction lines 1440-1487)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/lib/sync.ts (Plan 02 flushOutbox dispatch map — add the job_photo handler)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-09/D-10 presigned URL outbox; D-11 auto-sync)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Pattern 2 presigned-URL photo sync lines 241-259)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (Photos tab; Photo capture flow lines 234-240; error copy)
  </read_first>
  <action>
    Create src/app/(tech)/components/photo-uploader.tsx ('use client'): an `<input type="file" accept="image/*" capture="environment">` (native camera) with an Add Photo button; on file selection, read the File into a Blob and enqueue a `job_photo` outbox item with payload `{ jobId, filename: file.name, fileSize: file.size, blob }` and show an immediate thumbnail with a "pending" badge (read pending photo items via useLiveQuery). Render synced photos via getJobPhotoSignedUrls passed from the RSC. Long-press/overflow → Delete confirmation Dialog (UI-SPEC copy "Delete photo?"). Failed uploads show a destructive badge with Retry. Do NOT POST the blob through a server action — only metadata travels through actions (D-09).

    Extend src/app/(tech)/lib/sync.ts flushOutbox dispatch: add a `syncPhoto(item)` handler implementing Pattern 2 — call `getJobPhotoUploadUrlAction(jobId, filename, fileSize)`, then `await fetch(signedUrl, { method: 'PUT', body: blob })`, then `confirmJobPhotoAction(jobId, path)`; mark synced on success, failed + retryCount++ on error.

    Wire PhotoUploader into the Photos tab of src/app/(tech)/jobs/[id]/page.tsx (replace the Plan 02 placeholder for that tab).

    Create tests/tech/photo-sync.test.ts (fake-indexeddb + mocked actions/fetch): enqueue a job_photo outbox item with a small Blob; mock getJobPhotoUploadUrlAction to return { signedUrl, path }, mock global fetch to resolve, mock confirmJobPhotoAction to return success; run flushOutbox and assert the sequence signed-URL → PUT(blob) → confirm(path) occurred in order and the item is marked synced. Assert a failed PUT marks the item 'failed' with retryCount incremented and does NOT call confirm.
  </action>
  <behavior>
    - Selecting a file enqueues a job_photo outbox item containing the blob, filename, and fileSize
    - flushOutbox photo handler calls getJobPhotoUploadUrlAction, then fetch PUT with the blob, then confirmJobPhotoAction(path), in that order
    - A successful sequence marks the item synced; a failed PUT marks it failed and skips confirm
    - The blob never passes through a server action (only filename/fileSize do)
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/photo-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/components/photo-uploader.tsx uses `<input ... capture="environment">` and enqueues a job_photo outbox item with the blob
    - src/app/(tech)/lib/sync.ts has a photo handler that calls getJobPhotoUploadUrlAction → fetch PUT → confirmJobPhotoAction
    - The Photos tab of jobs/[id]/page.tsx renders PhotoUploader
    - `pnpm test --run tests/tech/photo-sync.test.ts` exits 0
  </acceptance_criteria>
  <done>Photos capture from the camera, queue offline with a pending badge, and upload via presigned URL on sync; verified by an ordered-sequence test.</done>
</task>

<task type="auto">
  <name>Task 2: Signature capture (signature_pad) + job_signatures server module + signature outbox sync</name>
  <files>src/app/(tech)/components/tech-signature-pad.tsx, src/lib/jobs/signatures.ts, src/app/(tech)/jobs/actions.ts, src/app/(tech)/lib/sync.ts, src/app/(tech)/jobs/[id]/page.tsx, tests/tech/signature-pad.test.tsx</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/lib/jobs/photos.ts (MIRROR this whole module for signatures — getServiceClient, BUCKET 'tenant-assets', signed upload URL creation lines 51-86, confirm insert lines 91-109; reuse the same path scheme and 10 MB cap)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (jobSignatures table added in Plan 01 — columns storagePath, signedBy, capturedBy)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(app)/jobs/actions.ts (getJobPhotoUploadUrlAction/confirmJobPhotoAction shape to mirror for signature actions; extractErrorMessage; logger usage)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (signature_pad React wrapper lines 482-515; Pitfall 6 touch-action none lines 347-351)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (Sign tab; Signature capture flow lines 242-247; Clear/Save copy; aria-label)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-16 job_signature outbox type addition; Open Question A5)
  </read_first>
  <action>
    Create src/lib/jobs/signatures.ts with `import 'server-only'`, MIRRORING src/lib/jobs/photos.ts: `createJobSignatureSignedUploadUrl(orgId, jobId, filename, fileSize)` (same signed-URL + path scheme `${orgId}/jobs/${jobId}/${uuid}.png`, 10 MB cap, image ext whitelist defaulting to png) and `confirmJobSignature(orgId, jobId, path, signedBy, capturedBy)` (insert into jobSignatures via withTenant with tenantId orgId, jobId, storagePath path, signedBy, capturedBy). Also export `getJobSignatureSignedUrls(orgId, jobId)` mirroring getJobPhotoSignedUrls for read-back.

    Add to src/app/(tech)/jobs/actions.ts ('use server'): `getJobSignatureUploadUrlAction(jobId, filename, fileSize)`, `confirmJobSignatureAction(jobId, path, signedBy)` (reads orgId/userId from auth, passes userId as capturedBy), and `saveCompletionNotesAction(jobId, notes)` (withTenant update of jobs.completionNotes scoped by tenantId + id; revalidate). Mirror the photo actions' auth guard + extractErrorMessage + logger error handling.

    Create src/app/(tech)/components/tech-signature-pad.tsx ('use client') from the RESEARCH wrapper: a `<canvas className="touch-none ... h-48">` (192px) with `aria-label="Customer signature pad"`, new SignaturePad on mount with white background, Clear (outline) and Save Signature (primary) buttons. On Save (when not empty): export `toDataURL('image/png')`, convert to a Blob, enqueue a `job_signature` outbox item with payload `{ jobId, filename: 'signature.png', fileSize, blob, signedBy }`, show optimistic saved thumbnail. Clear triggers the UI-SPEC "Clear signature?" confirm Dialog. Apply touch-action:none (Pitfall 6).

    Extend src/app/(tech)/lib/sync.ts flushOutbox: add `syncSignature(item)` (getJobSignatureUploadUrlAction → PUT blob → confirmJobSignatureAction) and `syncNote(item)` (call saveCompletionNotesAction(jobId, notes)). Wire both into the dispatch map.

    Wire TechSignaturePad into the Sign tab of jobs/[id]/page.tsx; show saved signatures (read-only) below via getJobSignatureSignedUrls from the RSC.

    Create tests/tech/signature-pad.test.tsx: render TechSignaturePad; assert the canvas has aria-label "Customer signature pad" and touch-none class; assert Save is disabled/no-op on an empty pad; simulate a non-empty pad (mock SignaturePad.isEmpty → false and toDataURL → a data URL) and assert Save enqueues a job_signature outbox item with a png blob.
  </action>
  <behavior>
    - The signature canvas has aria-label "Customer signature pad" and the touch-none class
    - Saving an empty pad does nothing (no outbox item)
    - Saving a non-empty pad enqueues a job_signature item with filename 'signature.png' and a png blob
    - flushOutbox signature handler calls getJobSignatureUploadUrlAction → PUT → confirmJobSignatureAction in order
    - confirmJobSignature inserts a row into job_signatures with capturedBy = userId
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/signature-pad.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/jobs/signatures.ts imports 'server-only' and exports createJobSignatureSignedUploadUrl, confirmJobSignature, getJobSignatureSignedUrls mirroring photos.ts
    - confirmJobSignature inserts into jobSignatures via withTenant with capturedBy set to the tech userId
    - src/app/(tech)/jobs/actions.ts exports getJobSignatureUploadUrlAction, confirmJobSignatureAction, saveCompletionNotesAction
    - src/app/(tech)/components/tech-signature-pad.tsx uses signature_pad, touch-none canvas, aria-label, Clear + Save Signature, and enqueues a job_signature outbox item
    - src/app/(tech)/lib/sync.ts handles job_signature and job_note outbox types
    - `pnpm test --run tests/tech/signature-pad.test.tsx` exits 0
  </acceptance_criteria>
  <done>Signatures capture on a touch canvas, store to Storage via signed URL, and record in job_signatures with the tech as capturedBy; offline-queued and synced.</done>
</task>

<task type="auto">
  <name>Task 3: Completion notes + equipment/spring specs section, with notes-sync and equipment tests</name>
  <files>src/app/(tech)/components/completion-notes.tsx, src/app/(tech)/components/equipment-section.tsx, src/app/(tech)/jobs/[id]/page.tsx, tests/tech/notes-sync.test.ts, tests/tech/equipment.test.tsx</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (jobs.completionNotes line 679; equipment table columns — door/opener/spring specs)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/lib/customers.ts (getEquipmentByServiceLocation line 366 — returns equipment rows)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/jobs/actions.ts (saveCompletionNotesAction from Task 2)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/lib/sync.ts (job_note handler from Task 2)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (Notes tab; Summary collapsible Equipment section; Collapsible component add)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-13 Equipment as Summary section or its own tab — planner's call; D-16 job_note outbox)
  </read_first>
  <action>
    Decision (Claude's discretion per CONTEXT D-13): render Equipment as a collapsible section inside the Summary tab (not a separate tab) to keep the 4-tab layout Summary | Photos | Sign | Notes intact. Install the Collapsible primitive if missing via `pnpm dlx shadcn@latest add collapsible` (pnpm only).

    Create src/app/(tech)/components/completion-notes.tsx ('use client'): a `text-base` Textarea for completion notes (16px to avoid iOS zoom) with a Save button. On save: when online call saveCompletionNotesAction(jobId, notes); when offline enqueue a `job_note` outbox item `{ jobId, notes }` and optimistically merge into the local Dexie job row. Show pending/synced state. Use the UI-SPEC "Discard note?" confirm for unsaved discards.

    Create src/app/(tech)/components/equipment-section.tsx ('use client' or RSC): given the job's serviceLocationId, render equipment cards grouped by kind (door / opener / spring) showing the spec fields (brand, model, size, material, style, color for doors; wire size, inside diameter, length, wind direction, cycle rating for springs). Prefer cached Dexie equipment (useLiveQuery) for offline; the RSC can also pass server-fetched equipment from getEquipmentByServiceLocation as the initial source of truth. Read-only (TECH-07).

    Wire CompletionNotes into the Notes tab and EquipmentSection into the Summary tab (collapsible) of jobs/[id]/page.tsx, replacing the Plan 02 placeholders.

    Create tests/tech/notes-sync.test.ts (fake-indexeddb + mocked action): enqueue a job_note item; mock saveCompletionNotesAction success; run flushOutbox and assert it calls saveCompletionNotesAction(jobId, notes) and marks the item synced; assert offline enqueue merges the note into the local Dexie job row.

    Create tests/tech/equipment.test.tsx: seed the Dexie equipment table (fake-indexeddb) with a door + spring record for a serviceLocationId; render EquipmentSection and assert both the door brand and the spring wire-size spec appear.
  </action>
  <behavior>
    - Saving notes offline enqueues a job_note outbox item and merges into the local job row
    - flushOutbox note handler calls saveCompletionNotesAction(jobId, notes) and marks synced
    - EquipmentSection renders door specs and spring specs from cached Dexie equipment
    - Equipment view is read-only (no edit controls)
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/notes-sync.test.ts tests/tech/equipment.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/components/completion-notes.tsx uses a text-base Textarea and routes through saveCompletionNotesAction (online) / job_note outbox (offline)
    - src/app/(tech)/components/equipment-section.tsx renders door and spring specs read-only from getEquipmentByServiceLocation / cached Dexie equipment
    - jobs/[id]/page.tsx Notes tab renders CompletionNotes and Summary tab renders the collapsible EquipmentSection
    - `pnpm test --run tests/tech/notes-sync.test.ts tests/tech/equipment.test.tsx` exits 0
  </acceptance_criteria>
  <done>Completion notes persist (online and offline-queued) and the technician can read the service location's equipment and spring specs on the job detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA → Supabase Storage (direct PUT) | Blob uploaded directly to signed URL; URL must be tenant-scoped |
| PWA → signature/notes server actions | Tech-submitted content crosses; server validates orgId/userId and writes via withTenant |
| Dexie equipment cache → UI | Cached specs must be tenant-partitioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Photo signed-URL path | mitigate | getJobPhotoUploadUrlAction issues server-side signed URLs scoped to `${orgId}/jobs/${jobId}/...`; 10 MB cap + image ext whitelist in createJobPhotoSignedUploadUrl |
| T-05-05 | Tampering | Signature storage path + DB row | mitigate | createJobSignatureSignedUploadUrl mirrors the tenant-scoped path scheme; confirmJobSignature inserts via withTenant under jobSignatures RLS; capturedBy = authenticated userId |
| T-05-12 | Information Disclosure | Equipment cache cross-tenant | mitigate | getEquipmentByServiceLocation runs withTenant(orgId); cached equipment rows carry tenantId and live in the per-org Dexie DB |
| T-05-13 | Denial of Service | Oversized file upload | mitigate | 10 MB cap enforced in the signed-URL server module; MIME/ext whitelist |
| T-05-14 | Input Validation | Completion notes / signedBy | mitigate | saveCompletionNotesAction / confirmJobSignatureAction validate non-empty + length server-side and write only scoped columns via withTenant |
</threat_model>

<verification>
- `pnpm test --run tests/tech/photo-sync.test.ts tests/tech/signature-pad.test.tsx tests/tech/notes-sync.test.ts tests/tech/equipment.test.tsx` all pass
- src/lib/jobs/signatures.ts mirrors photos.ts (server-only, tenant-scoped path, 10 MB cap)
- No blob travels through any server action (photos/signatures use signed URLs)
- Full suite: `pnpm test` green
</verification>

<success_criteria>
- A tech captures camera photos that queue and upload via presigned URL
- A tech draws and saves a customer signature recorded in job_signatures with capturedBy
- A tech adds completion notes that persist online and queue offline
- A tech views read-only equipment and spring specs for the service location
</success_criteria>

<output>
Create `.planning/phases/05-technician-pwa/05-03-SUMMARY.md` when done.
</output>
