---
phase: 05-technician-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - src/app/sw.ts
  - src/app/(tech)/manifest.json
  - src/app/(tech)/layout.tsx
  - src/app/(tech)/offline/page.tsx
  - src/app/(tech)/components/bottom-nav.tsx
  - src/app/(tech)/lib/dexie.ts
  - src/middleware.ts
  - src/db/schema.ts
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - tests/tech/idb-setup.ts
  - tests/tech/dexie.test.ts
  - tests/tech/status-sheet.test.tsx
  - vitest.config.ts
  - .gitignore
autonomous: false
requirements: [TECH-01, TECH-08, TECH-14]
user_setup: []

must_haves:
  truths:
    - "A technician (org:technician) signing in is redirected to /tech/jobs by middleware"
    - "A dispatcher requesting /tech/* is redirected to / (excluded); admins may visit /tech/* manually"
    - "pnpm build emits public/sw.js scoped to /tech/ and a reachable /tech/manifest.json with start_url /tech/jobs"
    - "The Dexie TechSyncDb opens with jobs, serviceLocations, equipment, and outbox tables, partitioned per org"
    - "The job_signatures table exists in the database after pnpm db:push"
  artifacts:
    - path: "src/app/sw.ts"
      provides: "Serwist service worker source scoped to /tech/ with offline fallback"
      contains: "Serwist"
    - path: "src/app/(tech)/manifest.json"
      provides: "PWA manifest with start_url /tech/jobs, display standalone"
      contains: "/tech/jobs"
    - path: "src/app/(tech)/layout.tsx"
      provides: "Mobile shell with bottom nav and Technician/Admin auth wrapper"
      exports: ["default"]
    - path: "src/app/(tech)/lib/dexie.ts"
      provides: "TechSyncDb Dexie class + OutboxItem/CachedJob/CachedLocation/CachedEquipment types"
      exports: ["createTechDb", "OutboxItem", "CachedJob"]
    - path: "src/db/schema.ts"
      provides: "jobSignatures table"
      contains: "job_signatures"
  key_links:
    - from: "src/middleware.ts"
      to: "/tech/jobs"
      via: "NextResponse.redirect for org:technician"
      pattern: "tech/jobs"
    - from: "next.config.ts"
      to: "src/app/sw.ts"
      via: "withSerwistInit swSrc"
      pattern: "swSrc"
---

<objective>
Build the PWA foundation for the technician app: install Serwist + Dexie + signature_pad + fake-indexeddb, wire the service worker and manifest scoped to /tech/, replace the technician "coming soon" middleware redirect with the real /tech/* role gate, create the mobile shell layout with bottom navigation, define the Dexie offline database (cache tables + outbox), and add the jobSignatures table with a blocking schema push.

Purpose: Every later Phase 5 wave (schedule, status, photos, signature, estimates, invoices, payments) reads from the Dexie outbox and renders inside this mobile shell. Without the shell, manifest, service worker, role gate, and outbox schema, nothing downstream is reachable or offline-capable.

Output: An installable, offline-capable /tech/ route group with a working bottom nav, an empty-state Jobs/Estimates/Invoices shell, a per-org Dexie database, and the jobSignatures table in the database.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-technician-pwa/05-CONTEXT.md
@.planning/phases/05-technician-pwa/05-RESEARCH.md
@.planning/phases/05-technician-pwa/05-PATTERNS.md
@.planning/phases/05-technician-pwa/05-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):
- `createTechDb(orgId: string)` — factory returning a per-org Dexie TechSyncDb instance (src/app/(tech)/lib/dexie.ts)
- `TechSyncDb` — Dexie subtype with `jobs`, `serviceLocations`, `equipment`, `outbox` EntityTables
- `OutboxItem` — interface `{ id, type, payload, createdAt, retryCount, error?, syncStatus }`
- `CachedJob`, `CachedLocation`, `CachedEquipment` — Dexie cache row interfaces
- `OUTBOX_TYPES` — union/const of `'job_status_update' | 'job_note' | 'job_photo' | 'job_signature' | 'estimate_create' | 'estimate_conversion' | 'invoice_create' | 'send_record' | 'manual_payment'`
- `BottomNav` — React component (src/app/(tech)/components/bottom-nav.tsx)
- `TechLayout` (default export of src/app/(tech)/layout.tsx)
- `jobSignatures` Drizzle table + `JobSignature` / `NewJobSignature` types (src/db/schema.ts)
- `isTechRoute` route matcher in src/middleware.ts
- `src/app/sw.ts`, `src/app/(tech)/manifest.json`, `src/app/(tech)/offline/page.tsx`
- `tests/tech/idb-setup.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install PWA dependencies and wire Serwist service worker + manifest + offline page scoped to /tech/</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, src/app/sw.ts, src/app/(tech)/manifest.json, src/app/(tech)/offline/page.tsx, public/icons/icon-192.png, public/icons/icon-512.png, .gitignore</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Serwist next.config.ts example lines 368-416; manifest/structure lines 176-209)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (No Analog Found section — manifest/sw.ts guidance)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/next.config.ts (current config — wrap, do not replace existing options)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.gitignore (add public/sw.js + generated SW assets)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/CLAUDE.md (pnpm only — never npm/npx/yarn)
  </read_first>
  <action>
    Install with pnpm only: run `pnpm add @serwist/next@9.5.11 serwist@9.5.11 signature_pad@5.1.3 dexie@4.4.4 dexie-react-hooks@4.4.4` and `pnpm add -D fake-indexeddb@6.2.5`. NEVER use npm, npx, or yarn.

    Wrap next.config.ts with `withSerwistInit` from `@serwist/next` using: swSrc `src/app/sw.ts`, swDest `public/sw.js`, swUrl `/tech/sw.js`, scope `/tech/`, register true, cacheOnNavigation true. Preserve all existing nextConfig options by spreading them into the wrapped config (read the current file first; do not drop existing keys).

    Create src/app/sw.ts following the RESEARCH.md Serwist example: import `defaultCache` from `@serwist/next/worker`, declare the `__SW_MANIFEST` global, construct a `Serwist` instance with precacheEntries `self.__SW_MANIFEST`, skipWaiting true, clientsClaim true, navigationPreload true, runtimeCaching `defaultCache`, and a document fallback entry pointing to `/tech/offline`. Call `serwist.addEventListeners()`.

    Create src/app/(tech)/manifest.json (static JSON, NOT a manifest.ts) with: name "TorsionDesk Tech", short_name "TD Tech", start_url "/tech/jobs", scope "/tech/", display "standalone", background_color and theme_color referencing the project neutral palette (use "#0a0a0a" background / "#fafafa" or appropriate neutral values consistent with globals.css), and icons array referencing /icons/icon-192.png (192x192) and /icons/icon-512.png (512x512), each with purpose "any maskable".

    Generate placeholder PWA icons at public/icons/icon-192.png (192x192) and public/icons/icon-512.png (512x512). Generate solid neutral PNGs with the letters "TD" if no logo asset is available; these are placeholders the owner can replace later.

    Create src/app/(tech)/offline/page.tsx — a minimal RSC showing the UI-SPEC offline copy: heading "You're offline" and body "Some features are unavailable until you're back online. Your queued changes will sync automatically." Use existing tokens (bg-background, text-muted-foreground) and centered layout with py-16 spacing.

    Add `public/sw.js`, `public/sw.js.map`, and `public/swe-worker-*.js` to .gitignore (Serwist-generated artifacts).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -iv "warn" | grep -i "compiled successfully\|✓ Compiled\|Generating static" ; test -f public/sw.js && echo "SW_GENERATED"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include @serwist/next, serwist, signature_pad, dexie, dexie-react-hooks and devDependencies include fake-indexeddb
    - next.config.ts imports withSerwistInit and exports the result of wrapping the existing config with swSrc 'src/app/sw.ts', scope '/tech/', swUrl '/tech/sw.js'
    - src/app/sw.ts exists and contains `new Serwist(` with a fallback entry for '/tech/offline'
    - src/app/(tech)/manifest.json contains `"start_url": "/tech/jobs"` and `"scope": "/tech/"` and `"display": "standalone"`
    - public/icons/icon-192.png and public/icons/icon-512.png exist
    - src/app/(tech)/offline/page.tsx exists and exports a default component containing the text "You're offline"
    - `pnpm build` exits 0 and produces public/sw.js
    - .gitignore contains public/sw.js
  </acceptance_criteria>
  <done>pnpm build succeeds, generates public/sw.js, and the /tech/ manifest + offline fallback are in place; no npm/npx/yarn was used.</done>
</task>

<task type="auto">
  <name>Task 2: Define Dexie offline database (cache tables + outbox) with per-org partitioning, plus RED unit tests</name>
  <files>src/app/(tech)/lib/dexie.ts, tests/tech/idb-setup.ts, tests/tech/dexie.test.ts, vitest.config.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Dexie schema example lines 418-479; outbox interface lines 222-239; Pitfall 8 tenant partitioning lines 359-363)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-16 outbox types lines 47-54; D-17 outbox item schema)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (dexie.ts section lines 377-401; tests/setup.ts section lines 435-456)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/tests/setup.ts (current single-line setup file)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/vitest.config.ts (current config — extend setupFiles, do not break existing alias array)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (CachedJob/CachedLocation/CachedEquipment field shapes must mirror jobs/serviceLocations/equipment columns — id and tenantId are text)
  </read_first>
  <action>
    Create src/app/(tech)/lib/dexie.ts exporting: the `OutboxItem` interface with fields `id: string` (client UUID), `type` (the OUTBOX_TYPES union from CONTEXT D-16 plus 'job_signature' and 'manual_payment'), `payload: unknown`, `createdAt: number`, `retryCount: number`, `error?: string`, `syncStatus: 'pending' | 'syncing' | 'synced' | 'failed'`; the `CachedJob`, `CachedLocation`, `CachedEquipment` interfaces matching the RESEARCH example (CachedLocation MUST include latitude, longitude, state, postalCode per the STATE.md Dexie schema gap); a `TechSyncDb` Dexie subtype with EntityTables jobs, serviceLocations, equipment, outbox; and a `createTechDb(orgId: string)` factory that constructs `new Dexie(\`TechSyncDb-${orgId}\`)` (per-org partition per Pitfall 8), declares version(1).stores exactly as the RESEARCH example (jobs: 'id, tenantId, status, startDate', serviceLocations: 'id, tenantId, customerId', equipment: 'id, tenantId, serviceLocationId', outbox: 'id, type, syncStatus, createdAt'), and returns the typed instance. Also export `OUTBOX_TYPES` as a const array of the type strings. Do not store any tenant data in a shared/global DB name; the org id must be in the database name.

    Create tests/tech/idb-setup.ts importing `'fake-indexeddb/auto'` and `'@testing-library/jest-dom/vitest'`, with a structuredClone polyfill guard (per PATTERNS.md).

    Update vitest.config.ts setupFiles to include both './tests/setup.ts' and './tests/tech/idb-setup.ts' (append; keep the existing entry). Do not modify the alias array.

    Create tests/tech/dexie.test.ts (RED — will pass once dexie.ts exists): assert createTechDb('org_A') returns a DB whose name includes 'org_A'; assert adding an OutboxItem with syncStatus 'pending' and reading it back via outbox.where('syncStatus').equals('pending').toArray() returns the item; assert a CachedJob added under tenantId 'org_A' is queryable by status; assert two DBs created with different org ids have different names (tenant partitioning). Clean up with db.delete() in afterEach.
  </action>
  <behavior>
    - createTechDb('org_A').name === 'TechSyncDb-org_A'
    - After outbox.add({ id, type: 'job_status_update', payload: {...}, createdAt: Date.now(), retryCount: 0, syncStatus: 'pending' }), outbox.where('syncStatus').equals('pending').toArray() has length 1
    - CachedJob inserted with status 'on_site' is returned by jobs.where('status').equals('on_site').toArray()
    - createTechDb('org_A').name !== createTechDb('org_B').name
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/dexie.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/lib/dexie.ts exports createTechDb, OutboxItem, CachedJob, CachedLocation, CachedEquipment, OUTBOX_TYPES
    - CachedLocation interface includes latitude, longitude, state, postalCode fields
    - createTechDb embeds orgId in the Dexie database name (`TechSyncDb-${orgId}`)
    - tests/tech/idb-setup.ts imports 'fake-indexeddb/auto'
    - vitest.config.ts setupFiles array includes both './tests/setup.ts' and './tests/tech/idb-setup.ts'
    - `pnpm test --run tests/tech/dexie.test.ts` exits 0 with all assertions passing
  </acceptance_criteria>
  <done>The per-org Dexie database with cache tables and the outbox is defined and verified by passing unit tests using fake-indexeddb.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace technician middleware redirect with the real /tech/* role gate, build the mobile shell layout + bottom nav, and add a status-sheet RED test</name>
  <files>src/middleware.ts, src/app/(tech)/layout.tsx, src/app/(tech)/components/bottom-nav.tsx, src/app/(tech)/jobs/page.tsx, src/app/(tech)/estimates/page.tsx, src/app/(tech)/invoices/page.tsx, tests/tech/status-sheet.test.tsx</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/middleware.ts (CURRENT file redirects org:technician to /mobile-coming-soon — this MUST be replaced; the PATTERNS.md excerpt is stale)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-04 route group, D-05 access control, D-06 three tabs, D-07 auto-redirect, D-08 manifest start_url)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (layout.tsx section lines 85-119; bottom-nav.tsx section lines 460-500)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(app)/layout.tsx (office layout analog for auth wrapper)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (Mobile Layout & Navigation section; bottom tab table; empty-state copy)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/lib/jobs/transitions.ts (ALLOWED_TRANSITIONS for the status-sheet RED test)
  </read_first>
  <action>
    Edit src/middleware.ts per D-05/D-07: add `const isTechRoute = createRouteMatcher(['/tech(.*)'])`. REPLACE the existing block that redirects `org:technician` to `/mobile-coming-soon` with this logic, evaluated after `await auth.protect()` and reading `{ orgRole }`:
    (1) If `isTechRoute(req)` AND orgRole is NOT 'org:technician' AND orgRole is NOT 'org:admin' → `return NextResponse.redirect(new URL('/', req.url))` (dispatchers and other roles excluded).
    (2) If NOT `isTechRoute(req)` AND orgRole === 'org:technician' → `return NextResponse.redirect(new URL('/tech/jobs', req.url))` (auto-redirect per D-07).
    (3) Admins land in the office app and are NOT redirected; they may navigate to /tech/* manually (no redirect for org:admin on either branch).
    Keep the existing `/settings` admin-only gate and the public-route opt-out unchanged. Remove the `/mobile-coming-soon` reference.

    Create src/app/(tech)/layout.tsx as an async RSC default export `TechLayout({ children })`: call `const { orgRole, userId } = await auth()`; root element `<div className="flex flex-col h-[100dvh] bg-background">`; a top app bar slot (`h-14` — leave room for the offline/sync badge added in a later wave; render a simple title for now), a `<main className="flex-1 overflow-y-auto pb-16">` for children, and `<BottomNav />` fixed at the bottom. Export a Next.js `viewport` (width=device-width, initial-scale=1, viewport-fit=cover) and `metadata` referencing the manifest. Do NOT set maximum-scale or user-scalable=no (accessibility, per UI-SPEC).

    Create src/app/(tech)/components/bottom-nav.tsx as the 'use client' component from PATTERNS.md: three tabs Jobs (/tech/jobs, Briefcase icon), Estimates (/tech/estimates, ClipboardList icon), Invoices (/tech/invoices, Receipt icon) from lucide-react; fixed bottom-0, h-16, pb-[env(safe-area-inset-bottom)], bg-card/95 backdrop-blur border-t; active tab uses text-primary semibold via usePathname startsWith, inactive uses text-muted-foreground; use cn() from @/lib/utils.

    Create the three empty-state shell pages: src/app/(tech)/jobs/page.tsx, src/app/(tech)/estimates/page.tsx, src/app/(tech)/invoices/page.tsx. Each is an async RSC with the auth guard (`const { orgId, userId } = await auth(); if (!orgId || !userId) redirect('/sign-in')`) rendering the UI-SPEC empty-state copy for that tab (Jobs: "No jobs assigned"; Estimates: "No estimates"; Invoices: "No invoices") using text-2xl semibold heading + text-base muted body, centered with py-12. These are placeholders that Wave 1+ replaces with real data; they exist so all three tabs render now per D-06.

    Create tests/tech/status-sheet.test.tsx (RED — covers TECH-03 contract, full component lands in Plan 02): assert that given currentStatus 'on_site', the set of legal next statuses equals ALLOWED_TRANSITIONS['on_site'] from @/lib/jobs/transitions, and that an illegal status (e.g. 'paid_in_full') is NOT in that list. This test imports ALLOWED_TRANSITIONS directly and asserts the data contract the bottom sheet will render.
  </action>
  <behavior>
    - ALLOWED_TRANSITIONS['on_site'] includes 'started', 'paused', 'completed', 'partially_completed'
    - ALLOWED_TRANSITIONS['on_site'] does NOT include 'paid_in_full' or 'invoiced'
    - BottomNav renders exactly 3 links: /tech/jobs, /tech/estimates, /tech/invoices
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/status-sheet.test.tsx && grep -L "mobile-coming-soon" src/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/middleware.ts no longer contains the string "mobile-coming-soon"
    - src/middleware.ts contains `createRouteMatcher(['/tech(.*)'])` (or equivalent isTechRoute matcher) and a redirect to '/tech/jobs' for org:technician on non-tech routes
    - src/middleware.ts redirects non-technician/non-admin roles away from /tech/* to '/'
    - src/app/(tech)/layout.tsx exists and exports default TechLayout rendering BottomNav
    - src/app/(tech)/components/bottom-nav.tsx renders 3 tabs linking to /tech/jobs, /tech/estimates, /tech/invoices
    - src/app/(tech)/jobs/page.tsx, estimates/page.tsx, invoices/page.tsx each exist with the auth guard and the correct empty-state heading
    - `pnpm test --run tests/tech/status-sheet.test.tsx` exits 0
  </acceptance_criteria>
  <done>Technicians are routed into /tech/jobs, dispatchers are excluded from /tech/*, the mobile shell with bottom nav renders all three tabs with empty states, and the status-sheet data contract test passes.</done>
</task>

<task type="auto">
  <name>Task 4: Add jobSignatures table to schema</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (jobPhotos table lines 1073-1103 — mirror EXACTLY: text id with gen_random_uuid default, tenantId text, composite unique on (tenantId, id), composite FK (tenantId, jobId) → (jobs.tenantId, jobs.id) onDelete cascade, pgPolicy tenant isolation, enableRLS; also read the jobs table lines 644-656 for the FK target columns)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (jobSignatures table section lines 404-431; note PATTERNS shows uuid() but the REAL jobPhotos uses text id + gen_random_uuid — follow the REAL schema)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Open Question A5 — signatures get their own table + outbox type)
  </read_first>
  <action>
    Add a `jobSignatures` pgTable named 'job_signatures' to src/db/schema.ts immediately after the jobPhotos table, mirroring the jobPhotos definition EXACTLY (it uses `text('id').primaryKey().default(sql\`gen_random_uuid()\`)`, NOT `uuid()`): columns id (text PK, gen_random_uuid default), tenantId (text notNull), jobId (text notNull references jobs.id onDelete cascade), storagePath (text notNull), signedBy (text — customer name free-text), capturedBy (text — userId of the tech), createdAt (timestamp defaultNow), updatedAt (timestamp defaultNow). Add the same table constraints as jobPhotos: `unique('job_signatures_tenant_id_unique').on(t.tenantId, t.id)`, composite `foreignKey({ columns: [t.tenantId, t.jobId], foreignColumns: [jobs.tenantId, jobs.id] }).onDelete('cascade')`, and `pgPolicy('job_signatures_tenant_isolation', { for: 'all', to: 'authenticated', using/withCheck on tenantId = current_setting('app.current_tenant_id', true) })`. Call `.enableRLS()`. Export `JobSignature` and `NewJobSignature` inferred types.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -i "schema.ts" || echo "SCHEMA_TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts contains `pgTable('job_signatures'` with a text id defaulting to gen_random_uuid()
    - The jobSignatures table has a composite foreignKey on (tenantId, jobId) referencing (jobs.tenantId, jobs.id) with onDelete cascade
    - The jobSignatures table has a pgPolicy tenant-isolation row policy and calls .enableRLS()
    - src/db/schema.ts exports JobSignature and NewJobSignature types
    - tsc --noEmit reports no errors in schema.ts
  </acceptance_criteria>
  <done>The jobSignatures table mirrors the jobPhotos RLS-enabled pattern and type-checks clean.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 5: [BLOCKING] Push schema to database (pnpm db:push)</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (confirm the jobSignatures table from Task 4 is present before pushing)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/package.json (db:push script = drizzle-kit push)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/CLAUDE.md (pnpm only — NEVER npx/npm/yarn for drizzle-kit)
  </read_first>
  <action>
    [BLOCKING] After Task 4 has added the jobSignatures table, run `pnpm db:push` (which runs drizzle-kit push) to migrate the database. pnpm ONLY — never npx, npm, or yarn. drizzle-kit may prompt interactively to confirm creating the job_signatures table; review the diff and accept the create (this is why the plan is autonomous: false). Downstream Plan 03 signature capture writes rows into this table, so the push MUST complete before phase verification. Do not proceed to verification if the push does not exit 0.
  </action>
  <verify>
    <automated>pnpm db:push 2>&1 | tail -5; echo "DB_PUSH_DONE"</automated>
    <human-check>Confirm drizzle-kit reported the job_signatures table created (or schema already in sync) and the command exited 0.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm db:push` exits 0
    - drizzle-kit reports the job_signatures table created (or schema already in sync)
    - No npm/npx/yarn was used
  </acceptance_criteria>
  <done>The job_signatures table exists in the database; downstream signature capture can write rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser PWA → middleware | Untrusted role claim crosses; middleware re-reads the verified Clerk session role |
| browser → IndexedDB (Dexie) | Cached tenant data lives client-side; must be partitioned per org |
| service worker → cache | SW controls fetch; scope must be limited to /tech/ so it cannot serve office routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Service worker scope | mitigate | Set scope '/tech/' and swUrl '/tech/sw.js' in next.config.ts; manifest start_url '/tech/jobs'; SW fallback only for /tech/offline so it never precaches office routes (RESEARCH Pitfall 4) |
| T-05-02 | Elevation of Privilege | /tech/* route gate | mitigate | Middleware allows only org:technician + org:admin into /tech/*; all others redirected to / (D-05); enforced server-side before render, not nav hiding |
| T-05-08 | Information Disclosure | Dexie cache cross-tenant | mitigate | createTechDb embeds orgId in the DB name (`TechSyncDb-${orgId}`); every cache row carries tenantId; DB cleared on sign-out (later wave) (RESEARCH Pitfall 8) |
| T-05-SI | Tampering | jobSignatures RLS | mitigate | jobSignatures has tenant-isolation pgPolicy + composite FK + enableRLS, mirroring jobPhotos |
| T-05-SC | Tampering | pnpm installs (serwist, dexie, signature_pad, fake-indexeddb) | accept | RESEARCH Package Legitimacy Audit returned slopcheck [OK] for all four packages; no [ASSUMED]/[SUS]/[SLOP] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `pnpm build` exits 0 and emits public/sw.js
- `pnpm test --run tests/tech/dexie.test.ts` and `pnpm test --run tests/tech/status-sheet.test.tsx` pass
- `pnpm db:push` creates the job_signatures table
- src/middleware.ts contains no "mobile-coming-soon" reference and gates /tech/* to technician+admin
- Full suite: `pnpm test` green
</verification>

<success_criteria>
- The /tech/ route group is installable (manifest + service worker scoped to /tech/) and renders an offline fallback
- A technician signing in lands on /tech/jobs; dispatchers cannot reach /tech/*; admins can navigate there manually
- The per-org Dexie database (jobs, serviceLocations, equipment, outbox) opens and round-trips an outbox item in tests
- The jobSignatures table exists in the database after the blocking push
- All three bottom-nav tabs render with empty states
</success_criteria>

<output>
Create `.planning/phases/05-technician-pwa/05-01-SUMMARY.md` when done.
</output>
