---
phase: 05-technician-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - src/app/sw.ts
  - src/app/(tech)/manifest.json
  - src/app/(tech)/offline/page.tsx
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - src/app/(tech)/lib/dexie.ts
  - tests/tech/idb-setup.ts
  - tests/tech/dexie.test.ts
  - vitest.config.ts
  - .gitignore
autonomous: false
requirements: [TECH-01, TECH-08]
user_setup: []

must_haves:
  truths:
    - "pnpm build emits public/sw.js scoped to /tech/ and a reachable /tech/manifest.json with start_url /tech/jobs"
    - "The Dexie TechSyncDb opens with jobs, serviceLocations, equipment, and outbox tables, partitioned per org"
    - "An OutboxItem with syncStatus 'pending' round-trips through the outbox table in tests using fake-indexeddb"
  artifacts:
    - path: "src/app/sw.ts"
      provides: "Serwist service worker source scoped to /tech/ with offline fallback"
      contains: "Serwist"
    - path: "src/app/(tech)/manifest.json"
      provides: "PWA manifest with start_url /tech/jobs, display standalone"
      contains: "/tech/jobs"
    - path: "src/app/(tech)/lib/dexie.ts"
      provides: "TechSyncDb Dexie class + OutboxItem/CachedJob/CachedLocation/CachedEquipment types"
      exports: ["createTechDb", "OutboxItem", "CachedJob"]
  key_links:
    - from: "next.config.ts"
      to: "src/app/sw.ts"
      via: "withSerwistInit swSrc"
      pattern: "swSrc"
    - from: "src/app/(tech)/lib/dexie.ts"
      to: "outbox table"
      via: "createTechDb version(1).stores"
      pattern: "outbox"
---

<objective>
Stand up the PWA install + offline-storage foundation for the technician app: install Serwist + Dexie + signature_pad + fake-indexeddb, wire the service worker and manifest scoped to /tech/, add the offline fallback page, and define the per-org Dexie offline database (cache tables + outbox) with passing unit tests.

Purpose: Every later Phase 5 wave reads from the Dexie outbox and runs inside an installable, offline-capable /tech/ shell. The service worker, manifest, and outbox schema must exist before the route gate, shell, and downstream runtime slices (built in Plan 01b and beyond) can be reachable or offline-capable.

Output: An installable, offline-capable /tech/ origin (manifest + service worker + offline fallback) and a per-org Dexie database (jobs, serviceLocations, equipment, outbox) verified by unit tests.

This plan was split from the original 05-01 (which carried 5 tasks / 18 files) into 05-01 (deps + Serwist + Dexie) and 05-01b (middleware gate + mobile shell + jobSignatures push) to stay within scope thresholds. 05-01b runs in the same wave but depends_on 05-01.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-technician-pwa/05-CONTEXT.md
@.planning/phases/05-technician-pwa/05-RESEARCH.md
@.planning/phases/05-technician-pwa/05-PATTERNS.md
@.planning/phases/05-technician-pwa/05-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):
- `createTechDb(orgId: string)` — factory returning a per-org Dexie TechSyncDb instance (src/app/(tech)/lib/dexie.ts)
- `TechSyncDb` — Dexie subtype with `jobs`, `serviceLocations`, `equipment`, `outbox` EntityTables
- `OutboxItem` — interface `{ id, type, payload, createdAt, retryCount, error?, syncStatus }`
- `CachedJob`, `CachedLocation`, `CachedEquipment` — Dexie cache row interfaces
- `OUTBOX_TYPES` — union/const of `'job_status_update' | 'job_note' | 'job_photo' | 'job_signature' | 'estimate_create' | 'estimate_conversion' | 'invoice_create' | 'send_record' | 'manual_payment'`
- `src/app/sw.ts`, `src/app/(tech)/manifest.json`, `src/app/(tech)/offline/page.tsx`
- `tests/tech/idb-setup.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install PWA dependencies and wire Serwist service worker + manifest + offline page scoped to /tech/</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, src/app/sw.ts, src/app/(tech)/manifest.json, src/app/(tech)/offline/page.tsx, public/icons/icon-192.png, public/icons/icon-512.png, .gitignore</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Serwist next.config.ts example lines 368-416; manifest/structure lines 176-209)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (No Analog Found section — manifest/sw.ts guidance)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/next.config.ts (current config — wrap, do not replace existing options)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.gitignore (add public/sw.js + generated SW assets)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/CLAUDE.md (pnpm only — never npm/npx/yarn)
  </read_first>
  <action>
    Install with pnpm only: run `pnpm add @serwist/next@9.5.11 serwist@9.5.11 signature_pad@5.1.3 dexie@4.4.4 dexie-react-hooks@4.4.4` and `pnpm add -D fake-indexeddb@6.2.5`. NEVER use npm, npx, or yarn.

    Wrap next.config.ts with `withSerwistInit` from `@serwist/next` using: swSrc `src/app/sw.ts`, swDest `public/sw.js`, swUrl `/tech/sw.js`, scope `/tech/`, register true, cacheOnNavigation true. Preserve all existing nextConfig options by spreading them into the wrapped config (read the current file first; do not drop existing keys).

    Create src/app/sw.ts following the RESEARCH.md Serwist example: import `defaultCache` from `@serwist/next/worker`, declare the `__SW_MANIFEST` global, construct a `Serwist` instance with precacheEntries `self.__SW_MANIFEST`, skipWaiting true, clientsClaim true, navigationPreload true, runtimeCaching `defaultCache`, and a document fallback entry pointing to `/tech/offline`. Call `serwist.addEventListeners()`.

    Create src/app/(tech)/manifest.json (static JSON, NOT a manifest.ts) with: name "TorsionDesk Tech", short_name "TD Tech", start_url "/tech/jobs", scope "/tech/", display "standalone", background_color and theme_color referencing the project neutral palette (use "#0a0a0a" background / "#fafafa" or appropriate neutral values consistent with globals.css), and icons array referencing /icons/icon-192.png (192x192) and /icons/icon-512.png (512x512), each with purpose "any maskable".

    Generate placeholder PWA icons at public/icons/icon-192.png (192x192) and public/icons/icon-512.png (512x512). Generate solid neutral PNGs with the letters "TD" if no logo asset is available; these are placeholders the owner can replace later.

    Create src/app/(tech)/offline/page.tsx — a minimal RSC showing the UI-SPEC offline copy: heading "You're offline" and body "Some features are unavailable until you're back online. Your queued changes will sync automatically." Use existing tokens (bg-background, text-muted-foreground) and centered layout with py-16 spacing.

    Add `public/sw.js`, `public/sw.js.map`, and `public/swe-worker-*.js` to .gitignore (Serwist-generated artifacts).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | grep -iv "warn" | grep -i "compiled successfully\|✓ Compiled\|Generating static" ; test -f public/sw.js && echo "SW_GENERATED"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include @serwist/next, serwist, signature_pad, dexie, dexie-react-hooks and devDependencies include fake-indexeddb
    - next.config.ts imports withSerwistInit and exports the result of wrapping the existing config with swSrc 'src/app/sw.ts', scope '/tech/', swUrl '/tech/sw.js'
    - src/app/sw.ts exists and contains `new Serwist(` with a fallback entry for '/tech/offline'
    - src/app/(tech)/manifest.json contains `"start_url": "/tech/jobs"` and `"scope": "/tech/"` and `"display": "standalone"`
    - public/icons/icon-192.png and public/icons/icon-512.png exist
    - src/app/(tech)/offline/page.tsx exists and exports a default component containing the text "You're offline"
    - `pnpm build` exits 0 and produces public/sw.js
    - .gitignore contains public/sw.js
  </acceptance_criteria>
  <done>pnpm build succeeds, generates public/sw.js, and the /tech/ manifest + offline fallback are in place; no npm/npx/yarn was used.</done>
</task>

<task type="auto">
  <name>Task 2: Define Dexie offline database (cache tables + outbox) with per-org partitioning, plus RED unit tests</name>
  <files>src/app/(tech)/lib/dexie.ts, tests/tech/idb-setup.ts, tests/tech/dexie.test.ts, vitest.config.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Dexie schema example lines 418-479; outbox interface lines 222-239; Pitfall 8 tenant partitioning lines 359-363; Open Question A5 — signatures get their own outbox type)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-16 outbox types lines 47-54; D-17 outbox item schema; D-03 Square card payments never queued)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-PATTERNS.md (dexie.ts section lines 377-401; tests/setup.ts section lines 435-456)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/tests/setup.ts (current single-line setup file)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/vitest.config.ts (current config — extend setupFiles, do not break existing alias array)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/db/schema.ts (CachedJob/CachedLocation/CachedEquipment field shapes must mirror jobs/serviceLocations/equipment columns — id and tenantId are text)
  </read_first>
  <action>
    Create src/app/(tech)/lib/dexie.ts exporting: the `OutboxItem` interface with fields `id: string` (client UUID), `type` (the OUTBOX_TYPES union), `payload: unknown`, `createdAt: number`, `retryCount: number`, `error?: string`, `syncStatus: 'pending' | 'syncing' | 'synced' | 'failed'`; the `CachedJob`, `CachedLocation`, `CachedEquipment` interfaces matching the RESEARCH example (CachedLocation MUST include latitude, longitude, state, postalCode per the STATE.md Dexie schema gap); a `TechSyncDb` Dexie subtype with EntityTables jobs, serviceLocations, equipment, outbox; and a `createTechDb(orgId: string)` factory that constructs `new Dexie(\`TechSyncDb-${orgId}\`)` (per-org partition per Pitfall 8), declares version(1).stores exactly as the RESEARCH example (jobs: 'id, tenantId, status, startDate', serviceLocations: 'id, tenantId, customerId', equipment: 'id, tenantId, serviceLocationId', outbox: 'id, type, syncStatus, createdAt'), and returns the typed instance. Do not store any tenant data in a shared/global DB name; the org id must be in the database name.

    Export `OUTBOX_TYPES` as a const array of the type strings. NOTE on the OUTBOX_TYPES set: this extends CONTEXT D-16 (which lists 7 types) with two additional types — add an inline comment on the const documenting both. (1) `job_signature` per RESEARCH.md Open Question A5: signature mutations need their own outbox slot, separate from job_photo. (2) `manual_payment` per D-03: offline cash/check note only — Square card payments are NEVER queued because tokenization/authorization require live connectivity. The full set is: `job_status_update`, `job_note`, `job_photo`, `job_signature`, `estimate_create`, `estimate_conversion`, `invoice_create`, `send_record`, `manual_payment`.

    Create tests/tech/idb-setup.ts importing `'fake-indexeddb/auto'` and `'@testing-library/jest-dom/vitest'`, with a structuredClone polyfill guard (per PATTERNS.md).

    Update vitest.config.ts setupFiles to include both './tests/setup.ts' and './tests/tech/idb-setup.ts' (append; keep the existing entry). Do not modify the alias array.

    Create tests/tech/dexie.test.ts (RED — will pass once dexie.ts exists): assert createTechDb('org_A') returns a DB whose name includes 'org_A'; assert adding an OutboxItem with syncStatus 'pending' and reading it back via outbox.where('syncStatus').equals('pending').toArray() returns the item; assert a CachedJob added under tenantId 'org_A' is queryable by status; assert two DBs created with different org ids have different names (tenant partitioning); assert OUTBOX_TYPES includes both 'job_signature' and 'manual_payment'. Clean up with db.delete() in afterEach.
  </action>
  <behavior>
    - createTechDb('org_A').name === 'TechSyncDb-org_A'
    - After outbox.add({ id, type: 'job_status_update', payload: {...}, createdAt: Date.now(), retryCount: 0, syncStatus: 'pending' }), outbox.where('syncStatus').equals('pending').toArray() has length 1
    - CachedJob inserted with status 'on_site' is returned by jobs.where('status').equals('on_site').toArray()
    - createTechDb('org_A').name !== createTechDb('org_B').name
    - OUTBOX_TYPES includes 'job_signature' and 'manual_payment'
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/dexie.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/lib/dexie.ts exports createTechDb, OutboxItem, CachedJob, CachedLocation, CachedEquipment, OUTBOX_TYPES
    - OUTBOX_TYPES contains all 9 types including job_signature and manual_payment, with an inline comment citing RESEARCH A5 and D-03
    - CachedLocation interface includes latitude, longitude, state, postalCode fields
    - createTechDb embeds orgId in the Dexie database name (`TechSyncDb-${orgId}`)
    - tests/tech/idb-setup.ts imports 'fake-indexeddb/auto'
    - vitest.config.ts setupFiles array includes both './tests/setup.ts' and './tests/tech/idb-setup.ts'
    - `pnpm test --run tests/tech/dexie.test.ts` exits 0 with all assertions passing
  </acceptance_criteria>
  <done>The per-org Dexie database with cache tables and the outbox (9 types) is defined and verified by passing unit tests using fake-indexeddb.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker → cache | SW controls fetch; scope must be limited to /tech/ so it cannot serve office routes |
| browser → IndexedDB (Dexie) | Cached tenant data lives client-side; must be partitioned per org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Service worker scope | mitigate | Set scope '/tech/' and swUrl '/tech/sw.js' in next.config.ts; manifest start_url '/tech/jobs'; SW fallback only for /tech/offline so it never precaches office routes (RESEARCH Pitfall 4) |
| T-05-08 | Information Disclosure | Dexie cache cross-tenant | mitigate | createTechDb embeds orgId in the DB name (`TechSyncDb-${orgId}`); every cache row carries tenantId; DB cleared on sign-out (later wave) (RESEARCH Pitfall 8) |
| T-05-SC | Tampering | pnpm installs (serwist, dexie, signature_pad, fake-indexeddb) | accept | RESEARCH Package Legitimacy Audit returned slopcheck [OK] for all four packages; no [ASSUMED]/[SUS]/[SLOP] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `pnpm build` exits 0 and emits public/sw.js
- `pnpm test --run tests/tech/dexie.test.ts` passes
- The /tech/ manifest resolves with start_url /tech/jobs and the SW is scoped to /tech/
- Full suite: `pnpm test` green
</verification>

<success_criteria>
- The /tech/ origin is installable (manifest + service worker scoped to /tech/) and renders an offline fallback
- The per-org Dexie database (jobs, serviceLocations, equipment, outbox) opens and round-trips an outbox item in tests
- OUTBOX_TYPES covers all 9 mutation types, documenting the two additions beyond D-16
</success_criteria>

<output>
Create `.planning/phases/05-technician-pwa/05-01-SUMMARY.md` when done.
</output>
</content>
