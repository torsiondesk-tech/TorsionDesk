---
phase: 05-technician-pwa
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - src/app/(tech)/estimates/page.tsx
  - src/app/(tech)/estimates/new/page.tsx
  - src/app/(tech)/estimates/[id]/page.tsx
  - src/app/(tech)/estimates/actions.ts
  - src/app/(tech)/components/estimate-form.tsx
  - src/app/(tech)/components/estimate-card.tsx
  - src/app/(tech)/lib/sync.ts
  - src/app/(tech)/lib/dexie.ts
  - tests/tech/estimate-create.test.tsx
  - tests/tech/estimate-convert.test.ts
autonomous: true
requirements: [TECH-09, TECH-10, TECH-14]
user_setup: []

must_haves:
  truths:
    - "A tech sees the Estimates tab with their estimates (or an empty state) and can open an estimate"
    - "A tech creates an estimate carrying customer, contact, service location, line items, and follow-up tasks; it queues offline if needed"
    - "A tech converts a won estimate to a job in one action; the conversion queues offline and calls the canonical Phase 6 action on sync"
    - "Estimate create and conversion outbox items are visually flagged as pending until synced"
  artifacts:
    - path: "src/app/(tech)/estimates/new/page.tsx"
      provides: "Offline-capable estimate create form"
      contains: "estimate"
    - path: "src/app/(tech)/estimates/actions.ts"
      provides: "PWA estimate action wrappers delegating to canonical Phase 6 actions (stub-safe)"
      contains: "createEstimateAction"
    - path: "src/app/(tech)/lib/sync.ts"
      provides: "estimate_create + estimate_conversion outbox handlers"
      contains: "estimate_conversion"
  key_links:
    - from: "src/app/(tech)/components/estimate-form.tsx"
      to: "estimate_create outbox / createEstimateAction"
      via: "submit → enqueue or direct call"
      pattern: "estimate_create"
    - from: "src/app/(tech)/estimates/[id]/page.tsx"
      to: "estimate_conversion outbox / convertEstimateToJobAction"
      via: "Convert to Job button"
      pattern: "convertEstimateToJob"
---

<objective>
Deliver the estimates surface of the PWA: view the Estimates tab, create an estimate offline-capably (carrying customer, contact, location, line items, follow-up tasks), and convert a won estimate to a job in one action. The PWA is a consumer of Phase 6 canonical actions (D-18); because Phase 6 may not yet exist, this plan defines stub-safe wrappers and full offline queueing so the surface ships now and lights up automatically when Phase 6 lands.

Purpose: Estimates are a core field capability (TECH-09/TECH-10). Shipping the tab + create + convert with the outbox keeps the three-tab shell complete (D-06) and exercises the estimate outbox types (TECH-14) without blocking on Phase 6.

Output: Functional /tech/estimates list, create form, and detail with convert-to-job, all routed through the outbox; estimate outbox types added to Dexie; sync handlers added.
</objective>

<execution_context>
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/marco/Desktop/HOME SERVICE CRM/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-technician-pwa/05-CONTEXT.md
@.planning/phases/05-technician-pwa/05-PATTERNS.md
@.planning/phases/05-technician-pwa/05-UI-SPEC.md
@.planning/phases/05-technician-pwa/05-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (exclude from drift verification):
- `EstimateForm`, `EstimateCard` — React components (src/app/(tech)/components/)
- `(tech)/estimates/actions.ts` — `createEstimateAction`, `convertEstimateToJobAction`, `listTechEstimatesAction` PWA wrappers that delegate to canonical Phase 6 actions when present and return a NOT_AVAILABLE_OFFLINE-safe result otherwise
- `syncEstimateCreate`, `syncEstimateConversion` flush handlers (src/app/(tech)/lib/sync.ts)
- `CachedEstimate` interface + estimates Dexie table (src/app/(tech)/lib/dexie.ts version bump)
</artifacts_this_phase_produces>

<context_interfaces>
Contracts this plan consumes / depends on:
- `createEstimateAction(input)`, `convertEstimateToJobAction(estimateId)`, `sendEstimateAction(...)` — CANONICAL Phase 6 actions (NOT YET BUILT). Per D-18, the PWA delegates to these once Phase 6 exposes them. Until then the PWA wrapper resolves the import dynamically; if the module/export is absent, the action returns `{ error: 'Estimates are not available yet.' }` for online attempts while offline attempts still queue (the outbox item flushes successfully once Phase 6 ships).
- Inline contact+location creation transaction pattern from createCustomer/createJob (STATE.md Phase 3 decision) — the Phase 6 createEstimate will mirror it; the PWA form collects the same newContact*/newLocation* shape.
- `enqueueOutboxItem`, `flushOutbox` dispatch map — src/app/(tech)/lib/sync.ts (Plan 02/03).
- `createTechDb`, `OUTBOX_TYPES` (includes 'estimate_create', 'estimate_conversion') — src/app/(tech)/lib/dexie.ts (Plan 01).
- `toISODate` / `parseCalendarDate` — src/lib/utils.ts (never .toISOString().slice).
- D-19: NEVER call searchPlacesAction / getPlaceDetailsAction from the PWA — use pre-synced location data or plain-text fallback for offline address entry.
</context_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Estimates list tab + estimate detail with stub-safe PWA action wrappers and Dexie estimates cache</name>
  <files>src/app/(tech)/estimates/page.tsx, src/app/(tech)/estimates/[id]/page.tsx, src/app/(tech)/estimates/actions.ts, src/app/(tech)/components/estimate-card.tsx, src/app/(tech)/lib/dexie.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/estimates/page.tsx (Plan 01 empty-state placeholder — replace)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/lib/dexie.ts (Plan 01 — add a CachedEstimate table via a version(2) upgrade; do NOT break version(1))
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(app)/jobs/actions.ts (action auth-guard + extractErrorMessage + logger pattern to mirror)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-06 tabs ship with empty states even if Phase 6 stub; D-18 consumer pattern)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (Estimates empty-state copy; estimate-card content)
  </read_first>
  <action>
    Bump src/app/(tech)/lib/dexie.ts to version(2): add an `estimates` table (stores 'id, tenantId, status, customerId, createdAt') and a `CachedEstimate` interface (id, tenantId, status, customerName, description, value, ratingfollow-up fields). Keep version(1) intact (Dexie upgrade chain).

    Create src/app/(tech)/estimates/actions.ts ('use server'): `createEstimateAction(input)`, `convertEstimateToJobAction(estimateId)`, and `listTechEstimatesAction()` — each reads orgId/userId from auth and attempts to delegate to the canonical Phase 6 action via a dynamic import of '@/app/(app)/estimates/actions' wrapped in try/catch; if the module or named export does not exist (Phase 6 not yet built), return `{ error: 'Estimates are not available yet.' }` (online path). Mirror the photo-action error handling (extractErrorMessage + logger). Document with a comment that these become pass-throughs once Phase 6 ships.

    Replace src/app/(tech)/estimates/page.tsx (RSC): auth-guard; render the tech's estimates from cached Dexie estimates (via a client list) with the empty state "No estimates" / "Create an estimate from a completed job, or pull down to refresh." plus a New Estimate button linking to /tech/estimates/new. Each estimate renders an EstimateCard.

    Create src/app/(tech)/components/estimate-card.tsx ('use client'): a Card showing customer name, description, value, and status badge, linking to /tech/estimates/[id].

    Create src/app/(tech)/estimates/[id]/page.tsx (RSC): show the estimate detail (from cache or server) with a "Convert to Job" primary button. The button calls convertEstimateToJobAction when online; when offline it enqueues an `estimate_conversion` outbox item `{ estimateId }`. Show a pending flag while queued.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -iE "tech/estimates" || echo "ESTIMATES_TYPECHECK_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/lib/dexie.ts declares version(2) adding an estimates table without removing version(1)
    - src/app/(tech)/estimates/actions.ts exports createEstimateAction, convertEstimateToJobAction, listTechEstimatesAction, each delegating to Phase 6 via dynamic import with a stub-safe fallback
    - src/app/(tech)/estimates/page.tsx renders the "No estimates" empty state and a New Estimate link
    - src/app/(tech)/estimates/[id]/page.tsx has a Convert to Job button wired to convertEstimateToJobAction (online) / estimate_conversion outbox (offline)
    - tsc --noEmit clean for the estimates files
  </acceptance_criteria>
  <done>The Estimates tab lists estimates, opens detail, and offers convert-to-job; the action wrappers are stub-safe against the not-yet-built Phase 6 module.</done>
</task>

<task type="auto">
  <name>Task 2: Offline-capable estimate create form + estimate_create/estimate_conversion sync handlers with tests</name>
  <files>src/app/(tech)/estimates/new/page.tsx, src/app/(tech)/components/estimate-form.tsx, src/app/(tech)/lib/sync.ts, tests/tech/estimate-create.test.tsx, tests/tech/estimate-convert.test.ts</files>
  <read_first>
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/estimates/actions.ts (Task 1 wrappers)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(tech)/lib/sync.ts (Plan 02/03 flushOutbox dispatch map — add estimate handlers)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/src/app/(app)/jobs/[id]/line-items.tsx (line-item entry UX to adapt to mobile; D-19 forbids Places API)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-CONTEXT.md (D-16 estimate_create/estimate_conversion; D-18 consumer; D-19 no Places API offline)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-RESEARCH.md (Open Question 2 — cache catalog for offline line items; recommendation: cache active products+services + plain-text fallback)
    - C:/Users/marco/Desktop/HOME SERVICE CRM/.planning/phases/05-technician-pwa/05-UI-SPEC.md (form input text-base 16px; thumb-first)
  </read_first>
  <action>
    Create src/app/(tech)/components/estimate-form.tsx ('use client'): a mobile form collecting customer (select from cached customers OR newContact*/newLocation* plain-text fields — never the Places autocomplete, D-19), service location, category, line items (cached products/services with a plain-text fallback line per RESEARCH Open Question 2), and follow-up tasks. All inputs use text-base (16px) to avoid iOS zoom. On submit: when online call createEstimateAction(input); when offline enqueue an `estimate_create` outbox item with the full payload and show the pending flag. Use toISODate for any date fields.

    Create src/app/(tech)/estimates/new/page.tsx (RSC): auth-guard, render EstimateForm with cached customers/locations/catalog passed as initial data (synced via hydrateTechData; extend hydration to cache active products/services).

    Extend src/app/(tech)/lib/sync.ts flushOutbox: add `syncEstimateCreate(item)` → createEstimateAction(payload), and `syncEstimateConversion(item)` → convertEstimateToJobAction(estimateId); mark synced on success, failed+retry on error. If the action returns 'Estimates are not available yet.' (Phase 6 absent), keep the item pending (do NOT mark failed) so it flushes once Phase 6 ships.

    Create tests/tech/estimate-create.test.tsx (fake-indexeddb + mocked action): render EstimateForm, fill required fields, simulate offline submit, and assert an estimate_create outbox item is enqueued with the entered customer + line items in the payload. Then mock createEstimateAction success, run flushOutbox, and assert the item is marked synced.

    Create tests/tech/estimate-convert.test.ts (fake-indexeddb + mocked action): enqueue an estimate_conversion item; mock convertEstimateToJobAction success and assert flushOutbox calls convertEstimateToJobAction(estimateId) and marks synced; mock the action returning 'Estimates are not available yet.' and assert the item REMAINS pending (deferred until Phase 6) rather than failed.
  </action>
  <behavior>
    - Offline estimate submit enqueues an estimate_create item containing customer + line items
    - flushOutbox estimate_create handler calls createEstimateAction(payload) and marks synced on success
    - flushOutbox estimate_conversion handler calls convertEstimateToJobAction(estimateId)
    - When the canonical action reports "not available yet" (Phase 6 absent), the outbox item stays pending (not failed)
    - The form never imports searchPlacesAction/getPlaceDetailsAction (D-19)
  </behavior>
  <verify>
    <automated>pnpm test --run tests/tech/estimate-create.test.tsx tests/tech/estimate-convert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(tech)/components/estimate-form.tsx collects customer/location/line-items/tasks with text-base inputs and no Places API import
    - Offline submit enqueues an estimate_create outbox item; online submit calls createEstimateAction
    - src/app/(tech)/lib/sync.ts handles estimate_create and estimate_conversion, keeping items pending when Phase 6 is absent
    - `pnpm test --run tests/tech/estimate-create.test.tsx tests/tech/estimate-convert.test.ts` exits 0
  </acceptance_criteria>
  <done>A tech can create an estimate (offline-capable) and convert one to a job; both route through the outbox and degrade gracefully until Phase 6 ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA → Phase 6 estimate actions | Tech-submitted estimate payload crosses; server enforces tenant + role |
| Dexie estimate cache → UI | Cached estimates must be tenant-partitioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09a | Tampering | Estimate create/convert action | mitigate | PWA wrappers read orgId/userId from auth and delegate to canonical Phase 6 actions that enforce withTenant + role; the PWA does not write estimate rows directly |
| T-05-15 | Information Disclosure | Estimate cache cross-tenant | mitigate | estimates table lives in the per-org Dexie DB and carries tenantId; cleared on sign-out |
| T-05-16 | Input Validation | Offline estimate payload | mitigate | Phase 6 createEstimateAction validates the payload (Zod) on sync; the PWA does not trust client payloads as authoritative |
| T-05-17 | Tampering | Places API in offline form | mitigate | D-19 enforced — estimate form uses pre-synced location data / plain-text fallback only; no searchPlacesAction import |
</threat_model>

<verification>
- `pnpm test --run tests/tech/estimate-create.test.tsx tests/tech/estimate-convert.test.ts` pass
- Estimate wrappers degrade gracefully (no crash) when Phase 6 module is absent
- No Places API import in any (tech)/estimates file
- Full suite: `pnpm test` green
</verification>

<success_criteria>
- The Estimates tab lists estimates and opens detail with convert-to-job
- A tech creates an estimate carrying customer/contact/location/line-items/tasks, offline-capable
- Estimate create + conversion queue in the outbox, are visually flagged pending, and sync once Phase 6 ships
</success_criteria>

<output>
Create `.planning/phases/05-technician-pwa/05-04-SUMMARY.md` when done.
</output>
