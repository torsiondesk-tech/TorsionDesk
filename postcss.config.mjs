/**
 * Tailwind CSS v4 uses the dedicated PostCSS plugin (no tailwind.config.ts
 * required — theme is declared inline via `@theme` in globals.css).
 */
const config = {
  plugins: ['@tailwindcss/postcss'],
}

export default config
