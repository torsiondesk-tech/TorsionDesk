'use server'

/**
 * Canonical Phase 8 customer communication actions.
 *
 * This module is a placeholder during Phase 5 (technician PWA). The PWA
 * wrappers in src/app/(tech)/tech/invoices/actions.ts dynamically import this
 * module and degrade gracefully when the expected exports are missing.
 */
export {}
