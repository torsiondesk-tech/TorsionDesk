'use server'

/**
 * Phase 6 stub — estimates surface is not implemented yet.
 *
 * This module exists only so the technician PWA's stub-safe wrappers can
 * dynamically import it without breaking the Vite/Vitest resolution graph.
 * It intentionally exports no actions; the PWA wrappers treat the missing
 * exports as "not available yet" and queue offline work in the Dexie outbox.
 */

export {}
