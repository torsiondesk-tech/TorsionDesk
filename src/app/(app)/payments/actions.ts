'use server'

/**
 * Canonical Phase 7 Square payment action.
 *
 * This module is a placeholder during Phase 5 (technician PWA). The PWA
 * wrapper in src/app/(tech)/tech/invoices/actions.ts dynamically imports this
 * module and degrades gracefully when the expected export is missing.
 */
export {}
